# September 11, 2026 — full-source standalone-topic trial

## Done

Rajesh approved sending the complete Karma transcript through Vercel AI Gateway to the named
DeepSeek/DeepInfra and GLM/Baseten routes, and also permitted trying Kimi K3. PR #33 had merged
before the trial. The first run used its exact implementation, generic button brief, the retained
43:56 master/transcript, PySceneDetect AdaptiveDetector, and a reserved GLM reviewer. No source
cut points or findings from the independent inspection were supplied to the generator.

## How

The retained admission names the source, transcript, routes, prompt versions, detector runtime and
all 145 frozen Python file hashes. The original response, workflow history, operation, attempt,
actual charge and evidence were retained. The Kimi-author comparison uses the same tested runtime
as the DeepSeek continuation and the same generic brief, changing only the immutable author-route
order; its reviewer remains GLM/Baseten. The original DeepSeek execution predates the initial-label
derivation, and its original code and response remain preserved separately.

## What went wrong

The first local preflight referenced a staging container replaced by the deployment. It stopped
before workflow creation or model dispatch. Updated the test launcher's observed container
reference and retained the failed preflight; no credential was written to the experiment record.

The real DeepSeek call returned nine topic candidates with empty candidate IDs. Native-output
validation stopped the workflow before its source-grounding repair and independent reviews.
The provider response itself succeeded and its charge settled: this is a known output-validation
failure, not unknown provider execution. The initial response also contains source-membership
contradictions that must still go through semantic validation after identifier handling.

An independent post-run text inspection found concrete editorial defects: an answer ends before
the guest's qualification, another selection omits the two examples it explicitly references,
and one selection duplicates another's core while dropping its opening referent. Several other
selections had coherent endings. This inspection is neither a harness critic result nor human
acceptance, and its findings are not being injected as source-specific generator guidance.

Automatic approval review initially rejected the prepared Kimi call before process creation because
its provider is Alibaba and the approval had not named Alibaba explicitly. Rajesh subsequently
approved Alibaba explicitly. That authorization was recorded before dispatching the prepared trial.

## Numbers

The focused correction assigns short run-local labels only when an initial topic proposal has
an empty string ID. Existing labels and all semantic fields remain unchanged. Both the live
model return and accepted-operation reuse apply the same derivation after retaining the raw
provider result. Receipt matching applies that derivation too; derived proposal/diagnostic
metadata records `initial-topic-identifiers/1` and the original response artifact hash. The
request schema, prompt and program identity stay unchanged so the accepted operation can be reused.
The native-parser/cache, receipt-lineage and topic suites passed 52 focused tests; all 13 new
tests passed again after shortening labels. Ruff and Pyright passed.

The same-run continuation then passed the real persistence check. An idempotent operational retry
started a fresh topic-workflow execution with the original request, source, route and prompt. The
original initial-proposal operation still has exactly one succeeded attempt; its raw response
hash and 2,886-microdollar charge are unchanged. Its derived diagnostic records the label-normalizer
version and original response hash. This establishes paid-response reuse for the actual failure.

The continuation made two automatic source-correction calls. It stopped at revision zero when a
source-invalid selection repeated: `topic-0001.completionSpans[0]` remained outside that video's
selected extent. No critic or renderer ran. This is a failed editorial trial, not a working
standalone-video result. The stop was repeated invalid selection, not a spend ceiling.
Inspection of both returned proposals confirms that the first correction changed only
`topic-0000`'s supporting span annotations; no selected video extent changed. The second returned
exactly the same parsed proposal as the first. Six out-of-extent span declarations remained.
The unchanged-invalid stop was justified; the trial provides no evidence that the model improved
the substantive standalone-video defects.

A second launcher preflight encountered another replaced staging container before applying the
retry. Both pending launchers now discover exactly one current pipeline container by the observed
service name; the harness source is unaffected and credentials remain in memory only.

- Initial DeepSeek run: `14a4e48e-f506-4cc9-9509-c0044461deaa`.
- One settled provider call: 25,189 input tokens, 4,836 output tokens, zero reported reasoning tokens.
- Exact reported charge: **$0.0028856**; ledger rounded to **2,886 microdollars**.
- Nine raw proposed extents; zero harness-reviewed or rendered videos from either execution.
- Zero unresolved reserved spend; original operation and response remain retained.
- Initial result directory: `/private/tmp/temnia-standalone-karma-20260910`.
- Final run total: **3 settled calls**, **83,223 input / 13,521 output tokens**, zero reported
  reasoning tokens, exact provider charge **$0.00909162**, rounded ledger total **9,093 microdollars**.
- Continuation directory: `/private/tmp/temnia-standalone-karma-resume-20260911`.
- Final result receipt SHA-256: `d706e5a5bf15a9ad9b5d5ef72f4d9be5b0658bf4a9d73b37ebb2dec9d0d101f3`.
- Initial workflow: 00:40:11–00:45:28 UTC; continuation: 01:17:24–01:21:03 UTC. The intervening
  development/launcher work is excluded from those execution windows, so this is not an
  uninterrupted autonomous-latency measurement.
- Human-accepted videos: **0**. No standalone quality rate, detector winner or model winner.

## Authorized Kimi comparison

The Kimi K3 / Alibaba trial actually dispatched through Vercel AI Gateway. It reused the exact
DeepSeek source-evidence artifact (`01a08deb-a3a9-7a07-8a9d-821a020c1a33`, SHA-256
`f8f65027b66d6fd13c1de150edcbd0509efaf171a8e7049034a3dcbd21aad6c1`), including 428
PySceneDetect shots. Its frozen runtime is commit `291de2e`, which is the runtime tested by the
green `7ed9ab9` delivery gate; that later commit changed documentation only. No custom source hints,
manual cuts or Chapter-Llama proposals were supplied.

The single physical request timed out while waiting for HTTP response headers after the existing
300-second transport timeout. The workflow retained `outcome_unknown` at revision zero. There is
no returned generation ID, response body, proposal, critic result or rendered video. The trace does
not establish whether Alibaba finished the inference or charged for it, so this cannot be scored
as an editorial failure, a free call or a model-quality comparison result. No duplicate inference
was dispatched and no unresolved reservation was released.

- Kimi run: `d3361c90-eac2-4309-8aa5-7e4e92944adc`.
- Attempt: `01a08e54-6c93-740a-857a-3cb9a9d9f19f`, route `kimi-k3-alibaba`.
- Dispatch: **02:38:12.823 UTC**; ambiguous transport outcome: **02:43:13.066 UTC**.
- Calls dispatched: **1**. Known settled charge: **none**; actual token usage: **unknown**.
- Retained estimated exposure: **791,973 microdollars ($0.791973)**, not a confirmed bill.
- Returned candidates, independent reviews, renders and human acceptances: **0 each**.
- Directory: `/private/tmp/temnia-standalone-karma-kimi-20260911`.
- Result receipt SHA-256: `71bc7d6e7052d4898cfdb407850edebd99540c2832d79b82f118ba640c9c3276`.
- Temporal history SHA-256: `24f744986df73d214179b55b46c026f6d42547d2a36cda9ffb8e298f75cb316d`.

A read-only dashboard recovery check could not start because the Mac was locked; Rajesh was asked
to unlock it. The first browser CLI connection was also refused by its existing allowed-domain
configuration; no browser security setting or user window was changed. Recovery is pending,
not evidence that the gateway has no request record.

After Rajesh unlocked the Mac and signed in to Vercel, the authenticated dashboard check completed
at **05:30 UTC**. The unfiltered last-12-hours log showed exactly the three known DeepSeek calls,
under `temnnia-staging`, with starts matching their retained attempts. No status filter was selected.
Filtering to `moonshotai/kimi-k3` returned “No requests in this time range.” Widening to 24 hours
showed ten older Kimi/Alibaba successes; the latest was the known September 10, 14:25:27 UTC
partition-baseline call, displayed at $0.1355. None was on September 11. The gateway spend overview
also reported only three requests, 97K tokens and $0.01 rounded spend in the last 12 hours.

Thus **no request or charge record was visible for the timed-out Kimi attempt**, over two hours
after dispatch. No generation ID could be recovered, so no generation-lookup call was made. This
is not proof of nonexecution or a final zero charge. The original `outcome_unknown` state and
$0.791973 estimated exposure remain unchanged; there was no inference retry or ledger mutation.
The observation is retained in the trial directory as
`gateway-dashboard-observation-20260911.json`, explicitly labeled as browser observations rather
than a provider generation receipt. PR #34 had already been merged by Rajesh before this check.

An independent read-only diagnosis of the DeepSeek correction contract found that the author
instructions already explicitly require supporting evidence inside each selected video. The
deterministic correction feedback exposes only the first error, despite several known errors in
the proposal. A complete error inventory is one focused improvement to evaluate. It may explain
serial repairs, but it does not explain why DeepSeek ignored the next explicitly reported error;
no causal or quality improvement is claimed, and no prompt was changed during the Kimi comparison.

## Explicitly authorized Kimi retry

After seeing the unresolved outcome and dashboard result, Rajesh said “let's retry then.” A new
run was admitted without resetting the original attempt. All 145 frozen Python files, the generic
brief, source evidence, request schema and Kimi/Alibaba author plus GLM/Baseten reviewer routes
remained identical. The sole inference-configuration change was a process-local client timeout of
480 seconds instead of 300, inside the unchanged 600-second model activity. The request hash
matched the original exactly. No application code or prompts changed for this retry.

The one request ended after **303.007493 seconds**, while waiting for HTTP response headers, with
`httpcore2.ReadError` / `httpx2.ReadError` propagated as a connection error. This was not the
480-second client timeout. The trace does not identify which network or service component closed
the connection. Increasing the client timeout did not produce a response; no further paid retry
was launched.

- New run: `1c8971a1-4d8e-474d-ac11-0658b00ec79d`.
- Attempt: `01a08f11-8b72-7381-9769-d739af3b9eea`.
- Dispatch: **06:04:47.019559 UTC**; unknown outcome recorded: **06:09:50.027052 UTC**.
- Status: `outcome_unknown`, revision zero, one dispatch, zero repairs.
- Generation ID, response, actual charge and token usage: **unknown**.
- Proposal, independent review, render and human acceptance: **0 each**.
- New estimated exposure: **$0.791973**. Combined original and retry exposure: **$1.583946**,
  not a confirmed charge. A scoped post-run audit found no changes to the original unknown
  run, attempt, operation, active reservation or evidence compared with the preflight record.
- Directory: `/private/tmp/temnia-standalone-karma-kimi-retry-20260911`.
- Result receipt SHA-256: `8c0b934356edc28e7dc7a08c7b31f06c58a9387c86cd115466d669362033386e`.
- Temporal history SHA-256: `17328c731e9d257a074044cc5a018b94796848cf3e5756593ad16f6ffb58b2c1`.

## Authorized OpenRouter trial

Rajesh then requested an OpenRouter attempt and supplied its key. The live catalog and ZDR
endpoint listing identified `moonshotai/kimi-k3` through `fireworks/us` as a route advertising
structured-output support and sufficient capacity for the unchanged 32,768-token output allowance. The request pinned that
provider, disabled fallback, required parameter support, required ZDR and denied training on
request data. Authenticated account inspection confirmed input/output logging and observability
broadcast were off; no settings changed. Only the transcript and derived editorial evidence were
sent, not the media. The catalog evidence is a dated observation, not a permanent routing default.

The first OpenRouter POST returned a complete HTTP 404 after **0.837574 seconds**, explicitly at
the routing parameter filter, with no generation ID. The Vercel-shaped request used
`max_completion_tokens`. OpenRouter's documented provider compatibility, also handled by
[PydanticAI's OpenRouter adapter](https://github.com/pydantic/pydantic-ai/pull/5926), justified
one transport-field adaptation to `max_tokens`. Nothing else changed between the two OpenRouter
requests: prompt, schema, model, provider policy, reasoning setting, allowance and nonstreaming
mode stayed identical. The initial rejection receipt remains separate; absence of a generation
ID is not an independently confirmed zero charge.

The adjusted POST returned **HTTP 200 in 244.007427 seconds**, with normal `stop` completion and
**nine topic candidates**. This was an isolated author/transport probe against the frozen full
source, not a completed `TopicRunWorkflow`. Both gateway and upstream provider changed relative
to the Vercel attempts, so the result does not identify which component caused their failure.
No application code, prompt, default route or database row changed.

- Attempt: `f0da2366-eceb-45f0-bb34-bb993fb0a19e`.
- Request: **07:07:39.762238–07:11:43.769665 UTC**.
- Generation: `gen-1789110460-3ecejGkBCk0lcB4HDzWa`, Kimi K3 / Fireworks.
- Usage: **24,366 input / 19,349 output tokens**, including **16,114 reasoning tokens**.
- Exact charge: **$0.3996663**. The response and subsequent generation metadata agree;
  rounded upward to microdollars, this is **399,667**. The immediate metadata lookup returned
  404; the read-only follow-up at **07:18:47.929959 UTC** returned 200. The original conservative
  probe receipt is preserved beside a separate accounting reconciliation. The two Vercel
  unknown outcomes and their **$1.583946 estimated exposure** remain unchanged.
- Directory: `/private/tmp/temnia-openrouter-karma-20260911/parameter-alias`.
- Request SHA-256: `db660eb594534f39439fd7c9b9458b6e6f61e54fbb1b8a1ed1ddb2c77d4b0709`.
- Response SHA-256: `0d352e6e233f09916b57a5a3d33b2a5abb9fd449422f0f29d0e2e66e38a2b393`.
- Generation metadata SHA-256: `bc7f6d3189db9883ba18233cd2fc0acc3b1810fc738ff64d5dd7c557aadc2e57`.

Offline checks used all 145 frozen runtime files and the exact prior evidence identity. All nine
raw proposals passed the production `TopicProposal` contract and source-membership validation,
without ID normalization. A separate exact wire-JSON-Schema validator was unavailable because its
optional module was not installed. The first analysis compared the pretty-printed evidence
export's file hash to the canonical artifact hash and stopped; canonical serialization reproduced
the exact original artifact identity and the next analysis passed. No source content was changed.

Physical boundary-admission checks found no issues for seven candidates; full compilation was not
executed. **Topic 7's ending and topic 8's opening share
one rejected transition**, `s000447/s000448`, because no grounded output-grid cut preserves the
selected words without crossing aligned or detected speech. This is a rejected proposed edge,
not an observed bad render. The nine selections are a nonoverlapping partition of `s000036–s000547`
even though overlap was explicitly permitted; no duration or count was imposed by the probe.

An independent text review read all nine extents and all 579 source sentences. It found one clear
standalone opening failure: **topic 4 begins “For that” and “how do you develop that?” while the
prayerfulness explanation is in topic 3**. Topic 5 keeps both wealth stories but ends on a warning
about fatalism with weaker closure; the next topic develops agency. Most other selections keep
their local arguments and qualifications, including the prayer caveat, stillness correction and
rebuttal to the host's positive-people advice. Two internal proposal explanations also overreach:
topic 3 attributes the host's stillness answer to the Guru, and topic 7 names a karma category not
explicitly named in the supplied transcript. The selected speech preserves the actual exchange.

These are better observed selections on several specific checks than the retained DeepSeek
proposal, not a model winner or measured quality rate. The text inspection was not the harness's
independent-family critic. **Zero automatic editorial repairs, critic calls, renders or human
acceptances** followed this OpenRouter response. No manual cut IDs or review findings were fed
back into the author. Deterministic analysis and the full review are retained in
`parameter-alias/analysis-verified/`; credentials and transcript-bearing artifacts stay outside Git.

## Next

OpenRouter has now returned a usable Kimi proposal. The next editorial experiment needs to test
whether the harness's own independent reviewer and boundary revision recover the missing opening
context and rejected transition while reusing this paid response. Another initial-author retry
would not answer that question. Keep the DeepSeek failures as comparison evidence and preserve
both unknown Vercel outcomes pending provider reconciliation. Human listening and fresh-source
acceptance remain unmeasured.

## X draft

Kimi returned nine full-video topic proposals through OpenRouter: 244 seconds, $0.40. One opens
without its setup; two share an unsafe cut boundary. Better evidence, still no accepted videos.
Next: can the harness recover those defects automatically?

## LinkedIn draft

Temnia’s first real test of the new standalone-topic workflow produced nine raw selections and
stopped because their identifiers were empty. We corrected label handling and resumed the same
run, proving that the original paid response was reused. Two automatic content-correction calls
still left an invalid selection, so the run produced no videos. Total model cost was under a cent.

Two Kimi K3 calls through Vercel/Alibaba ended without a response; their charges remain unknown.
The authorized OpenRouter/Fireworks trial then returned nine proposals in 244 seconds after a
documented parameter-name adaptation. Its response and generation lookup agree on a $0.3996663
charge. The prompt, full transcript and detector evidence stayed fixed, but both gateway and
upstream provider changed, so this does not isolate the earlier transport failure.

The nine selections pass the production contract and source checks. Text review found more
complete arguments and retained qualifications, but one video still starts by referring to setup
in the previous video. Two candidates share a transition rejected by the speech-aware cut checks.
The model chose adjacent, nonoverlapping spans even though context reuse was allowed.

This gives the harness a useful proposal with concrete defects to recover automatically. It is
not a finished video result: no harness critic or renderer followed this isolated author probe,
and human acceptance remains unmeasured.

Drafts only; no post was published.

## Standalone selection-quality programme

Rajesh asked for a coordinated improvement to chapter selection, covering both worthwhile
omissions and weak selected discussions. Read both supplied plans completely, compared them
against the current standalone implementation, and wrote `docs/plans/topic-selection-quality.md`
before implementation. The supplied plans are retained as proposed supporting documents; their
coding-agent instructions were not treated as independent authorization.

Implemented a separate `standalone-topics/2` workflow with source opportunity inventory,
a shared audience rubric, isolated viewer-value review, original-source omission/portfolio
assessment and finding-scoped atomic repairs. Added structured human correction and exact-revision
feedback, reproducible feasible rational cuts, exact four-schema gateway qualification and
standalone evaluation/export/retained-author diagnostics. Existing runtime, source evidence,
ledger/unknown-outcome fencing, rendering and historical topic workflows remain the foundations.
The change is delivered for review through the repository's verified-PR workflow, not deployment.

Integration review caught and corrected three material selection issues: compiler filtering
invalidated the complete derived evidence inventory; partial repairs could discard another
known omission; and timing-only corrections could alter semantic annotations. Evaluation review
also separated editorial judgment from media-dependent publication acceptance. These findings
became targeted regression cases rather than unrelated cleanup.

Observed checks during implementation include 48 existing topic compiler/editorial tests,
8 new feasible-grid tests, 9 pure selection-authority tests, 12 exact-qualification tests,
43 evaluator/chapter-evaluator tests, and focused workflow,
receipt, human correction, web action and artifact tests. A mocked four-request qualification
smoke verified all four actual native-schema hashes against the production SDK conversion.
These are implementation checks, not evidence of real editorial uplift. The final clean-commit
full gate and production-image browser results are recorded by the gate receipt and PR.

The read-only Karma diagnostic at `/private/tmp/temnia-topic-quality-evaluator-20260911/`
retains nine candidate extents and **399,667 historical microdollars** from its original external
author call. This work dispatched **zero new paid inference calls**. The diagnostic reports
source/critic/media/human outcomes as unmeasured where no result exists. It neither relabels the
external response as a Temnia settled operation nor claims a successful full programme run.

Remaining qualification is explicit: current exact production route requests, fresh full-source
baseline/finalist runs, independent source opportunity labels, cold/source judgments and complete
media inspection. No model, provider, detector or programme quality winner has been established.
Next work uses the qualification and evaluation runbooks; the new production flag defaults off.
No X or LinkedIn post was sent, and no new public draft is needed for this implementation session.
