# 2026-09-09 — Chapter harness integration and qualification

Continuation of the September 8 working session after midnight in Asia/Kolkata.
PR #24 remains the delivery branch; the runtime has not been merged or deployed to shared staging.

## Done

The recorded-provider browser journey reached a rendered chapter edit, structural corrections,
human acceptance and a downloadable export. Later edits and cancellation preserved the accepted
output. Acceptance reused the existing checked media and made no additional model calls.

The scoped evaluation exporter ran against the actual browser database and media store. It
preserved the distinction between current revision 14 and accepted revision 13, all ten synthetic
attempts and their cost provenance. A failed run without source evidence exported an explicitly
unmeasured source fingerprint. These are actual system runs with synthetic model responses, not
evidence of model quality or live inference cost.

The corrected live short GPU run then completed all three stages. A deliberately lost activity
result recovered from checkpoints with three physical dispatches before and after. The existing
151-minute audio also completed through the isolated stages, producing 20,588 words and two
speakers. Independent coverage retained a listening-review warning.

## How

Used the real local database, object store, Temporal worker, ffmpeg renderer and browser. A frozen
source copy kept browser workflow imports stable while the final isolated fixes were tested.
Generated Python date-time contracts now validate timezone-aware values without applying a string
regular expression after parsing. The TypeScript and JSON Schema wire validation remains intact.

An actual Temporal cancellation regression deliberately delayed cleanup in two parallel activities.
The workflow now waits for both cleanup acknowledgements before writing its cancelled state.
Separate tests cover cancellation before dispatch, an uncertain submission acknowledgement, a
known remote handle, and idempotent speech-run settlement.

## What went wrong

Export first failed because the Python contract generator applied an RFC3339 string pattern to a
parsed datetime. Fixing the generator allowed the browser to reach export and later recovery
controls. Starting a second chapter run on the same source then exposed an artifact identity
conflict. Evidence publication carried a consumer run ID despite having a source-scoped identity.
Removing that transient metadata, while preserving strict bytes and dependency checks, fixed the
second run. Both runs share one accepted evidence artifact and one storage ledger entry.

The first live GPU qualification then exposed a separate integration defect: recognition and
alignment succeeded in their own containers, but diarization referenced a WhisperX submodule that
the isolated process had not imported. All three attempts reached known terminal states; the two
accepted checkpoints and independent coverage artifact were preserved and hash-verified. The
import correction follows the pinned release's own explicit submodule import. This failed run
remains part of the accounting; the replacement short and long tests use tighter limits to remain
within the original aggregate exposure ceiling.

A final rollout audit also removed the worker's local fallback for an omitted deployed speech build
ID and its disconnected progress-Dict override. Boot now requires the explicit deployment identity;
reader and writer use the protocol's fixed progress channel. Required media-check warnings remain
visible and eligible, while missing checks and failures block acceptance; the docs now state that
rule precisely. Route schema validation is also distinguished from separately retained audition
evidence.

The standalone schema audit found a generator ordering defect: files emitted before their
dependencies were visited contained unresolved references. The aggregate used by Python was
complete. The correction builds all definitions before selecting each contract's reachable set;
the integrity check covers generated documents as well as ordinary contract validation.

The first full gate passed all 635 Python tests but revealed that Turborepo filtered the
database variable from the web task, silently skipping its two PostgreSQL regressions. The
verified push was stopped before publication. The web task now explicitly receives its test
database and local-gate flag, disables database-test caching, and refuses to skip those tests
inside the gate. Final delivery must execute them.

The production browser run then passed the full chapter journey and found three older regressions.
Two were stale test interception contracts: correction commands now carry a revision inside an
object, and their artifact filenames include a mutation UUID and hash. The third was a real
deletion regression: treating every failed workflow as an uncertain external writer also fenced
files rejected during probing. The correction requires bounded, complete evidence that the exact
failed run never scheduled a media writer; the original deletion assertion stays intact.

The next production run passed 17 of 18 browser tests, including deletion and late-save recovery.
The remaining test failed in its own setup: the initial machine revision has no persisted edit
annotations. It now captures the displayed original turn's word indices and independently derives
their documented machine identities before delaying the next revision. The exact revision and
target-word assertions remain intact.

Enabling the web database regressions also exposed a parallel-test isolation race. The database
suite selected an arbitrary project from the seeded organization, which could belong to another
suite and disappear during its cleanup. Its own harness cleanup was also unbounded. The correction
tracks exact fixture identities for isolation assertions and removes only the suite's own graph,
preserving the cross-tenant assertions while allowing database suites to run concurrently.
The update-denial probe also now issues a harmless same-ID SQL update; an empty update followed
by a catch had previously accepted a query-builder error as evidence of database rejection.

## Numbers

- The full gate before the final browser-test setup correction passed all 187 web tests, including
  all three real database regressions with zero skips, and all 635 Python tests in 66.92 seconds.
  Both deploy images built. Its browser result was 17 passed, one setup failure; it did not publish
  a successful receipt. Final delivery requires the complete gate to pass on the final commit.
- The separate real database isolation suite passed all 41 tests.
- Repository formatting checked 150 files without changes.
- The focused harness/evaluation batch passed 102 tests before the second-run identity fix.
- Five focused speech tests passed, including actual Temporal cancellation and PostgreSQL settlement.
- Actual evaluation bundle: 83,976 bytes, ten synthetic attempts, zero live-provider attempts.
- Corrected Python preflight: 624 passed, 11 skipped; a separate explicit model-loading run then
  passed all 11 model tests in 13.98 seconds.
- Shared contracts: 28 passed, including standalone reference integrity. All four chapter/structural browser journeys passed together in
  2.1 minutes. Retrying an actual earlier failed export also reached ready on the same revision,
  retaining the dispatch count of nine.
- Live GPU qualification: three attempts in the first known failed short run, three in the corrected
  short recovery test and three in the long run. All nine terminal attempts retain 11,625,003 micros
  of unknown-cost exposure; no invoice cost is known.
- Long workflow: 18m4.81s; stage function total 1,021.261s. Sampled GPU device peaks were
  10,391,388,160 / 3,146,776,576 / 2,363,490,304 bytes for recognition/alignment/diarization.
  Process high-water values were unavailable. This run was slower than the old combined path.
- Exact-commit production-image verification is delivered through the local receipt and required
  GitHub provenance check on PR #24, separate from these preflight counts.

## Tomorrow / next

Review the delivered PR before the paired rollout. Qualify live model transport/privacy once the
gateway credential is configured, collect human boundary/transcript labels, and reconcile actual
invoice costs. Compare model choices and isolated-stage latency using controlled measurements.

## X draft

Temnia's chapter workflow now reaches accepted export in the browser. A later edit or cancellation
keeps that accepted version available. Approving chapters reuses checked media without another model
call. The tests also found and fixed a second-run identity bug. A 151-minute recording completed through
separate GPU stages; it still needs human accuracy review.

## LinkedIn draft

The chapter editing harness reached an important integration point: a real browser journey can
render chapters, correct boundaries, accept the result and export it. Further edits and cancellation
preserve the last accepted output, and approval itself makes no additional model calls.

The useful failures were outside the first happy path. Export exposed a generated datetime
validator bug. Running the same source again exposed an artifact identity conflict. A cancellation
test showed that requesting cancellation was not the same as waiting for both activities to clean up.

These tests use synthetic model responses with real storage, workflows and media rendering. They
prove those mechanics; they do not establish editorial accuracy or live model economics.


## Build-in-public rewrite — current status — 2026-09-09 06:17 IST

### Source record and scope

The build-session record above is preserved verbatim from runtime commit `720544d49c26a952883ace1ae8aaf6cbfbef2702`;
its SHA-256 is `bda9c32c0f9d8283f2f5fb80fcbfc21a8bd7574ba29fcfaabf1eb12ee7493972`. It describes that
session's work, not new runtime work by the editorial session. The earlier gate failures and
preflight counts are chronological records; the exact final receipt and required status below
establish the subsequently successful delivery. This docs branch preserves the existing daily
record before adding today's posts, without modifying the separate runtime worktree.

Rajesh asked to rewrite the posts after analyzing current status. These five drafts replace the
September 8 pack for a new review on September 9. Keep the earlier pack as dated history. Use the
confirmed Premium+ preference for three concise founder X posts, one standard-length product X
post, and one personal LinkedIn post. This retains the requested expanded pack, not a new daily
quota. Each X post has one distinct takeaway; LinkedIn develops a separate verification lesson.

No current publication history is inferred from yesterday's confirmation. This session has not
published, scheduled, edited profiles, run new media/model tests or changed runtime code.

### Current-state audit

| Item | Verified state at inspection |
| --- | --- |
| Runtime delivery | PR #24 is open, ready for review, and unmerged at `720544d49c26a952883ace1ae8aaf6cbfbef2702`. Its worktree is clean. |
| Final runtime gate | Exact-SHA receipt completed 2026-09-08 20:57:25.579 UTC (September 9, 02:27:25 IST); includes full package checks, both images and production-image chapter render/review/export. |
| GitHub provenance | Required `checks` run completed successfully at 2026-09-08 20:58:15 UTC. The earlier draft-triggered skip is not used as pass evidence. `local-ci` also reports success for the same SHA. |
| Shared staging | Remains the prior merged `555eb248bb309855279e3f722c676eb0a43dabcd` runtime, per main and rollout records. No new shared rollout is claimed. |
| Chapter workflow | Fixed/synthetic editorial responses, actual local database, storage, Temporal and media rendering; review, accepted export, second run, later edits/cancellation covered. Browser clicks on Accept are not human editorial-quality labels. |
| Live GPU recovery | Separate 8.824-second qualification: lost activity result after saved checkpoints, activity attempts 1 and 2, physical dispatches 3 and 3. |
| Latest long GPU run | Same 9,060.473-second audio; 1,084.81-second workflow (about 18 minutes), three isolated stages, 20,588 words. No failure was injected in this long run. |
| Comparison | Earlier combined-path run: 660.264 seconds (about 11 minutes), 20,587 words. These observations do not isolate the cause of the slower run or establish a cost/quality win. |
| Remaining evidence | Live editorial-model transport/auditions, human chapter/transcript labels, actual invoices, broad memory/language claims and live OOM recovery remain unqualified. |

Runtime [PR #24](https://github.com/TemniaHQ/temnia/pull/24) and its
[successful required check](https://github.com/TemniaHQ/temnia/actions/runs/34277856922/job/102235204834)
are separate from the docs-only gate for this editorial commit on PR #25. Neither a docs gate nor
synthetic proposals establish live editorial-model performance.

### What changed since the previous drafts

- The chapter implementation is committed and has passed the full gate; it is no longer accurate
  to describe the complete browser render/review/export path as wholly future work.
- The corrected live GPU qualification goes beyond the prior CPU crash probe. Its short result-loss
  recovery and the long execution run are different tests and stay separate in public copy.
- The 11-minute number belongs to the earlier combined path. The latest isolated-stage observation
  is about 18 minutes. An extra transcript word is not proof of better accuracy.
- Current branch progress hides an unmeasured compute percentage and reports stage plus update
  freshness. That is time since a heartbeat, not total elapsed compute or measured completion.
  The old "next change is stage and elapsed time" wording is retired.
- The sentence redesign occurred on September 7; calling it "yesterday" on September 9 would be
  wrong. The new pack leads with fresher evidence and uses no ambiguous day labels.

### Evidence map

| Draft | Evidence and wording boundary |
| --- | --- |
| XP1 | [Browser review/export](https://github.com/TemniaHQ/temnia/blob/720544d49c26a952883ace1ae8aaf6cbfbef2702/apps/web/e2e/chapters.spec.ts#L270) exercises a shared-boundary change, acceptance without increased model dispatch count, export and playback. Fixed proposals and real rendering are both explicit. Full runtime gate passed; no public launch or shared deployment implied. |
| XP2 | [Live GPU qualification](https://github.com/TemniaHQ/temnia/blob/720544d49c26a952883ace1ae8aaf6cbfbef2702/docs/design/checkpointed-speech-qualification-2026-09-09.md#L52) distinguishes corrected short recovery and long execution. Dedicated application/resources were stopped/cleaned up; shared staging was not replaced. No invoice saving, universal retry guarantee or failure injection on the long source is claimed. |
| XP3 | [Second-run record](https://github.com/TemniaHQ/temnia/blob/720544d49c26a952883ace1ae8aaf6cbfbef2702/docs/log/2026-09-09.md#L37), [database regression](https://github.com/TemniaHQ/temnia/blob/720544d49c26a952883ace1ae8aaf6cbfbef2702/apps/pipeline/tests/test_harness_persistence.py#L1021), and [browser second run](https://github.com/TemniaHQ/temnia/blob/720544d49c26a952883ace1ae8aaf6cbfbef2702/apps/web/e2e/chapters.spec.ts#L467). Shared evidence keeps source identity; consumer runs retain their own references. The database regression uses a memory object store; the browser journey uses actual storage. No claim that the whole editorial run avoids model calls. |
| XH1 | [Shared-cut nudge and media checks](https://github.com/TemniaHQ/temnia/blob/720544d49c26a952883ace1ae8aaf6cbfbef2702/apps/web/e2e/chapters.spec.ts#L313) changes the two adjacent renders while preserving other sections; boundary audition is also exercised. This is a development workflow with fixed test proposals, not proof that live AI chooses good cuts. |
| LI1 | [Gate findings](https://github.com/TemniaHQ/temnia/blob/720544d49c26a952883ace1ae8aaf6cbfbef2702/docs/log/2026-09-09.md#L64), [web task environment/cache config](https://github.com/TemniaHQ/temnia/blob/720544d49c26a952883ace1ae8aaf6cbfbef2702/apps/web/turbo.json), [database test guard](https://github.com/TemniaHQ/temnia/blob/720544d49c26a952883ace1ae8aaf6cbfbef2702/apps/web/tests/harness-queries-db.test.ts#L7), and final runtime receipt/check. Missing database configuration fails inside the gate; optional local runs may still skip. The first failed gate is never called successful. |

All drafts use text alone. No demo, screenshot, customer quote or media access URL is promised.
Public copy excludes source IDs, provider handles, private recordings and control-plane details.

### XP1 — founder X — chapter workflow milestone — selected September 9 v1

- Account: x_personal / @RajeshBuilds; Premium+.
- Editorial date: 2026-09-09, Asia/Kolkata.
- Takeaway: The tested workflow can reach accepted export; live editorial quality needs its own proof.
- Evidence and state: matching audit/map entry above; source commit is fixed for this review.
- Asset: none required; text-only.
- Checks: 466 ASCII characters including line breaks, 73 words. Confirmed Premium+
  supports the concise founder format; no artificial 280-character cap.
- Review: pending Rajesh's review of this exact new copy.
- Publication: no action by this session; subsequent user publication is unknown.

Public copy:

```text
Temnia's chapter workflow now reaches an accepted export in a production-build browser test.

It renders chapters, lets the editor adjust the cut, and exports the accepted version. Approval reuses the checked media without another model call.

The proposals in this test are fixed responses; the rendering, storage and workflow execution are real.

The branch has passed its full gate. Live AI chapter quality still needs human evaluation before I can call it ready.
```

### XP2 — founder X — recovery and latency tradeoff — selected September 9 v1

- Account: x_personal / @RajeshBuilds; Premium+.
- Editorial date: 2026-09-09, Asia/Kolkata.
- Takeaway: Report observed recovery and observed latency separately, without turning either into a cost claim.
- Evidence and state: matching audit/map entry above; source commit is fixed for this review.
- Asset: none required; text-only.
- Checks: 513 ASCII characters including line breaks, 78 words. Confirmed Premium+
  supports the concise founder format; no artificial 280-character cap.
- Review: pending Rajesh's review of this exact new copy.
- Publication: no action by this session; subsequent user publication is unknown.

Public copy:

```text
The latest Temnia run processed a 151-minute recording in about 18 minutes. The earlier combined path took about 11.

The new path saves recognition, alignment and speaker assignment separately.

In a separate short live GPU test, I deliberately lost the activity result after those checkpoints were saved. Recovery reused them: three physical dispatches before, three after.

That proves this recovery case. It doesn't establish a speed or cost win; those comparisons still need controlled runs and actual bills.
```

### XP3 — founder X — second-run evidence identity — selected September 9 v1

- Account: x_personal / @RajeshBuilds; Premium+.
- Editorial date: 2026-09-09, Asia/Kolkata.
- Takeaway: Reusable source evidence should not inherit the identity of the job consuming it.
- Evidence and state: matching audit/map entry above; source commit is fixed for this review.
- Asset: none required; text-only.
- Checks: 456 ASCII characters including line breaks, 75 words. Confirmed Premium+
  supports the concise founder format; no artificial 280-character cap.
- Review: pending Rajesh's review of this exact new copy.
- Publication: no action by this session; subsequent user publication is unknown.

Public copy:

```text
Temnia's first chapter run reached export. Running it again on the same source exposed an identity bug.

The saved source analysis was reusable, but it carried the ID of the run that first consumed it. A second run made otherwise identical evidence conflict.

I separated the shared evidence from each run's reference to it.

The database regression and browser journey now cover that second run. Two runs reuse one evidence artifact and one storage entry.
```

### XH1 — product X — inspect both sides of a cut — selected September 9 v1

- Account: x_product / @TemniaHQ.
- Editorial date: 2026-09-09, Asia/Kolkata.
- Takeaway: A shared cut needs review of both adjacent chapters.
- Evidence and state: matching audit/map entry above; source commit is fixed for this review.
- Asset: none required; text-only.
- Checks: 258 ASCII characters including line breaks, 41 words. No links; ordinary count equals
  standard weighted X count, below 280 characters.
- Review: pending Rajesh's review of this exact new copy.
- Publication: no action by this session; subsequent user publication is unknown.

Public copy:

```text
A clean ending can still leave the next chapter starting mid-thought.

Temnia's review workflow lets you adjust the shared cut and inspect both sides.

Verified in development with fixed test proposals and real media. Live AI cut quality is still unmeasured.
```

### LI1 — personal LinkedIn — checks must actually run — selected September 9 v1

- Account: linkedin_personal / Rajesh.
- Editorial date: 2026-09-09, Asia/Kolkata.
- Takeaway: A passing test summary is useful only alongside evidence of which checks executed.
- Evidence and state: matching audit/map entry above; source commit is fixed for this review.
- Asset: none required; text-only.
- Checks: 937 ASCII characters including line breaks, 146 words. Below LinkedIn's
  3,000-character limit.
- Review: pending Rajesh's review of this exact new copy.
- Publication: no action by this session; subsequent user publication is unknown.

Public copy:

```text
Temnia's package tests looked healthy while its web database regressions were being skipped.

The task runner wasn't passing the test database setting through. The test files treated a missing setting as a reason to skip, so the result looked better than the coverage actually was.

The verified push stopped before publication. The gate now passes that setting explicitly and fails if it's missing. Those database tests also run without cached results.

Once they ran together, they exposed another issue: parallel test suites could select or clean up each other's fixtures. Their setup and cleanup now use only the records each suite owns.

The final revision passed the full gate, including chapter render, review and export in the production-image browser tests.

For this build, checking what actually ran was as useful as checking what passed. Live AI chapter quality still needs a separate evaluation with human-labelled examples.
```

### Editorial session record

- Done/how: analyzed the latest committed runtime, exact receipt and live PR statuses with an
  independent read-only evidence review; rewrote five distinct posts and preserved prior history.
- Corrections/limits: earlier 11-minute, uncommitted-work and next-integration framing was stale.
  Current AI quality and costs remain unmeasured; historical test counts are not summed into a new
  result. No live service probe or deployment was performed for this writing task.
- Numbers: 5 new selected drafts, including 3 Premium+ founder drafts of 73–78 words; 0 social
  actions and 0 runtime edits. Recorded runtime measurements belong to their cited tests.
- Verification: independent exact-copy factual/editorial review, character counts, Markdown checks
  and source-log preservation. Deliver this clean docs commit with `pnpm push:verified` on PR #25;
  the exact-SHA receipt and GitHub status establish that delivery separately from PR #24.
- Next: Rajesh reviews the copy. Record actual final posts and URLs after publication; recheck
  release state if posting follows a runtime merge/rollout. Build qualification continues in its
  own task; these drafts do not promise when the remaining measurements will be available.

## Integration note — later technical qualification

The editorial snapshot above predates the speech optimization measurements below. Its selected
post text and publication state are retained from PR #25; the later technical record supplies
the current runtime results. No post was published by this integration.

## Speech performance follow-up

Rajesh authorized implementing the performance improvements after discussing the slower isolated
GPU run. The work adds an off-thread progress publisher, explicit four/eight-CPU resource profiles,
and a versioned execution graph with independent speaker turns and recognition/alignment. A CPU
join retains both accepted inputs. The original protocol remains readable and replayable.

The controlled experiment uses one frozen model snapshot: 23 regular files totaling 6,624,978,799
bytes. The image separately identifies WhisperX's bundled VAD, tokenizer assets and observed
library versions. The four variants change progress reporting, CPU allocation and scheduling in
steps. The predeclared matrix has four short recovery cases and two reversed long-source blocks,
at most 36 GPU calls and 13,770,018 micros of configured new exposure under a 14,000,000-micro
admission ceiling. This is not an invoice ceiling; prior unresolved exposure remains separate.

Preparation found actual deployment problems before GPU inference. The first CPU preparation
failed because its volume environment was not propagated remotely. The next exposed Modal's
canonical mount root. A successful snapshot initially included two transfer logs; a new snapshot
excludes them. Deploying the speech module by filesystem path also changed its remote import
layout; the package-module entrypoint works and the wrong form is now rejected. All preparation
costs remain unmeasured without authoritative billing evidence.

Independent review found additional cancellation and attribution edges: an exact physical attempt
must match its returned checkpoint, and cancellation after a terminal GPU result must wait for
accounting to commit. The same review covers sibling cancellation, lost handles and one-time
benchmark admission. The first performance commit passed the full gate: 717 Python tests without
skips, 187 web tests, 41 database isolation tests and 18 production-browser tests, with both deploy
images built. The first live short case then completed all three GPU stages but failed to persist
the CPU assignment because the artifact enum omitted its kind. The failure report also referenced
an incorrect dependency column. No later case was admitted. This exposed missing integration-test
coverage despite the green gate: the actual assignment and report SQL need real PostgreSQL tests.

The correction adds the enum through Drizzle and preserves the first run's accepted checkpoints
and 1,062,501 micros of unresolved exposure. Its planned recovery uses a separate CPU-only run
with a one-micro budget, so a cache miss cannot start another GPU attempt. The remaining eleven
cases retain the original journal and ceiling. The corrected assignment and an actual Temporal
recovery passed against migrated PostgreSQL, proving zero new attempts and unchanged original
accounting. The report query and object preservation also passed against real rows from two runs.
The focused benchmark/driver suite passed all 17 tests; Ruff, Pyright and independent review are
clean. The subsequent release-gate results and live recovery preflight are recorded below; no
performance improvement has been established yet.

The corrected commit's first full gate failed on missing Next.js route globals while `next typegen`
and `next build` ran concurrently. Ordering typechecking after its own build removed that shared-output
race, but the second gate still failed with generated files present. The effective TypeScript
configuration revealed the cause: it included the generated types while excluding their entire
`.next` directory. A stale incremental program retained an unresolved side-effect import after
generation. A nonincremental probe passed; allowing the generated files as explicit roots also
passed with incremental checking. The correction removes the conflicting exclusion and retains
type generation and build ordering. Neither failed gate produced a receipt or admitted more GPU work.

The corrected full gate passed on `c933d0349fb400677fdb20d623a6aee285dde8da` at
2026-09-09 03:24:21 UTC: 722 Python tests with zero skips (89.14 seconds), 187 web tests,
41 database tests, 28 contract tests, both Linux deploy images and all 18 production browser
tests. Migration 0004 then applied successfully to the dedicated benchmark database.

The recovery preflight caught another real artifact-shape assumption before starting a workflow:
independent speech coverage shares the `speech_checkpoint` enum kind with GPU checkpoints. The
source therefore has four rows of that kind, with only three referenced by GPU operations. No
additional GPU call occurred. The journal already held the recovery marker, so continuation needs
an explicit proof that the latest Temporal execution and sole database run are still the original
failed execution. The repair selects checkpoints by exact operation result IDs, preserves the
coverage evidence, and tests the actual four-artifact shape. The original journal, three attempts
and 1,062,501 micros remain unchanged; no reset or extra benchmark case is authorized.

The corrected selector, journal transformation, original ledger hash and latest failed Temporal
execution all passed a read-only check against the actual benchmark database. Review also caught
an early-failure receipt path that dereferenced a missing recovery run; that path now needs a
durable failure receipt even before a harness run exists. Continuation tests cover real coverage
publication and reject a newer execution or changed accounting before any provider dispatch.
The final focused tranche passed 22 benchmark/driver tests and three real PostgreSQL/Temporal
tests, with Ruff and Pyright clean. Independent review is clean. A create-only, fsynced start-intent
record closes the process-loss window before Temporal acknowledges recovery; a missing run now
produces a durable failure receipt. The worker fingerprint remains unchanged. The full gate and
controlled live continuation follow this repair.
The first gate for this repair was deliberately interrupted during its web-image build after a
late driver correction changed the checked-out file. The start-intent now hashes the journal after
the normal recovery marker is written; the already-recovering path retains its acknowledged hash.
That interrupted gate produced no successful attestation and admitted no GPU work. The corrected
commit requires a fresh complete gate.

The corrected driver passed the full gate on `214f4ee6c58889c496056911c7a0a6c51b9aab24`
at 04:06:02 UTC: 728 Python tests without skips, all 16 build/lint/typecheck/test tasks,
both deploy images and all 18 production browser tests. The first short source then recovered
through a separate CPU-only run in 12.274 seconds with zero new GPU attempts or exposure;
the original failed run and its accounting hash remained unchanged. All four short variants
produced identical 93-word/two-speaker token, timing and speaker hashes. The remaining three
short cases also proved deliberate lost-result recovery with dispatch counts `[3, 3]`.

The first long A case produced a ready 20,587-word/two-speaker transcript from three successful
GPU calls totaling 892.008 function seconds. The benchmark stopped because Temporal retried
the activity unexpectedly. History identifies a heartbeat timeout; the retry reused the three
accepted checkpoints. The 989.952-second workflow wall time is excluded from comparisons.
Reconstruction of the exact accepted CPU assignment was byte-identical and took a median
0.207 seconds across five calls, so CPU joining alone does not explain a ten-second timeout.
The missing heartbeat coverage includes asynchronous storage and ledger work after GPU polling.
The repair covers the whole activity and is tested with deliberately delayed post-GPU work.
Fifteen successful terminal calls retain 5,652,507 micros of unresolved exposure. Seven fixed
long cases remain; no additional repetition or cap increase is planned.

The heartbeat repair passed two real Temporal/PostgreSQL regressions in 33.48 seconds using
simulated provider responses and the production two-second interval/ten-second timeout. A
twelve-second quiet tail completes on one activity attempt; cancellation keeps heartbeating
through another twelve seconds of cleanup and publishes no assignment. Eight lifecycle tests,
one real cache-persistence test and 30 adjacent lifecycle/workflow/accounting tests also pass.
Review caught and corrected cancellation swallowed during helper shutdown and late heartbeat
errors hidden by a successful result. The disposable liveness test database was dropped and
verified absent; no benchmark resource or live GPU call was added by these tests.

The completed-case continuation passed 27 focused tests and static checks. Its actual read-only
preflight verified the original ledger/source, ten objects, complete Temporal history and three
terminal Modal calls in 7.9 seconds; the journal hash remained unchanged. No workflow or paid
call was started. A current-service probe also established that the billing API is accessible:
complete hourly intervals through 05:00 UTC report $0.46483580 of owned benchmark/preparation
application usage. It is grouped by application/hour/resource before credits, so it does not
resolve exact per-attempt charges or separate C from D in their shared application. No ledger
reservation was released on that aggregate evidence.

The full heartbeat gate then exposed test isolation that separate focused processes had hidden.
On `4ad53ae755e860e528aa2840710a8ea8fd3f5151`, 729 Python tests passed, including both new
Temporal regressions, but a following persistence test failed while closing their inherited
PostgreSQL pool and thirteen transcription fixtures reported `PoolClosed`. The new tests had
left the pool open when their function-scoped event loops ended. Their local async teardown now
closes it in `finally` before loop shutdown. Production database code and the worker/GPU
fingerprints remain unchanged. The exact module order reproduced one failure and thirteen
errors in 33.90 seconds before the fix, then passed all sixteen tests in 35.81 seconds.
Ruff and Pyright passed, and the disposable test database was dropped and verified absent.
The benchmark remains at fifteen calls until the repaired commit passes the full gate.

### PR #24 final controlled matrix and manual-readiness work

The frozen heartbeat worker passed the full gate on `aba6d93da5c691657969e2508864d326263ac96d`
at 05:42:34 UTC: 743 Python tests without skips, 187 web tests, 41 database tests, 28 contract tests,
all 16 build/lint/typecheck/test tasks, both Linux deployment images and 18 production browser tests.
The completed-case acknowledgment preserved the original A1 failure/timeout evidence and continued
only the seven remaining cases, with no extra repetition or budget increase.

The fixed matrix finished all twelve cases at approximately 07:15 UTC: four short recovery cases and
two reversed long repetitions per variant, 36 successful physical GPU calls, 13,770,018 micros of
retained exposure under the original 14,000,000-micro cap. The long input was the same 151-minute
English recording, not eight independent recordings. The native GPU build and model volume remained
frozen; B1 onward used the repaired worker. A1's wall time remains excluded for its heartbeat retry.

The clean synchronous/coalesced four-CPU comparison was 1,077.178 versus 819.884 workflow seconds,
23.89% lower with 25.23% lower resource estimate. The two parallel eight-CPU runs took 523.518 and
533.682 seconds, but increased resource estimates 34.08% and 27.96% over coalesced four-CPU serial.
Eight-CPU serial did not meet the intended cost/performance tradeoff. Exact outputs split into two
internally identical groups, 20,587 and 20,588 words. The difference begins in recognition; the same
fully frozen serial C configuration produced both groups. No candidate satisfies the predeclared
global exact-output gate, so no default or billed-cost winner is selected. Three lexical substitutions,
one insertion, seven punctuation/case-only changes and localized timing differences require listening
or labels before accuracy can be judged.

Provider billing through 07:00 UTC reported $2.10079169 across owned experiment and preparation/probe
apps, before credits/reservations. It excludes the final A2 interval and cannot separate C/D or exact
attempts. Unknown per-attempt costs and reservations remain preserved. The earlier nine protocol-1
attempts retain their separate 11,625,003 micros of unresolved exposure.

The private final archive preserved 159 hash-verified files, all 36 terminal provider results,
13 Temporal execution histories, exact scoped database rows, and 132 source-prefix object proofs.
A private export-helper schema mismatch was corrected to follow the attempt's exact operation;
seven regressions passed and its first partial export remained untouched. Cleanup then re-proved
unchanged evidence, stopped all three exact GPU apps (zero tasks), removed the experiment model
volume and 36 exact shared-Dict keys, emptied/deleted twelve owned prefixes, and removed the dedicated
namespace/database. Temporary operator/storage env files were deleted. Full evidence, frozen inputs,
and unknown-cost records remain private. The cleanup receipt hash is
`2e2a4d89f093cf75dd6cc9ddbf3487445550e3715acffd7438dc56ac11cd20a5`.

The separate integration checkout incorporated main `a32faec`'s documentation-only PR #25 while the
benchmark worker remained frozen. Existing selected post copy and publication history were preserved.
Manual-readiness review found that the old export link exposed only a JSON manifest. The UI now
verifies the accepted artifact graph and offers actual per-chapter video/caption downloads through
the existing scoped streaming media proxy. It preserves the last accepted revision after edits or
cancellation, handles manifest retry, and suppresses late results after switching views. Independent
review also corrected raw schema errors reaching the UI, an early stale-response assertion and
duplicate warning keys. The focused download validator passed 23 tests, formatting and typechecking.

A startup guard now checks configured output/context headroom for every primary and failover route
in required seats; incompatible settings fail before a chapter run. Nine focused Python settings
tests and static checks passed. The manual walkthrough uses two fresh 40-second fixture uploads,
keeps recorded chapter planning ahead of transcript corrections, downloads accepted files, and
explicitly clears inherited single-file transcription overrides. Three shell blocks parse and
relative links resolve. A live gateway credential remains unavailable; recorded editorial tests do
not qualify chapter planning on Rajesh's own recordings.

The final integrated commit requires a fresh complete release gate and `pnpm push:verified`; its
exact receipt and required GitHub check, rather than any earlier commit's result, establish delivery.
Shared staging is unchanged until Rajesh merges PR #24 and the paired rollout is verified. Remaining
qualification is live model transport/privacy and seat auditions, human transcript/boundary labels,
correction time, additional source/language coverage and attributable costs. No extra social draft
or publication was created during this technical follow-up.

### Gateway staging follow-up

Rajesh merged PR #24 as `0c73420` at 09:49:59 UTC and saved the gateway key on the pipeline
application. The web deployed at 09:51:45 UTC, applied the harness migrations and seed, and returned
HTTP 200 from its health endpoint. The pipeline deployed at 09:56:25 UTC, received the key with
`HARNESS_ENABLED=0`, and connected to the existing Temporal queue. No transcription default changed.

The key authenticated successfully from the staging host. The first bounded, synthetic request
with the mandatory zero-data-retention/provider controls returned HTTP 403: Vercel's Pro Trial
does not allow ZDR requests. The setup guidance had omitted this paid-plan prerequisite. The real
service response confirmed the restriction, and the runbook now distinguishes account entitlement,
gateway credits, authentication, route transport qualification, and editorial evaluation.

One entitlement request was admitted with a 743-micro reservation and a 256-token output cap.
It returned no generation ID. At 10:04:59 UTC the authenticated credits endpoint still reported
zero total usage. Request intent, the exact response and a scoped source inventory were preserved
privately with hashes. Two retained 151-minute recordings have ready transcripts for the later
chapter smoke; no new GPU or model inference result was produced by this preflight.

Public endpoint research produced provisional candidates, not production routes. Rajesh then
activated paid Pro. A strict ZDR entitlement request succeeded at 10:30:23 UTC. Full production
schemas exposed two adapter issues: outdated token field names and integer-zero monetary fields
rejected by strict Decimal validation. The fixes preserve exact costs, native and reasoning token
counts, and reject malformed receipts. A retained response reconciled read-only at $0.0000505.

DeepSeek initially inferred intermediate word IDs, so versioned summary/proposal instructions now
require copying visible anchors verbatim. DeepSeek then passed all three schemas. Qwen/Alibaba
passed all three too. Gemini/Vertex rejected an integer enum; Kimi/Wafer returned an invalid schema.
Those routes were excluded. A separately qualified Kimi/Alibaba endpoint passed all three schemas.
Owner sources verify the Qwen, DeepSeek and Kimi weight releases; none is declared an editorial
quality winner from these small authored-input checks.

The finite qualifier saves admission before dispatch, SDK responses before validation, exact
identity/cost receipts and bounded redacted HTTP-failure details. It refuses duplicate admission,
unproven production snapshots and output collisions; uncertainty stops the batch. Sixteen HTTP
requests across the reviewed batches consumed $0.03845786, independently confirmed by credits at
10:52:42 UTC. Conservative retained exposure was $0.851151 under the $1 cap. The final balance was
$14.96154214. Failed responses and reservations remain preserved. No key left the staging host.

The focused gateway/qualifier/CLI/workflow suite passes 48 tests. The completed follow-up requires
the exact-commit release gate and verified PR delivery. The first full gate caught missing type
annotations in the new CLI test helpers; those were corrected and the full Pyright check then
passed with zero errors, alongside all twelve CLI tests. Shared staging still has the merged PR #24
image and HARNESS_ENABLED=0; no code was hot-patched, no GPU setting changed, and no real-source
chapter workflow has run yet. Next: merge/deploy the accounting and prompt corrections, configure
a reviewed live staging experiment, and measure source-faithful boundaries, correction effort and
total cost. Existing selected X/LinkedIn copy remains unchanged; no new social draft or publication
was created for this operational follow-up.
