# 2026-09-14

## The first GPU render, and why it failed

Rajesh's 11:00 IST Karma run (`70030622`) reached rendering on the newly deployed
`render_sections` and failed after three attempts: `ModalFailure: ModuleNotFoundError: No module
named 'psycopg'`. Sixteen model calls and $1.35 were retained; the run is resumable. The function
imported `temnia_pipeline.harness.rendering` for `timeline_identity`, which imports
`harness.artifacts`, which imports the database driver the Modal image never had. Behind that,
`render_remote` would have failed on `temporalio`, and `media.chapters` on PyAV through
`media.probe`. The release smoke passed because it exercises speech, not rendering.

Fixed on `fix/modal-render-imports`: contracts and the timeline identity moved into
dependency-light modules, a `RENDER_MODULES` list shared by the function, a probe and a test, a
`render_probe` step in the smoke, and import errors classified as "cannot start" with the CPU
fallback the absent-function case already had.

Earlier today: PR #45 reviewed and merged with the retitle and any-axis corrections; run `f7396b5e`
(started by Claude at 09:44 IST) finished 9 of 9 videos for $0.72 on the CPU fallback; the
architecture review, the loop redesign plan and the prompt review were written on
`docs/harness-loop-redesign`.

## Retry after the deploy

With #46 deployed (Modal workflow green, render probe `nvenc: true`, worker up 06:30:45Z),
Rajesh pressed Retry on run `70030622` twice. Both executions failed at `start_chapter_run`
in three seconds: `IdentityConflict: request key was reused with different evaluation
programme`. The diff between the frozen programme and the worker's was one field,
`implementationSha256`; every stage's prompt and schema hash was equal. The panel kept
saying "Retry started; resuming from the retained work" because the refusal happened before
the run was claimed and `mark_chapter_run_failed` only writes for the owning execution.

Fixed on `fix/retry-after-deploy`: `editorial_identity` (the programme minus the build) is
what a product resume must match; the resuming build is recorded on the run; explicit
experiment claims stay strict; the web action waits up to eight seconds for an early
failure and reports it.

## Seen while verifying the retry

The retry rendered four videos with four Modal calls, one per video: `render_topic_revision`
loops over videos and the remote render path runs per video, so every video downloads the
master and pays a cold start. The worker also downloads the master to re-verify identity before
handing the job to the card. Five candidates were withheld; two of them only on
`physical_boundary_constraint`, which the compiler emits and only a model repair can resolve.
Both recorded as review findings F14 and F15 and plan items R1 and R2.

## Workspace sync

- Done: fast-forwarded this checkout's `main` by eight commits, from `5c63091` to
  `4bc52c6`, matching `origin/main`.
- How: fetched origin, compared the six local documentation files with incoming
  versions, saved them with `git stash --include-untracked` as
  `pre-pull-main-2026-09-14-local-docs`, then pulled with `--ff-only`.
- What needed care: the local September 13 log and staging plan differ from the
  incoming documents; their complete originals remain in the stash. The other local
  documentation content is already included upstream. The stash was not reapplied.
- Validation: Git reports zero commits ahead or behind; all six files are in the
  retained stash. No application tests, paid calls, deployment or publication.
- Next: continue from updated main; reconcile the saved drafts when needed. No new
  social drafts. This session record remains uncommitted.

## Review of PR #48

- Done: reviewed the architecture, loop redesign and prompt documents against merged
  `4bc52c6`, current contracts and validators, relevant decisions and the supplied
  clipping-agent prompt pack. Detailed findings: `/tmp/temnia-pr48-review.md`.
- Findings: six actionable issues covering repair-group conflicts, run-level unknown
  expense fences, cross-revision label identity, missing calibration observations,
  superseded long-source assumptions and the source-review causal diagnosis.
- How: inspected the source paths and existing evaluation contracts; an offline
  counterexample confirmed that disjoint candidate findings can authorize the same
  source gap. Repeated per-video render dispatch is supported by the code.
- Validation: 46 focused admission/evaluation tests passed in 1.60 s using
  `uv run --frozen --no-sync`; no dependency synchronization, paid calls, staging
  changes, playback reruns or external review comments.
- What went wrong: two document audits were delegated before reading the newly pulled
  13 September no-delegation decision. Stopped further delegated work when that rule
  was read and completed the remaining review and synthesis directly.
- Next: revise the design before implementation. Reviewed documents are unchanged;
  this log addition remains uncommitted. No social drafts or publication.

## Indexed evidence as the harness foundation

- Direction: Rajesh proposed source indexing and tools for author/reviewer models because
  intermittent 44-minute full-transcript success does not establish a path to two- and
  four-hour recordings. Recorded the direction in AGENTS.md and marked PR #48's late,
  author-only W4 sequencing superseded.
- Design: wrote `docs/design/indexed-editorial-evidence-2026-09-14.md`. It proposes a
  chronological hierarchy, hybrid retrieval, exact source reads and bounded context for
  discovery, authoring and source review; cold review stays isolated. Global coverage,
  internal topic structure, identities, accounting and calibration are explicit obligations.
- How: checked the live source paths and current admission/deadline behavior, the existing
  tool prohibition and response-finish handling, PydanticAI's durable integration, primary
  retrieval/context research and the legacy architecture's relevant lessons. No delegation.
- Numbers/limits: six primary research/documentation sources; no new model/GPU calls, no
  scale run, no tests in this design-only step. Earlier 46-test review validation remains
  separate. No quality, latency or cost improvement has been measured.
- What needed care: a source search index cannot establish whole-source discovery; appending
  every tool result recreates oversized prompts. Tool continuations are currently rejected
  by the harness, despite support in the underlying integration.
- Next: resolve the proposed index/tool/context contracts and implementation choices, then
  validate recovery and editorial results on actual full-length sources. Changes remain
  uncommitted; no code implementation, social drafts or publication.

## First indexed editorial vertical slice

- Done: implemented a run-scoped `topic-source-index/1` before inventory and authoring. The
  inventory, author and independent source reviewer prompts now receive compact index identity
  and tool limits instead of the whole transcript. Cold review remains limited to selected speech,
  and patch repair retains its finding-authorized evidence window.
- Index/retrieval: exact ordered regions normally contain at most 32 sentences and 8,000
  characters. Deterministic all-text keywords and first/middle/last previews support the complete
  chronological browse. Source-local search combines Okapi BM25 with exact cosine similarity over
  the pinned MiniLM revision; embedding text is split into 800-character units before regional
  averaging. Exact reads are capped at 80 sentences and 64,000 characters with continuation IDs.
- Agent path: PydanticAI exposes exactly `browse_source`, `search_source` and `read_source` to the
  three source-aware roles, with scope/source/index identity supplied by immutable dependencies.
  The physical gateway validates the same complete tool set. Tool calls execute as one-attempt
  Temporal activities; every model continuation is reserved, dispatched, settled and retained as
  a separate ledger request. The optional route pre-flight now retains multiple paid rounds for a
  logical stage.
- Admission/evidence: code reconstructs a source-text-free `topic-source-inspection/1` from actual
  tool calls and results. It requires a cursor-zero browse through every region, hybrid search and
  exact reads of every sentence in every span claimed by the typed answer. Accepted inventories,
  selections and source assessments depend on the exact index, final response and inspection.
- Scale check: a deterministic 2,400-sentence, four-hour fixture produces 75 regions while its
  initial serialized index overview remains under 500 characters. Tests traverse every page,
  retrieve a rare phrase and paginate exact speech. This is payload/retrieval evidence, not an
  editorial or provider-quality result.
- Limits: the map is flat; hierarchy, bounded context reconstruction, candidate/media inspection
  tools and safe cross-run index reuse remain open. Active PydanticAI tool history still grows
  within a seat. No paid model/GPU call, real long-source run, deployment or publication occurred.
- Validation: 151 focused source-index, selection, workflow, gateway, qualification and accounting
  tests passed in 3.10 s, plus 15 Temporal registration/liveness tests in 30.38 s. The first
  clean-commit local gate reached all 1,209 pipeline tests and failed seven model-transport fixtures:
  they invoked the newly tool-enabled agents with plans that omitted source-index authority, so the
  production guard refused them. The fixtures now publish a scoped synthetic index and bind it to
  the inventory, author and source-review plans; all seven database-backed cases then passed in
  2.21 s. The complete clean-commit gate then passed on `e7a2d52`: 1,208 pipeline tests passed with
  one expected skip, both production images built, and all 18 containerized browser tests passed in
  56.3 s, including the web-to-Garage/Temporal-to-pipeline topic render/review/export path. No social
  drafts were written.

## Hierarchical source navigation

- Done: advanced the source artifact to `topic-source-index/2` with an exact episode → section →
  region hierarchy. Sections own at most eight of the existing bounded leaves. The episode and
  section descriptions derive deterministically from all owned speech; their normalized vectors are
  sentence-count-weighted compositions of child vectors.
- Traversal/admission: each indexed role must browse the root and then every section in source order
  through its final page. Search remains leaf-only and exact reads remain authoritative. Admission
  now reproduces parent/child ownership, ordered leaf coverage, deterministic descriptions,
  bottom-up vectors, tool-result IDs and every cursor transition.
- Scale check: the synthetic four-hour fixture remains 2,400 sentences and 75 leaves, now grouped
  into 10 sections. Its initial overview remains under 500 serialized characters. New adversarial
  tests refuse a broken parent, changed section description, changed section vector, missing section,
  out-of-order section and leaf browse.
- Documentation: updated the indexed-evidence design, route pre-flight and repository decision; added
  `docs/design/topic-source-index-v2-2026-09-14.md` as the contract, invariant, traversal, migration
  and limitation record.
- What needed care: generated Python contracts represent enum values and constrained child IDs as
  wrapper types. Initial validation compared their display forms and failed. Boundary helpers now
  use the enum value and unwrap child roots. Tightened deterministic validation also exposed old
  workflow fixtures that hand-authored arbitrary descriptions; the fixtures now exercise the real
  index builder with an offline encoder double.
- Validation: 105 focused hierarchy, workflow, qualification and gateway tests passed in 2.20 s;
  all 33 contract tests, schema drift, Biome, TypeScript and targeted Python type checks passed. The
  exact clean-commit gate for `4cbc48f` then passed: 1,211 pipeline tests with one expected skip, 33
  contract tests, 202 web tests, 41 database isolation tests, three legacy-reference tests, two
  scripts tests, every lint/type/build check, both production images and all 18 containerized
  Playwright tests including the exact topic generation/correction/export path. No paid model/GPU
  call, deployment, real-source run or publication occurred.
- Next: implement durable per-role progress checkpoints and rebuild each continuation from compact
  state plus explicitly selected exact excerpts. Cross-run reuse, candidate/media tools and real
  long-source qualification remain later ordered milestones. No social drafts were written.

## Durable indexed-agent checkpoints

- Done: inventory, author and source-review calls now rebuild each model continuation from the
  unchanged prompt plus one bounded `topic-agent-checkpoint/1`. Previous assistant/tool messages do
  not return to the wire. The checkpoint retains compact source-access facts and the most recent
  exact source sentences; the final answer must reread and retain every sentence covered by its
  typed source spans.
- Persistence and recovery: every checkpoint is a run/stage/role/index-bound immutable `checks`
  artifact published before dispatch. Sequence zero depends on the prepared inputs; every later
  sequence also depends on its direct parent; every model response depends on the checkpoint it saw.
  After a known provider failure, the workflow loads and verifies the latest checkpoint and retries
  the same request on the current or next qualified route. Unknown outcomes keep the existing fence.
- Admission: new answers produce `topic-source-inspection/2`, which binds the final checkpoint hash,
  request sequence, complete source-tool trace, retained exact sentence IDs and eviction count. An
  earlier read that has left working context is progress evidence but cannot ground a final claim.
  Historical v1 inspection remains readable under its original rules.
- Limits: 256 successful calls, 8,192 returned IDs, 320 retained sentences, 128 KiB retained exact
  text and a 384 KiB checkpoint. A third identical successful call is refused. These are payload
  safety envelopes, not production-tuned duration or output-count limits; bounded portfolio
  reconciliation is still required when final evidence cannot coexist within them.
- Qualification: indexed route pre-flight uses the same history processor. Its tool/final fixture
  confirms that the second request has only the original prompt and checkpoint, while both physical
  requests remain separately accounted.
- What needed care: PydanticAI resolves a history processor's type annotation at runtime, so the
  qualification adapter's message type must be a runtime import. The first retry design also only
  persisted checkpoints; it did not restore them after a known provider failure. The completed path
  now loads the exact artifact before same-route retry or route fallback and preserves its sequence
  until a new successful tool result exists. Run identity was added to the artifact fingerprint to
  prevent matching content from being reused across runs.
- Documentation: added
  `docs/design/indexed-agent-checkpoints-2026-09-14.md`; updated the indexed-evidence design, v2 index
  contract, route pre-flight runbook and repository decision.
- Validation: 94 focused source-index, checkpoint, prompt, qualification, workflow, gateway and
  model-transport tests passed in 3.04 s. The exact clean-commit gate for `a8cfcf8` then passed:
  1,217 pipeline tests with one expected skip, 33 contract tests, 202 web tests, 41 database
  isolation tests, three legacy-reference tests, two scripts tests, every lint/type/build check,
  both production images and all 18 containerized Playwright tests. The database-backed checkpoint
  parent-chain test is included in that result. One unchanged upload-resume browser test missed its
  transient waiting state on the first gate attempt; a complete rerun passed all 18 tests. No paid
  model/GPU call, staging change, deployment or publication occurred.
- Tomorrow: solve safe source-index reuse and bounded portfolio reconciliation before asking for
  real long-source evaluation.

## Candidate and measured-media review tools

- Done: source review now receives mandatory paginated `inspect_candidate` and optional
  `read_media_evidence` in addition to the shared source-navigation tools. Candidate inspection
  exposes every index region intersecting each accepted candidate and is replayed in accepted
  selection order. Measured media exposes chronological sentence-alignment, boundary, pause, shot
  and speech-coverage records without transcript text or a playback claim.
- Authority and admission: the accepted selection and evidence are immutable source-review inputs.
  Tool results bind their exact selection/index/evidence hashes. Admission requires every candidate
  through its final page and reproduces any media page used. The source inspection artifact now
  directly depends on the accepted selection as well as evidence, index, response and checkpoint.
- Loop and transport: candidate region IDs and kind-scoped media event IDs survive checkpoint
  compaction without copying result prose. Inventory/author wires require exactly three source
  tools; source review requires exactly five. The streaming SDK qualification fixture caught and
  closed a stale physical-request validator that still allowed only the original three.
- Prompt and documentation: source prompt `/11` requires exact reads across a candidate's internal
  region changes and distinguishes measured sensor records from listening or viewing. Added
  `docs/design/candidate-media-evidence-tools-2026-09-14.md`; updated the indexed architecture,
  checkpoint contract, route pre-flight and repository decision.
- Validation: 11 focused candidate/media, checkpoint, qualification and real streaming SDK
  tests passed in 1.84 s; touched Python lint and the complete Python type check passed. The first
  exact-commit gate passed 1,220 of 1,221 executed pipeline tests with one expected skip, then found
  one stale database-backed output-profile fixture: it manually constructed source-review authority
  with only the index. The fixture now publishes and binds one coherent evidence/index/selection set
  and expects the five-tool wire. Its first rerun correctly refused the synthetic evidence because
  its transcript UUID did not belong to the ready source; later pool failures were cascades. The
  helper now binds evidence to the source's real transcript revision. The isolated database-backed
  test passes on a fresh migrated database. The complete exact-commit gate then passed on
  `81ff708`: 1,221 pipeline tests passed with one expected skip, 33 contract tests, 202 web tests,
  41 database isolation tests, three legacy-reference tests, two script tests, and every
  lint/type/build check. Both production images built from a cleared Docker cache; the pipeline
  image downloaded and loaded its exact pinned offline model snapshots. All 18 containerized
  Playwright tests passed in 54.6 s, including topic generation, correction and accepted export.
  No paid model/GPU call, real-source run, staging change, deployment or publication occurred.
- Next: implement safe cross-run reuse of an immutable evidence-bound source index, then decompose
  whole-source inventory/portfolio judgments into bounded reconciliation units.

## Source-bound index reuse

- Done: `topic-source-index/2` now uses a stable producer identity instead of a run identity. It
  binds the exact accepted evidence reference, source and transcript revision, builder/format,
  immutable MiniLM revision, all partition/description constants and relevant runtime-library
  versions. Audience, rubric, model route and editorial programme stay outside this source-fact
  cache.
- Admission: a hit is reported only after scope and deletion-fence checks, accepted evidence/index
  reference comparison, exact metadata/dependency lineage, stored-byte/hash verification, full v2
  structural validation and encoder identity validation. Corrupt or mismatched artifacts fail
  closed before the encoder loads rather than rebuilding silently under the same identity.
- Audit and concurrency: every consuming run publishes `topic-source-index-use/1`, naming the exact
  evidence and index and whether this activity avoided encoder work. Concurrent misses may duplicate
  local construction, but the existing unique artifact identity accepts only byte- and lineage-
  identical results. A worker that performed construction reports a miss even if another worker won
  the publication race.
- Documentation: added `docs/design/source-index-reuse-2026-09-14.md`; updated the v2 index,
  checkpoint, reviewer-tool and overall indexed-evidence records plus the repository decision.
- Validation so far: 63 focused non-database tests passed; four reuse tests passed against a fresh
  migrated Postgres database; touched Python lint and type checks passed. The first exact-commit gate
  for `33d84c9` stopped at `ruff format --check`: two new test expressions needed the formatter.
  Two exact-commit runs for the formatted `93eb832` then passed 1,226 pipeline tests with one expected
  skip, all TypeScript tests and checks, and both cold production-image builds. Both stopped on the
  same unchanged upload-resume browser assertion: the upload had correctly resumed through parts 2
  and 3 and reached 100%, but the two-second adoption grace had elapsed before Playwright observed
  the transient waiting label. The test now refreshes the seeded browser's upload liveness immediately
  before the second browser starts, preserving the countdown assertion instead of weakening the
  resume proof. The exact clean-commit gate then passed on `f97249a`: 1,226 pipeline tests passed with
  one expected skip, 33 contract tests, 202 web tests, 41 database-isolation tests, three
  legacy-reference tests, two script tests, and every lint, type and build check. Both production
  images built after the required Docker cache prune; all 18 containerized Playwright tests passed
  in 2.5 minutes, including the stabilized adoption-grace case and the full topic
  generation/correction/export path. The gate reported success at
  `2026-09-14T13:29:33.304Z`. No paid model/GPU call, real-source run, staging change, deployment or
  publication occurred.
- Next: decompose whole-source opportunity discovery, authoring and independent portfolio review
  into bounded units with deterministic reconciliation and durable partial completion.

## Bounded opportunity inventory

- Done: added `standalone-topics/4` and `TopicSelectionWorkflowV4` without reinterpreting V3
  history. V4 derives one immutable work item per `topic-source-index/2` section, runs up to three
  reviewer-seat inventory calls concurrently and enters authoring only after every planned shard is
  admitted.
- Ownership and evidence: an opportunity ID carries its section prefix and belongs to the section
  containing its earliest core sentence. Setup, completion and meaning-changing follow-ups may be
  read across the edge. Each call must completely browse its target section, use hybrid search and
  retain exact reads for all cited speech in its final durable checkpoint.
- Failure behavior: every settled answer becomes `topic-opportunity-inventory-shard/1` or
  `topic-opportunity-inventory-shard-rejection/1`. `topic-opportunity-inventory-manifest/1` exists
  only for the exact complete ordered shard set. A missing, rejected, foreign or reordered shard
  stops before the author and retains completed shards/checkpoints for review.
- Programme and operations: V4 has separate Temporal and PydanticAI activity names, its own frozen
  five-stage program manifest and a `topic-selection-v4` optional pre-flight suite. The controlled
  experiment operator can select it. The web remains on V3 until exact route qualification and real
  long-source editorial review.
- Scale check: the valid four-hour fixture produces 10 sections with no normal section above 256
  sentences. Tests cover cross-edge completion, wrong ownership, exact manifest assembly, scoped
  inspection, workflow ordering and refusal before authoring. The clean exact-commit gate passed for
  `0ca907d7a38c4866f3f95398de9b4c1785b5a80a` at `2026-09-14T14:08:41.556Z`: 1,237 pipeline tests
  passed with one expected environment skip, alongside 33 contracts, 202 web, 41 database and 18
  Playwright tests; frozen installs, generated-contract checks, database migration, builds, lint,
  type checks and both cold production images also passed.
- Documentation: added
  `docs/design/bounded-opportunity-inventory-2026-09-14.md`; updated the indexed architecture,
  checkpoint contract, route pre-flight and repository decision.
- Limits: this decomposes independent opportunity discovery. The assembled inventory still enters
  one author answer, and source review still returns one whole-portfolio answer. Their bounded
  reconciliation is next. No paid model/GPU call, staging change, deployment or real-source run was
  performed.

### X draft

One model answer should not have to inventory a four-hour transcript. Temnia now divides independent
topic discovery into source-owned sections, keeps each agent loop bounded, and assembles the result
only when every exact shard is present. A partial run cannot silently become a complete inventory.
Local four-hour shape tests pass; real long-source editorial quality is still unmeasured.

### LinkedIn draft

An indexed transcript and bounded tool history still leave one scaling trap: asking a model to
return every worthwhile discussion from a four-hour recording in one final answer.

Temnia now creates a deterministic work item for each source section. Up to three independent
inventory calls run at once. A discussion belongs to the section containing its earliest core
sentence, while setup and completion can cross the section edge. Every answer is checked against its
owned section and exact source reads.

The whole inventory is published only when every planned shard is present in the right order. A
rejected or interrupted shard leaves its response, evidence checkpoint and diagnostic available,
and authoring does not start from the partial result.

This proves bounded request shape and failure behavior on a synthetic four-hour source. It does not
yet prove opportunity recall or final video quality. Author packaging, portfolio review and real
44-minute/two-hour/four-hour evaluation remain the next stages.

## Bounded author packaging

- Done: added `standalone-topics/5` and `TopicSelectionWorkflowV5`. V5 retains the complete V4
  section inventory, publishes an immutable author plan and sends each author call at most 12 exact
  opportunities from one section.
- Ownership: each work item has a deterministic section/batch ID. Every returned candidate must use
  that prefix, every candidate must be linked by an assigned opportunity, and the author may change
  only the inventory's packaging/disposition fields. Candidate extents can cross section edges when
  the complete discussion requires it.
- Evidence loop: each author shard must browse its target section, use hybrid retrieval, read the
  assigned spans and retain every final cited sentence in its durable checkpoint. Up to three work
  items run concurrently under the existing route gate, ledger and unknown-outcome fence.
- Completion: a settled answer becomes an admitted `topic-author-packaging-shard/1` or exact
  rejection. Only every exact shard in plan order produces
  `topic-author-packaging-manifest/1` and the downstream `topic-selection/2`; partial packaging never
  reaches cold review, source review or compilation.
- Identity: the manifest records all author families used after route fallback, and assessment
  refuses a reviewer family that participated in packaging. V5 has disjoint Temporal/PydanticAI
  identities and an exact `topic-selection-v5` optional pre-flight suite. The web remains on V3.
- Scale check: a pathological 2,400-sentence/four-hour fixture with one opportunity per sentence
  creates 206 section-preserving work items. Every item contains at most 12 opportunities; the
  rendered request excludes later inventory entries and transcript text and remains under 30 KB.
  Pure admission/assembly tests, connected workflow success/refusal tests, the mocked five-stage
  qualification, all Ruff checks and the complete Python type check pass.
- Exact gate: clean commit `d354517f77df5f94f9d3bcd2709cc9c4269451b3` passed at
  `2026-09-14T14:43:09.646Z`. The pipeline suite reported 1,247 passed and one skipped; both
  production images rebuilt from cold dependencies, and all 18 Playwright workflow tests passed.
- Documentation: added `docs/design/bounded-author-packaging-2026-09-14.md`; updated the indexed
  architecture, checkpoint contract, V4 follow-up, pre-flight runbook and repository decision.
- Limits: source review still produces one whole-portfolio answer and repair can receive all
  findings at once. No paid model/GPU call, staging change, deployment or real-source run was
  performed.

### X draft

Temnia's author no longer has to package a four-hour inventory in one answer. The harness assigns at
most 12 section-owned opportunities per call and publishes a selection only after every exact shard
passes source and ownership checks. Partial work stays visible; it cannot silently reach rendering.

### LinkedIn draft

After dividing transcript discovery into bounded section calls, the next scaling trap was the
author: one answer still had to package every opportunity from the full recording.

Temnia now freezes a complete author work plan. Each call receives at most 12 inventoried
opportunities, investigates the source through the shared index tools, and returns candidates owned
by that exact work item. Candidate extents can cross a section boundary when the discussion needs
it; work boundaries do not become video cuts.

The downstream selection exists only when every planned shard is present and valid. A rejected or
interrupted answer leaves its response, work identity and source checkpoint available, while source
review and rendering remain blocked from the partial portfolio.

The local four-hour stress fixture keeps each request under 30 KB even with one opportunity per
sentence. This proves bounded request and assembly behavior, not editorial quality. Bounded source
review, repair and real 44-minute/two-hour/four-hour evaluation remain.

## Bounded independent source review

- Done: added `standalone-topics/6` and `TopicSelectionWorkflowV6`. V6 reuses the bounded V4
  inventory and V5 author package, then replaces the whole-portfolio source answer with an immutable
  review plan, bounded shards and a complete-manifest gate.
- Work ownership: candidate decisions use batches of at most four, opportunity judgments at most
  12, and exact overlap/handoff work at most two pairs. Candidate and opportunity ownership follows
  the section containing the first candidate or earliest opportunity-core sentence; relationships
  follow the left candidate.
- Omission correction: one exact scan now owns each source-index leaf. It receives every candidate
  intersecting that leaf and every connected opportunity, bounded at 16 and 48, and must read the
  complete leaf span. Planning refuses a denser local representation instead of letting the
  reviewer falsely call a candidate from another batch missing.
- Tool authority: `browse_source` accepts only the assigned section and `inspect_candidate` only the
  assigned IDs. Search and exact reads remain source-wide for distant dependencies. Final admission
  proves the complete browse and inspection cursors, hybrid search, every required exact read and
  the response's durable-checkpoint dependency.
- Admission: exact ordered assignments, work-item ID namespaces, finding scope, opportunity-to-
  inspected-candidate scope, missing-opportunity leaf ownership, existing V4 review rules and
  author/reviewer family independence are all replayed in code.
- Completion: only every exact admitted `topic-source-review-shard/1` in plan order produces
  `topic-source-review-manifest/1`. A partial or rejected review records the settled evidence and an
  assessment reason, but has no repair authority.
- Qualification: added the exact optional `topic-selection-v6` five-stage suite with the changed
  local source-review request. Earlier programme reports cannot qualify it. No provider was called.
- Scale check: a synthetic four-hour source with 2,400 six-second sentences and 1,200 candidates
  produces 10 sections, 75 leaf omission scans and 1,078 total bounded review work items. The
  largest rendered request is 20,242 bytes and the mean is 9,605 bytes. This is a request-shape
  stress test; 1,078 calls would be costly and is not a proposed real portfolio size.
- Validation: 45 focused planning, connected-workflow and qualification tests pass, together with
  Ruff, full Pyright, contract lint/type checks and generated-schema drift checks. The clean gate
  passed for exact commit `234cd9929a5bf144baa1db4e14894aa4b181e646` at
  `2026-09-14T15:29:51.984Z`: 1,256 pipeline tests passed with one intentional skip, the full
  monorepo check/build graph passed, both production images were built cold, and all 18 Playwright
  workflow tests passed.
- Documentation: added `docs/design/bounded-source-review-2026-09-14.md`; updated the indexed
  architecture, checkpoint contract, prior bounded-stage follow-ups, route pre-flight and repository
  decision.
- Limits: repair still receives every required finding in one answer. No paid model/GPU call,
  staging change, deployment or real-source run was performed.

### X draft

Temnia's independent reviewer no longer has to judge a whole long-video portfolio in one answer.
Candidate structure, opportunities, leaf-level omissions and pairwise handoffs now have bounded
owners. Repair starts only after every exact review shard is present; partial findings cannot edit a
video.

### LinkedIn draft

After bounding discovery and authoring, one large request remained: the independent reviewer still
had to compare every proposed video and return the whole verdict at once.

Temnia now freezes that work before review. Every candidate gets an explicit internal-structure
decision, opportunities are judged in bounded batches, and overlaps and handoffs are reviewed in
exact pairs. Missing-topic detection uses one source-index leaf per scan with all intersecting local
coverage, so splitting candidate decisions into batches cannot manufacture a false omission.

Each call can browse only its assigned section and inspect only its assigned candidates. The
complete portfolio review exists only when every shard passes source, ownership and checkpoint
checks. Until then, the evidence remains visible but no partial finding can authorize repair.

The synthetic four-hour stress case keeps its largest review request at 20,242 bytes. It also shows
the other side of bounded work: an unrealistic 1,200-candidate portfolio needs 1,078 calls. Real
provider behavior, cost and human editorial quality still require measured full-source runs.

## Bounded atomic repair

- Done: added `standalone-topics/7` and `TopicSelectionWorkflowV7`. V7 retains the complete V4–V6
  indexed path and replaces the one whole-assessment patch answer with deterministic connected
  components of required findings, affected candidates and opportunities.
- Plan and authority: a component contains at most 12 findings, eight existing candidates, 24
  opportunities and eight source sections. An oversized coupled component refuses for explicit
  adjudication; code does not split shared authority just to fit a request.
- Evidence loop: each component gets the three source tools, a section allowlist and its own durable
  `repair` checkpoint chain. Every operation and added candidate belongs to the exact work-item
  namespace, every assigned finding must be cited, and every replacement span must have an admitted
  exact source read.
- Atomic completion: up to three components run concurrently. Every settled output becomes an
  admitted `topic-repair-shard/1` or an exact retained rejection. Assembly requires all shards in
  plan order, proves their actual candidate/opportunity writes disjoint, reruns the full patch
  validator and applies the aggregate once. A missing, invalid or conflicting shard changes no
  selection.
- Loop closure: an admitted aggregate creates one new selection revision, clears stale plans and
  assessment, adds repair families to reviewer exclusion, and runs fresh cold and bounded source
  review. The existing repair allowance still bounds complete correction cycles.
- Programme and qualification: registered a separate Temporal/PydanticAI V7 identity, experiment
  mapping, frozen programme manifest and optional `topic-selection-v7` five-stage pre-flight. The
  changed patch stage is `topic-selection-patch-component/1`; earlier reports do not qualify it.
- Scale check: a synthetic four-hour source with 2,400 six-second sentences, 1,200 candidates and
  1,200 independent title findings produces 1,200 one-finding components. A rendered component
  prompt stays below 50 KB and excludes distant transcript text. This proves bounded shape, while
  also exposing the potentially high total call count of a pathological portfolio.
- Validation: eight repair planning/admission/scale tests, the mocked connected V7 workflow,
  registration and the exact mocked five-stage qualification pass. The clean exact-commit gate
  passed for `e04e56d738fef895244d5653c8795ee1c76e209b` at `2026-09-14T16:12:26.345Z`: 1,268
  pipeline tests passed with one intentional skip, alongside 33 contract, 202 web, 41 database
  isolation, three legacy-boundary, two repository-script and 18 Playwright tests; all lint, type,
  build and both cold production-image checks passed.
- Documentation: added `docs/design/bounded-atomic-repair-2026-09-14.md`; updated the indexed
  architecture, checkpoint contract, V6 follow-up, route pre-flight and repository decision.
- Limits: no paid provider/GPU call, staging change, deployment or real-source run was performed.
  Real V7 tool behavior, 44-minute/two-hour/four-hour latency, cost and human editorial quality
  remain unmeasured.

### X draft

Temnia's repair model no longer receives every review finding in one growing prompt. Findings that
share a candidate or opportunity stay in one bounded component; independent components can run in
parallel. No edit lands until every shard is present and code proves their writes do not conflict.

### LinkedIn draft

Bounding transcript discovery, authoring and independent review still left one large final answer:
the repair model could receive every required finding and candidate in the portfolio at once.

Temnia now turns required findings into a graph. Findings connected through a candidate or
opportunity stay together, preserving coupled editorial authority. Each component investigates the
source through the same indexed browse, search and exact-read tools and keeps its own durable
checkpoint.

The components do not edit the selection as they finish. Code waits for every exact shard, rejects
any overlapping writes, replays the full patch validator and applies one aggregate revision. A
missing or invalid component leaves the assessed selection unchanged. A successful revision gets a
fresh independent review.

A synthetic four-hour stress case keeps every repair request bounded across 1,200 independent
findings. That is request-shape evidence, not a claim about cost or editorial quality. Exact route
qualification and controlled real 44-minute, two-hour and four-hour evaluation are next.

## Indexed implementation production review

- Done: reviewed `2140fbd` across indexing/reuse, all five tools, checkpoints, V3–V7 orchestration,
  stage admission, routes/accounting, recovery, compile/render gates and committed staging config.
  Added `docs/design/indexed-harness-production-review-2026-09-14.md` with seven actionable findings,
  remaining capabilities, an ordered remediation sequence and the refusal/termination matrix.
- How: source inspection, installed SDK inspection and pure fixture reproductions. No application
  code changes, provider calls, deployments or staging mutations.
- What went wrong: the green suite checks bounded history and audit identities but misses dropped
  navigation/media observations. Internal-structure coverage is still mostly prompt language;
  resource-count batching does not guarantee the final evidence fits; route filtering uses only
  the current author despite retaining all contributors.
- Numbers: 28 focused existing tests passed in 2.89 s. Local reproductions showed an 80-sentence
  candidate admitted after one exact read, a completely read 321-sentence range refused after
  retention dropped it to 320, and 75 four-hour omission calls against 64 committed dispatches.
  Installed PydanticAI defaults to 50 model requests per invocation. Browse previews and source
  ranges disappeared from the actual compacted next request.
- Next: fix observation delivery, typed internal review/evidence-sized continuation, all-family
  route exclusion, explicit limits and stopped-state recovery before live indexed evaluation.

### X draft

Reviewed Temnia's indexed harness after its green gate. Found a real bug: compacting agent history
removed the search descriptions the model needed to choose what to read. A source review could
also accept 80 sentences after reading one. Tests passed; production proof is still ahead.

### LinkedIn draft

Today I reviewed the entire indexed harness, including how it refuses work and what can resume.

The most useful finding was at the tool boundary. We kept source IDs and bounded the conversation,
but removed browse descriptions, search ranges and media measurements before the next model turn.
The model had an audit trail without the observations it needed.

A second reproduction accepted an 80-sentence candidate after one exact sentence read. The prompt
asked for internal-structure review, but admission did not require the supporting inspection.

The existing focused tests still passed: 28 in 2.89 seconds. The review now records seven findings,
the limits that stop runs, and the next implementation order. Reliable long-source processing needs
tests of what an agent can actually see and substantiate, alongside schema and checkpoint tests.


## Recoverable indexed work and the prompt-size clarification

- Done: implemented bounded observation retention, cumulative delivered-speech coverage, three
  corrective admission attempts, SDK tool/output corrections, sibling settlement, nullable dispatch
  ceilings, all-contributor reviewer exclusion and V7 cold-review pagination. Removed the evidence
  assembler's raw word/sentence count refusal. Details and remaining work are in
  `docs/design/indexed-harness-recovery-2026-09-14.md`.
- Why: Rajesh wants long uploads attempted and recoverable technical errors corrected. Clarified
  that complete candidate evidence is read over bounded pages; it is not a full transcript prompt.
- Validation so far: 97 focused tests passed. Added regressions are being verified with the full
  gate. No live provider call, expense, deployment or publication occurred in this change.
- What went wrong: the first retry prompt appended diagnostic prose after source JSON, breaking the
  fixture parser; moved it before the prompt. Existing refusal fixtures expected one call; updated
  them to prove three retained attempts. The V7 fixture's foreign cold candidate ID was corrected;
  the new admission properly refuses it instead of silently losing that judgment.
- Next: complete verification, then adaptive subdivision, continuation and controlled long-source
  measurements. This work is not yet a claim that the harness is production-ready.
- X draft: Long-video reliability needs more than a smaller first prompt. Today I fixed evidence
  disappearing between tool calls and gave invalid work units a corrective retry path. Reading a
  whole candidate can happen in pages; the whole transcript does not belong in every prompt.
- LinkedIn draft: Today’s harness work separates transcript size from prompt size. Reviewers keep a
  bounded working context while durable checkpoints remember which exact speech reached earlier
  requests. Invalid work units get diagnostic correction attempts, and cold review reads only the
  proposed video’s speech. Tests cover these mechanics; long-source editorial acceptance is still
  a separate measurement.

- Verification follow-up: 100 focused pipeline tests and 34 contract tests passed. The first full
  gate on `b488871` stopped at three web tests. Two expected the removed dispatch defaults; the
  third exposed an existing-run idempotency check comparing old frozen settings with today's
  defaults. Fixed the lookup and retained scoped request/policy/brief checks; rerunning the gate.
