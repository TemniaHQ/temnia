# 2026-09-08 — S2 second-review hardening

Dated session record. The later runtime follow-ups record the authorized PR #23 merge, paired
staging rollout and long-source result, followed by PR #24 implementation. Earlier statements
that work was unrun or uncommitted describe those earlier sessions and editorial inspections.

## Done

Implemented the fourteen residual findings from the second S2 review on the existing PR #23.
The work covers timing normalization, revision-safe transcript editing, retry ownership and
recovery, storage accounting, GPU result framing, deployed smoke identity, HLS reuse, immutable
model loading, parameter validation, and gate cleanup. No merge or deployment was performed.

## How

Recorded the follow-up plan in the PR before code changes. Three agents implemented separate
areas; the coordinating session handled integration, accounting, CI cleanup and documentation.
Independent review found and corrected additional integration details: Turbo dropped HF_HOME;
unknown Temporal statuses needed an explicit closed-status allowlist; the smoke needed the actual
GPU build identity; incoming envelopes must require protocol fields; corrupt playlist text must
reject reuse; and separate Modal environments alone do not isolate Temporal dispatch.

The storage regression uses two real PostgreSQL connections and checks the database's blocker
graph. Model setup loaded the actual pinned SaT, MiniLM and tokenizer snapshots; the model suite
then ran offline. The media checks also encoded a real four-second video/audio/I-frame ladder
with local ffmpeg, published it to obstore, and verified reuse. Browser regressions use delayed
real server-action responses and revision requests, plus a large generated transcript.

## What went wrong / limits

The first pnpm invocation found an uninitialized native launcher; running its installed setup
restored the pinned launcher. Read-only Ruff commands initially tried to write their cache outside
the sandbox; a task-specific temporary cache resolved that without changing project settings.
The previous Docker credential-helper hang did not recur in the bounded registry probe.

The first exact-commit gate passed all package checks and built both images, then passed 13 of
14 browser tests. The retry-error test used a broad alert selector that also matched Next's route
announcer; the expected error was present. The selector was scoped to the transcript. Independent
SDK-source review also caught that workflow descriptions spell cancellation `CANCELLED`, unlike
the proto's `CANCELED`; the closed-status set is now checked against the SDK type and tests cover
each terminal status. Nothing was pushed from that failed gate.

Protocol 4 cannot be rolled underneath workers still expecting version 3. Drain old activities
and saved GPU handles, deploy Modal, run the explicit deployed smoke, then start matching workers.
Live GPU smoke, GPU cost/quality measurements and the staging scale run remain unrun in this session.
HLS checks detect missing/changed-size objects and changed playlists, not same-size media corruption.
Unknown submission acknowledgments still cannot prove zero duplicate paid work.

## Numbers

- Fourteen second-review findings addressed; no dependency or lockfile change.
- Pipeline: 444 passed in preflight, then 445 in the first exact-commit gate with real models
  offline. The final focused GPU/media suite also passed 164 cases.
- Full web unit suite: 124 passed. Gate cleanup: 2 passed. Python strict type checking: 0 errors.
- Four new browser regressions; the virtualization case contains 27,900 words.
- Local media smoke: 4 seconds, 12 named artifacts, 592,326 media bytes and 593,198 retained bytes
  including its completion marker.
- Exact-commit gate, image builds and browser delivery evidence live in the local-ci receipt and
  PR #23's required checks; these are the delivery authority rather than a forecast in this log.

## Tomorrow / next

Review and merge at Rajesh's discretion. Follow the documented paired rollout, run the deployed
speech smoke and the staging scale/quality measurements, and record actual GPU time and cost.
Then continue S3's source-evidence and edit-compiler work against the annotated corpus.

## X draft

Hardening Temnia's transcript spine for chapter cutting: a word must keep its timing, an edit its
revision, and a retry its owner. Fixed 14 review findings and passed 445 pipeline checks. The next
proof is the deployed GPU path on real footage.

## LinkedIn draft

Today I worked on the reliability underneath Temnia's chapter-cutting workflow.

A missing word timestamp could borrow seconds from a long pause. A slow transcript refresh could
pair an old passage with a new revision number. Two transactions could each look correct while
leaving the storage ledger wrong together.

This review round addressed fourteen findings and added tests for those exact situations. The
pipeline suite passed 445 checks with real models loaded offline, and the web unit suite passed
124. Browser coverage now includes delayed saves and a 27,900-word transcript.

The deployment still has its own proof to earn. The new GPU result format needs a coordinated
rollout, followed by a smoke test that checks the build actually doing the transcription. After
that come real-footage quality and cost measurements.

## Follow-up: live GPU proof and architecture audit

### Done / how

Rajesh authorized the live GPU smoke and asked whether the original review and the entire
architecture were implemented. The existing staging app reported protocol 3. Deployed the exact
PR #23 pipeline code at `7135852` under a temporary named protocol-4 app, without starting a
Temporal worker or replacing the staging app. Ran the standalone deployed-handle smoke against
real WhisperX, alignment, diarization and object storage. Helper and GPU identities matched the
expected build; all expected words were present. The helper deleted its test prefix before
returning, and the temporary app was stopped afterward. No merge or production rollout occurred.

Added `docs/design/harness-implementation-status-2026-09-08.md` with code evidence, original-finding
status and the raw measured smoke report. Corrected the review header's overly broad claim that
all defects were fixed. The fourteen follow-up fixes are implemented, but persisted evidence,
the edit compiler, model routing, budgets/attempt accounting and the chapter editing workflow
are still unfinished. The audit also found conflicting parallel-dispatch/budget assumptions, a
missing unknown-outcome state in the proposed call table, and no adequate failed-GPU accounting
destination. Sprint labels do not make these complete.

### Numbers / limits

- Nine-second real speech sample: 27 words, English, one speaker, zero interpolated word timings,
  no missing expected words.
- GPU-function elapsed time: 29.2 seconds. Smoke-helper elapsed time: 35.9 seconds. These are
  application measurements, not a retrieved provider bill or a throughput benchmark.
- Source/config build: `3f2c496c9aa90a16ed74f4eb35bd38ebe4513eac7b17e8b3a3eeb6f33bd9ed55`.
- The exact runtime-code commit had already passed 445 pipeline, 124 web, 37 DB isolation,
  18 contract, 3 legacy-oracle, 2 script and all 14 production-image Playwright tests.
- No runtime code changed for this follow-up. Its documentation commit goes through the same
  exact-commit verified gate; the receipt and required PR check record its outcome.
- Remote logs warned that TorchCodec file decoding was unavailable. The existing in-memory
  waveform path completed successfully; file decoding is not established by this test.
- Representative scale/quality, peak VRAM, incomplete-recognition detection, failed-attempt cost
  and the paired staging rollout remain unproven. The original staging app remains protocol 3.

### Tomorrow / next

Complete the remaining architecture in the order recorded in the implementation-status report.
Resolve the operation/budget state machine first, then versioned evidence and the deterministic
compiler, then prove the chapter workflow against annotated real recordings. Deploying the current
PR still requires draining old work and pairing Modal/worker versions after Rajesh's merge.

### X draft update

Temnia's real GPU speech check passed: 27 words, all expected phrases, no interpolated timings.
That proves a short path through the deployed build. The larger editing harness still needs its
evidence layer, edit compiler and measured quality/cost proof. Keeping those claims separate.

### LinkedIn draft update

Today the hardened Temnia transcription build processed a real speech sample on a deployed GPU.
Recognition, word alignment, speaker assignment and storage all completed, with the exact build
identity checked at both ends.

The sample returned 27 words and all expected phrases. It reported 29.2 seconds inside the GPU
function; that is not an invoice or a long-recording benchmark.

The accompanying architecture audit matters just as much. Fourteen follow-up fixes are in code,
but versioned source evidence, the deterministic edit compiler and the model budget/attempt ledger
are still unfinished. A successful smoke test should make the status more precise, not inflate it.


## Build-in-public research review

### Done

Reviewed both supplied build-in-public guides and revised them under `docs/build-in-public/`,
with a short index and a dated research assessment. Replaced the old strategy entry point and
updated the agent decision record and sprint-plan session-log rule to agree with the revision.
Rajesh clarified that the old posting habits were unresearched and asked to change them where
warranted; the new guidance intentionally supersedes those habits.

### How

Independent agents reviewed the attachments, checked official platform sources and reviewed the
new guides. The coordinating session inspected the linked frequency study's methodology and
wrote the final strategy. Platform limits, observational associations and editorial choices are
separated. The product story leads with chapters/coverage; the founder and product accounts have
different reader promises. Existing daily logs remain the evidence and draft history.

### Went wrong / limits

The first review framing treated the old cadence as a constraint. Rajesh corrected that, and the
review was reframed around whether the guidance is supported. The supplied frequency article's
summary is stronger than its linked analyst write-up; the shown SQL excludes posts with five or
fewer engagements, so exact growth/per-post forecasts are not adopted. The browser tool's domain
allowlist prevented its first source reads; official pages were retrieved through the web tool.
The X Business page was available through indexed text but returned 404 directly; it supports
only qualitative writing advice. No social analytics, live profile state or conversions were audited.

The original checkout was on a merged, stale branch with unrelated changes. Work uses a clean
`docs/build-in-public-guides` worktree from `origin/main` at `555eb24`; the existing edits are
preserved. An initial patch attempt to replace a file in one delete/add patch was rejected before
changes; the replacement was then applied normally. No application behaviour changed.

### Numbers

- Two supplied documents reviewed; four current reference files saved in the new docs folder.
- Initial trial: 7 founder X + 3 product X + 5 personal LinkedIn posts weekly, with optional
  independent founder extras. These are proposed workload numbers, not measured results.
- Initial budget: 30 minutes/day; workload review after 14 days and audience review after 28.
- Zero social posts published or scheduled, zero profile changes, and zero growth results claimed.
- Documentation checks and exact-commit validation are recorded in the delivery evidence and
  required PR checks, not inferred from these editorial changes.

### Tomorrow / next

Rajesh reviews the strategy PR. Record the actual first week using the trial, final published
copy and available response data; revisit workload before increasing the number of posts.

### X draft / LinkedIn draft

No additional daily social pack was requested during this document review. Earlier drafts in
this log are preserved. The new guides contain explicitly labelled profile/format examples for
later review; they are not approved or published posts and do not replace those earlier drafts.


## Initial two-post proposal — superseded by the expanded pack below

### Preparation record

Rajesh requested today's posts under the revised guide. Tuesday's baseline is one founder X
and one personal LinkedIn post. These two are the selected review candidates for this request;
earlier drafts remain historical versions. No extra product post is prepared for Tuesday;
Wednesday is the next product-account day. No posts were published or scheduled by this session.
The optional question about already-published posts has no confirmed answer at preparation;
actual publication history remains unknown, not assumed empty.

Read both guides, today's earlier drafts, yesterday's drafts, the committed staging qualification
and the current separate chapter-worktree evidence. The current remote PR #24 head is still a
documentation commit. Its passing local-ci status does not validate the uncommitted chapter
implementation. This pack uses a completed staging run and a recorded read-only detector probe;
it makes no claim that the new harness is delivered or publicly available.

The two takeaways differ: progress reporting needs verification even when processing succeeds;
detector/transcript disagreement needs listening before it becomes an omission claim. Both posts
are text-only, with no required screenshot, customer footage, access link or provider-cost claim.
The CPU crash/retry lesson is retained as unused backlog rather than adding another late-day post.

### Evidence for this pack

- [Committed staging qualification at PR #24's documentation head](https://github.com/TemniaHQ/temnia/blob/35de833f58604d85825e5c4591828bafacf3d4db/docs/design/s2-staging-qualification-2026-09-08.md): merged runtime `555eb24`,
  existing audio extract from a 9,060.473-second recording, 660.3-second workflow, 20,587 words.
  Source duration is rounded to 151 minutes; workflow elapsed to about 11 minutes. No fresh
  upload/HLS encode, recognition-accuracy benchmark or provider invoice is implied.
- The same report's Browser checks and findings: displayed progress stayed 0% through recognition,
  alignment and diarization, then showed 100% during writing, despite healthy heartbeats.
- The same report records a last word end at 9,044,040 ms and source end at 9,060,473 ms:
  16,433 ms without recognized words, rounded to 16 seconds in the LinkedIn copy.
- Detector evidence was inspected in `docs/design/harness-foundation-probes-2026-09-08.json` in
  the `feat/chapter-editing-core` worktree. That source file was uncommitted at inspection, so
  it is not represented as a remotely delivered artifact. The minimal metadata snapshot below
  preserves the referenced values without copying recordings, text, source IDs or access details.
- The detector compares against protocol-4 raw recognition spans; it is not a human reference
  transcript or a word-alignment accuracy label. Its 434 ms after the last recognized word is
  less than half a second. Other short disagreements are prompts to listen, not confirmed omissions.
  The old raw warning's stronger omission wording is deliberately not repeated in public copy.
- The intended chapter/coverage workflow and need for uncertainty/human evaluation are recorded
  in the accepted product direction and today's chapter follow-up plan. The final next-step
  sentence is a plan, not a claim that human annotation has already happened.

Inspected probe-source SHA-256: `f4dcef062a4d9f5afd7c8b54164a404f293e652432b0b1c5f3603b8835958b7e`.

```json
{
  "observedAt": "2026-09-08T14:57:36.831264+00:00",
  "sourceDurationMs": 9060473,
  "inputTranscriptSha256": "c4222ccd028e5be24270c8411baaba589000bf997578161b322b674c8e1b51be",
  "fullProbeReportSha256": "2c5e98e91f8fb326f835dd3eccaf1ba754013c49956b1823eaf478774ea19a7f",
  "detector": "silero-vad-6.2.1-op15-16khz",
  "detectorRevision": "7e30209a3e901f9842f81b225f3e93d8199902b1",
  "assessmentStatus": "needs_review",
  "detectorSpeechAfterLastRecognizedWordMs": 434,
  "largestUncoveredRecognitionSpanMs": 2119
}
```

### XP1 — founder X — draft v1

- Account: `x_personal` / @RajeshBuilds.
- Editorial date: 2026-09-08, Asia/Kolkata.
- Reader: builders and people designing long-running editing workflows.
- Takeaway: a completed job does not establish that its progress display was accurate.
- Evidence: committed staging qualification above; no additional run was performed for this draft.
- State: observed behaviour on one deployed staging run; public access is not asserted.
- Asset: none required; text-only.
- Checks: 229 characters including line breaks. Plain ASCII, no links: ordinary count
  equals standard X weighted count. No internal hostname, customer identity or competitor name.
- Review: ready for Rajesh's review; approval pending. Known log drafts checked for repeated
  takeaways; actual same-day publication history remains unconfirmed.
- Publication: this version not published or scheduled by the drafting session; no URL.

Public copy:

```text
Temnia transcribed a 151-minute recording in about 11 minutes on staging.

The UI stayed at 0% through recognition and alignment, then jumped to 100% while writing.

I had a completed transcript and a misleading progress display.
```

### LI1 — personal LinkedIn — draft v1

- Account: `linkedin_personal` / Rajesh's personal profile.
- Editorial date: 2026-09-08, Asia/Kolkata.
- Reader: editors, long-form creators and video teams evaluating transcript-based chapter work.
- Takeaway: distinguish a transcript gap, a detector disagreement and a human-confirmed omission.
- Evidence: committed staging qualification and the preserved detector snapshot above.
- State: one completed staging transcription plus a separate detector comparison; chapter
  editing remains work under development. No human quality result or launch is asserted.
- Asset: none required; text-only.
- Checks: 840 ASCII characters including line breaks, 131 words; below LinkedIn's 3,000-character
  limit. The body distinguishes detections from proven omissions and labels the next work as a plan.
- Review: ready for Rajesh's review; approval pending. The founder framing restates recorded
  product intent; it does not invent a customer interview or a personal reaction.
- Publication: this version not published or scheduled by the drafting session; no URL.

Public copy:

```text
An empty stretch in a transcript isn't automatically silence.

Today's Temnia staging run processed a 151-minute recording in about 11 minutes and returned 20,587 words.

The last 16 seconds had no recognized words. An independent speech detector marked less than half a second of that tail as speech. Across the recording, it also flagged short gaps between detected and recognized speech.

Those flags tell me where to listen. They don't establish that words were missed.

That distinction matters for chapter editing. A gap in the transcript can affect where one chapter ends and the next starts.

I'm building Temnia around complete chapters from long recordings. Before trusting those cuts, I want uncertain passages to be visible during review.

Next: listen to the flagged spans and test chapter boundaries against human annotations.
```

### Delivery and next

The two drafts are saved as one selected pack in the existing daily log; earlier session records
are preserved. Their text, counts and evidence are independently reviewed before the docs commit
is delivered with `pnpm push:verified` on the existing strategy PR. Its exact-SHA receipt and
required check record validation; they do not qualify uncommitted runtime in another worktree.
No application changes, model/media probes or social-account actions are part of this drafting task.

If posting moves beyond September 8, revise "Today's" in LI1 before review; do not backfill missed
slots in a burst. Rajesh can edit and publish the selected copy, then record the actual account,
final text, publication time and URL. Keep missing analytics and publication facts unknown.


## Expanded September 7–8 pack — v2; founder drafts superseded by v3 below

### Request, selection and publication state

Rajesh confirmed that **nothing has been published today**, then asked for more than one post to
cover the substantial work across yesterday and today. This five-post pack supersedes the initial
two-post proposal above. Its earlier LI detector story remains unused backlog; XP2 carries forward
the reviewed progress-display copy. September 7 publication history remains unconfirmed.

Prepared **three founder X posts, one product X post and one personal LinkedIn post**. Each X
post has a distinct takeaway. LinkedIn develops a separate reliability story connecting two
revision-integrity failures and their repairs. This is the requested expansion for this material,
not a permanent five-post daily target. The product post is independently warranted under the
account guide; if published, count it toward the week's product total. Tuesday's default is a
planning aid, not a reason to withhold a useful requested draft.

All five are text-only. No screenshot, recording, customer quote or access URL is required.
The founder posts are independent originals, not variants or a numbered thread. Keep their
chronology if publishing later: change "Yesterday" in XP1 to "On September 7". Pick posting
windows when Rajesh can respond; preparing a larger pack does not require publishing it all at
once. No post was published or scheduled by this session, and the user's publication-history
answer is not approval of the exact copy.

### Evidence and limits

- **XP1 — redesign after parity:** the September 7 late-evening log records Rajesh's correction,
  Segment-any-Text sentence boundaries, chapter candidates and retained legacy baseline. The
  initial parity result applied to fixtures; it was not proof of editorial accuracy. The revised
  sentence implementation is merged, but its quality on human-annotated recordings is unmeasured.
  Legacy paragraph rules remain; this post does not claim the whole legacy was discarded.
  [Dated log](https://github.com/TemniaHQ/temnia/blob/555eb248bb309855279e3f722c676eb0a43dabcd/docs/log/2026-09-07.md#late-evening-the-substrate-redesigned-the-harness-researched-s3-rewritten); [implemented sentence splitter](https://github.com/TemniaHQ/temnia/blob/555eb248bb309855279e3f722c676eb0a43dabcd/apps/pipeline/src/temnia_pipeline/substrate/sat.py).
- **XP2 — progress display:** the immutable staging qualification and numeric scope in the initial
  proposal above remain the evidence: one 151-minute source, about 11 minutes end to end, progress
  0% through recognition/alignment, 100% while writing. Existing audio was reused; no new ingest,
  HLS encode, accuracy benchmark, invoice or repaired-progress claim is implied.
- **XP3 — crash probe:** one submitted CPU-only cloud call with function retries set to zero entered
  two observed containers. The second refused the execution claim. Zero inference calls occurred;
  this proves neither GPU recovery nor zero duplicate billing. The new speech guard and chapter
  integration were uncommitted at inspection. Minimal evidence below preserves the actual probe
  values without publishing provider handles or infrastructure identifiers.
- **XH1 — shared-boundary review:** the September 4 coverage decision and September 8 follow-up
  plan require selecting neighbouring cuts together. The local chapter compiler and focused
  rendering checks support ongoing implementation, not a launched or fully qualified workflow.
  The post explicitly says it is being built and full validation remains underway.
- **LI1 — accepted corrections:** review found the per-revision object overwrite path; the merged
  implementation uses per-attempt keys and conditional revision publication. The browser stale-save
  test uses two open tabs in sequence; it does not reproduce a simultaneous cross-store race.
  The delayed-refresh regressions test revision identity with real delayed action responses.
  The staging qualification establishes deployment of these merged fixes. No customer data-loss
  incident, production reproduction or universal guarantee is asserted.
  [Correction implementation](https://github.com/TemniaHQ/temnia/blob/555eb248bb309855279e3f722c676eb0a43dabcd/apps/web/app/actions/transcript.ts#L336); [browser regressions](https://github.com/TemniaHQ/temnia/blob/555eb248bb309855279e3f722c676eb0a43dabcd/apps/web/e2e/transcript.spec.ts#L287); [follow-up plan](https://github.com/TemniaHQ/temnia/blob/555eb248bb309855279e3f722c676eb0a43dabcd/docs/plans/s2-hardening-followup-360-view.md).

CPU probe metadata inspected in the active chapter worktree; source SHA-256: `f4dcef062a4d9f5afd7c8b54164a404f293e652432b0b1c5f3603b8835958b7e`. The source remains uncommitted, so this
snapshot records observation evidence, not verified runtime delivery.

```json
{
  "observedAt": "2026-09-08T14:42:24.761780+00:00",
  "functionRetries": 0,
  "gpu": null,
  "observedContainers": 2,
  "result": {
    "admissionRefused": true,
    "inferenceCalls": 0
  },
  "applicationStopped": true,
  "admissionObjectRemoved": true
}
```

### XP1 — founder X — redesign after parity — selected v2

- Account: x_personal / @RajeshBuilds.
- Editorial date: 2026-09-08, Asia/Kolkata; source work September 7–8.
- Takeaway: A faithful port can preserve a decision that deserves re-examination.
- Evidence: matching entry above.
- State: Merged sentence implementation and recorded founder decision; quality comparison remains ahead.
- Asset: none required; text-only.
- Checks: 250 ASCII characters including line breaks; 38 words. No links; ordinary count equals
  standard X weighted count, within 280 characters.
- Review: pending Rajesh's review of this exact copy.
- Publication: not published or scheduled by this session; no URL.

Public copy:

```text
Yesterday, Temnia's new sentence splitter passed parity tests against the old version.

I asked for a redesign anyway.

It now predicts sentence breaks from text instead of trusting transcript punctuation. The old rules stay as a comparison baseline.
```

### XP2 — founder X — long-run progress — selected v2

- Account: x_personal / @RajeshBuilds.
- Editorial date: 2026-09-08, Asia/Kolkata; source work September 7–8.
- Takeaway: A completed job does not establish that its progress display was accurate.
- Evidence: matching entry above.
- State: Observed on one deployed staging run; no claim the display was repaired.
- Asset: none required; text-only.
- Checks: 229 ASCII characters including line breaks; 36 words. No links; ordinary count equals
  standard X weighted count, within 280 characters.
- Review: pending Rajesh's review of this exact copy.
- Publication: not published or scheduled by this session; no URL.

Public copy:

```text
Temnia transcribed a 151-minute recording in about 11 minutes on staging.

The UI stayed at 0% through recognition and alignment, then jumped to 100% while writing.

I had a completed transcript and a misleading progress display.
```

### XP3 — founder X — crash/retry assumption — selected v2

- Account: x_personal / @RajeshBuilds.
- Editorial date: 2026-09-08, Asia/Kolkata; source work September 7–8.
- Takeaway: Test container recovery separately from a function retry setting.
- Evidence: matching entry above.
- State: Completed CPU-only service probe; no inference, GPU or billing qualification.
- Asset: none required; text-only.
- Checks: 255 ASCII characters including line breaks; 40 words. No links; ordinary count equals
  standard X weighted count, within 280 characters.
- Review: pending Rajesh's review of this exact copy.
- Publication: not published or scheduled by this session; no URL.

Public copy:

```text
I disabled retries, then deliberately crashed a cloud container in a Temnia CPU test.

The same call ran in a second container.

A saved execution claim blocked it before model work. Zero inference calls.

Retry settings alone didn't prevent re-execution.
```

### XH1 — product X — shared chapter boundaries — selected v2

- Account: x_product / @TemniaHQ.
- Editorial date: 2026-09-08, Asia/Kolkata; source work September 7–8.
- Takeaway: Review both adjacent sections when moving a chapter cut.
- Evidence: matching entry above.
- State: Design under implementation; complete workflow validation remains underway.
- Asset: none required; text-only.
- Checks: 255 ASCII characters including line breaks; 40 words. No links; ordinary count equals
  standard X weighted count, within 280 characters.
- Review: pending Rajesh's review of this exact copy.
- Publication: not published or scheduled by this session; no URL.

Public copy:

```text
Review a chapter cut from both sides: the ending you keep and the opening it leaves behind.

Temnia's chapter workflow is being built around shared boundaries, so moving one cut means reviewing both neighbours.

Full workflow validation is still underway.
```

### LI1 — personal LinkedIn — preserving accepted edits — selected v2

- Account: linkedin_personal / Rajesh.
- Editorial date: 2026-09-08, Asia/Kolkata; source work September 7–8.
- Takeaway: Revision safety must protect accepted files and the text in the editor together.
- Evidence: matching entry above.
- State: Fixes merged and deployed to staging; full chapter and human-quality validation remains ahead.
- Asset: none required; text-only.
- Checks: 990 ASCII characters including line breaks; 164 words. Within LinkedIn's 3,000-character limit.
- Review: pending Rajesh's review of this exact copy.
- Publication: not published or scheduled by this session; no URL.

Public copy:

```text
A database can reject a stale transcript edit while the accepted file has already been overwritten.

That failure path turned up in Temnia's review across September 7 and 8.

The fix writes each attempted correction to its own file. Only the save that wins the revision check becomes the accepted version. A rejected save can no longer overwrite that file.

The same review found another way to lose an edit: a delayed refresh could pair older transcript text with a newer revision number. The editor now keeps revision identity tied to the text being edited.

Both changes are in the build deployed to staging. Browser tests cover a stale save from a second tab and delayed responses.

These details matter for the chapter workflow I'm building. An editor needs to correct words and review cuts without a stale browser tab silently undoing earlier work.

The next proof is a complete render-and-review journey with human judgement of the chapter boundaries. That validation is still ahead.
```

### Session completion record

- Done: reviewed the two-day evidence and earlier drafts; prepared five distinct selected posts;
  marked the initial two-draft proposal superseded and retained its history.
- How: checked dated records, merged implementation/browser tests, deployed-build evidence and
  the uncommitted CPU probe. Preserved minimal probe metadata and exact public-copy counts.
- Limits: September 7 publication history and human editorial-quality results remain unknown.
  The previous two-post selection underrepresented the requested two-day scope; the expanded pack
  follows Rajesh's correction. No application code or other worktree was changed.
- Verification: independent evidence/copy review plus Markdown, count and whitespace checks;
  final commit delivery uses `pnpm push:verified` on the existing open strategy PR. Exact-SHA
  receipt and required GitHub status establish repository validation, not additional product proof.
- Numbers: 5 selected drafts (3 founder X, 1 product X, 1 personal LinkedIn), 0 social actions,
  0 new media/model runs. Unused versions and prior test totals are not counted as new work.
- Next: Rajesh edits and posts as appropriate; record final account, copy, timestamp and URL after
  actual publication. Human chapter evaluation and runtime qualification belong to the build task.


## Premium+ founder revision — current five-post selection

Rajesh reconfirmed on September 8 that his personal X account has Premium+ and asked for a little
more explanation when useful, while keeping posts concise. Earlier drafts were unnecessarily
constrained to the standard cap despite the already-recorded Premium+ context. The guides now
make the account-specific preference explicit: founder drafts have no 280-character editing cap;
roughly 50–100 words is a flexible starting range for an explanation, with shorter posts welcome.
The product account's subscription remains unconfirmed. No larger permanent posting quota follows.

**Current selection:** XP1 v3, XP2 v3 and XP3 v3 below, plus the exact XH1 v2 and LI1 v2 copy in
the previous pack. This still makes five posts. Prior founder versions are preserved as history,
not additional posts. No new facts about publication are inferred from this wording preference;
these revisions are not published or scheduled by this session and await review of the exact copy.

### Added context and evidence

- XP1 explains what fixture parity established and why it left the design choice open. September 7's
  redesign record supports that sequence. A comparison on annotated recordings remains future work;
  the post claims no measured quality improvement over the legacy baseline.
- XP2 includes the already-measured 20,587 words and speaker stage. Healthy heartbeats and changing
  stages establish active work despite 0%. The planned stage/elapsed fallback comes from the
  September 8 follow-up plan §1; this post does not claim the display repair was deployed.
- XP3 states the order: save an immutable claim, crash, observe another container refuse that claim.
  The preserved CPU probe in the previous pack and its recorded limits support that sequence.
  Zero inference calls applies to this probe. Crashes before the guard remain unqualified; no
  GPU-recovery or billing guarantee is claimed.

All evidence references and snapshots from the previous pack remain applicable. These are three
revisions of the same distinct stories, with extra causal explanation rather than extra headlines.
All are text-only. If XP1 is used after September 8, change "Yesterday" to "On September 7".

### XP1 — founder X — redesign after parity — selected v3

- Account: x_personal / @RajeshBuilds; Premium+ confirmed by Rajesh.
- Editorial date: 2026-09-08, Asia/Kolkata; source work September 7–8.
- Takeaway, evidence and delivery state: same story as v2; added context checked above.
- Asset: none required; text-only.
- Checks: 426 ASCII characters including line breaks, 65 words; concise founder draft
  using confirmed longer-post access. The 280-character cap is not applied to this account.
- Review: pending Rajesh's review of this exact revised copy.
- Publication: not published or scheduled by this session; no URL.

Public copy:

```text
Yesterday, Temnia's sentence splitter passed parity tests against the old version.

That showed the Python port reproduced the old behaviour. It left the design question open: was that behaviour worth keeping?

I asked for a redesign. Sentence breaks now come from a text model instead of relying on transcript punctuation. The old rules remain as a comparison baseline.

Next: compare both approaches on annotated recordings.
```

### XP2 — founder X — long-run progress — selected v3

- Account: x_personal / @RajeshBuilds; Premium+ confirmed by Rajesh.
- Editorial date: 2026-09-08, Asia/Kolkata; source work September 7–8.
- Takeaway, evidence and delivery state: same story as v2; added context checked above.
- Asset: none required; text-only.
- Checks: 425 ASCII characters including line breaks, 68 words; concise founder draft
  using confirmed longer-post access. The 280-character cap is not applied to this account.
- Review: pending Rajesh's review of this exact revised copy.
- Publication: not published or scheduled by this session; no URL.

Public copy:

```text
Temnia transcribed a 151-minute recording in about 11 minutes on staging: 20,587 words.

The progress display stayed at 0% through recognition, alignment and speaker assignment. It jumped to 100% while writing.

The work was advancing, but the percentage didn't show it.

The next change is to show the stage and elapsed time when there's no honest percentage to report. A fast result still needs a useful waiting experience.
```

### XP3 — founder X — crash/retry assumption — selected v3

- Account: x_personal / @RajeshBuilds; Premium+ confirmed by Rajesh.
- Editorial date: 2026-09-08, Asia/Kolkata; source work September 7–8.
- Takeaway, evidence and delivery state: same story as v2; added context checked above.
- Asset: none required; text-only.
- Checks: 481 ASCII characters including line breaks, 75 words; concise founder draft
  using confirmed longer-post access. The 280-character cap is not applied to this account.
- Review: pending Rajesh's review of this exact revised copy.
- Publication: not published or scheduled by this session; no URL.

Public copy:

```text
I disabled function retries, then deliberately crashed a cloud container in a Temnia CPU test.

The same submitted call entered a second container anyway.

Before the crash, the first container had saved an execution claim. The second found it and stopped before model work. The probe made zero inference calls.

That checks one failure path. Crashes before that claim is saved still need separate testing.

The retry setting alone didn't describe the service's recovery behaviour.
```

### Session completion record — length preference

- Done/how: updated both guides to v2.1 and recorded the durable preference in AGENTS.md; revised
  the three founder posts, preserved v2, and identified the current five-post selection explicitly.
- Limits: Premium+ is user-confirmed account context, not a live profile audit. Product-account
  entitlement, human chapter-quality results and subsequent publication history are not invented.
- Numbers: 3 revised drafts at 65, 68 and 75 words; the current selection remains 5 posts.
  Zero new product claims, social actions, media/model probes or subscription changes.
- Verification: exact-copy counts, Markdown links/fences, whitespace and independent factual review;
  the final commit uses `pnpm push:verified` on the same open PR. Its receipt and required status
  are the repository gate evidence.
- Next: use the concise Premium+ preference for future founder drafting; record final copy and
  publication details only after Rajesh actually posts.

## Follow-up: merge, paired staging rollout and long recording

### Done / how

Rajesh chose to merge #23 and test on staging, with findings and the remaining chapter work in a
new PR. Merged the checked head by squash to `555eb24`. Paused automatic deployments and web
submissions, confirmed Temporal and Modal were drained, stopped the old worker, deployed protocol 4
to the actual media app, and ran the deployed speech smoke. Started the matching web and pipeline
images; verified migrations, health, the worker queue, runtime configuration, all 58 packaged runtime
file hashes and the new columns through the pipeline role. Restored and verified both automatic
deployments. Staging remains live.

Used the actual deployed web action to transcribe an existing 151-minute source that had no
transcript, preserving a completed copy for comparison. A single SSH observer followed its saved
Modal call through recognition, alignment, speaker assignment and writing. A separate verifier
pinned the completed workflow run and checked its result, fresh scoped database rows, strict
transcript contract, stored bytes and ledger. All 18 consistency checks passed.

Browser checks covered transcript loading, two speaker labels, single-word search, next-match
navigation, click-to-seek and playback on the existing result. The new result appeared automatically,
searched and sought successfully. Both browser sessions had empty error/console output. These checks
used a temporary private SSH route to the deployed app, so public Cloudflare login/upload CORS were
not exercised. Temporary browsers, observer and tunnel were closed afterward.

Recorded metadata-only evidence in `docs/design/s2-staging-qualification-2026-09-08.md` and its JSON
verification record. Added `docs/plans/chapter-workflow-followup-360-view.md` for all four next steps:
remaining S2 reliability, evidence/edit/accounting foundations, the complete chapter path and honest
quality/cost evaluation. The new draft PR initially contains this evidence and plan; it contains no
runtime fixes or completed architecture implementation.

### What went wrong / limits

GitHub's linear-history rule rejected a merge commit; the subsequent squash merge succeeded.
Two read-only preflight attempts used the wrong schema field or failed to close their pool; they
were replaced with bounded reads before any rollout mutation. The first browser tunnel targeted an
unreachable Docker overlay address; using the verified private bridge address fixed access. A
post-build inspection used a wrong service suffix and another ran before worker startup; direct
checks of the new container then succeeded. These were test/rollout helper errors, not product
failures, and did not require a second transcription.

The run displayed 0% throughout recognition/alignment/diarization despite stage changes and fresh
heartbeats, then 100% at writing. The next PR must use measured progress or an indeterminate display.
The final 16.433 seconds have no recognized words; independent speech evidence or listening is still
needed to distinguish silence from an omission. Detected speakers and valid timings do not prove
recognition or speaker accuracy. No peak VRAM, forced OOM, cancellation, failed-attempt cost,
held-out quality or provider invoice was measured. This run used an existing audio extract, not a
new upload/HLS encode. The master is 1.91 GB, not tens of gigabytes.

### Numbers

- Actual deployed smoke: 27 words, one speaker, no missing expected words or interpolated timings;
  29.576 GPU-function seconds and 37.4 helper seconds.
- Long source: 9,060.473 seconds; original master 1,911,080,861 bytes.
- Workflow: 660.264 seconds (about 11 minutes); GPU function: 635.22 seconds on L4.
- First attempt, one saved/completed remote call, 20,587 words, two speakers, 241 utterances.
- Zero malformed, interpolated or zero-duration word timings; zero unassigned-speaker words.
- 2,609 alignment scores below 0.5; this uncalibrated diagnostic is not word-error rate.
- Canonical revision: 2,117,635 bytes; stored size and transcript storage ledger agree.
- One idempotent accepted transcription usage entry for 9,060 source seconds; not an invoice.
- All 18 independent result-consistency checks passed. Runtime code is unchanged from merged #23.
- The new documentation commit is delivered through the repository's exact-SHA verified gate;
  its local receipt and PR checks are the authority for that delivery.

### Tomorrow / next

Use the new PR for the remaining S2 reliability changes and chapter implementation plan. Add
independent completeness evidence, reusable speech stages/memory recovery and truthful progress;
resolve atomic reservations and failed/unknown attempt accounting before parallel model work;
persist evidence and edit contracts; then implement planning, shared-boundary compilation,
rendering, verification and human correction. Qualify editorial quality and total cost separately.
Do not declare the full harness complete from this successful long transcription.

### X draft update

Temnia's hardened transcription path processed a 151-minute recording in 11 minutes on staging:
20,587 words, two speakers, one attempt. That proves one long run. Speech completeness, recovery,
actual cost and the chapter-cutting workflow still need their own evidence.

### LinkedIn draft update

Today Temnia's transcript hardening reached staging and processed a 151-minute recording in
11 minutes. Recognition, word alignment, speaker assignment, saving and metering completed on
the first attempt. The result contained 20,587 words and two speaker labels.

I checked the completed workflow against the database, stored transcript and usage ledger, then
verified the transcript in the browser. All 18 consistency checks passed; search and word-to-video
seeking worked.

That is useful engineering evidence, not a quality certificate. The last 16 seconds need a speech
completeness check, displayed progress stayed at 0% during computation, and the reported GPU time
is not an invoice. The next work joins those reliability gaps with the source-evidence layer,
operation accounting and the complete chapter planning, cutting and review path.

## Follow-up: implementing the chapter harness on PR #24 (in progress)

### Done / how

Rajesh authorized continuing the planned harness work without another permission pause. The draft
PR now carries the implementation plan and its amendments. Work is proceeding on immutable source
evidence and edit revisions, scoped physical-attempt accounting, checkpointed speech stages,
deterministic chapter compilation, finite Temporal planning/review workflows, real media checks,
and structural transcript corrections. The integrated chapter feature is not yet qualified.

A real R2 conditional-create probe accepted its first object, refused a different second body, and
preserved the original bytes. The probe object was removed. A separate CPU-only Modal fault probe
created an immutable execution claim and then crashed its container. Modal restarted the same call
in another container despite function retries being disabled. The second container refused the
existing admission claim before inference. The temporary application was stopped and its object
removed. The speech implementation now uses that admission guard before model work.

Ran the independent ONNX speech detector on the existing 151-minute staging audio. This was a
read-only comparison against the previous protocol-4 recognition output, not another GPU run.
It found only 434 milliseconds of detector speech after the last recognized word. Across the whole
recording, many short detector/recognition disagreements exceeded the provisional aggregate review
threshold. That is a prompt to listen and calibrate, not evidence of 654 seconds of missing speech.

The worker now registers chapter workflows and checkpointed speech alongside the existing paths.
Short chapter controls have separate activity slots in the same Python process: two occupied media
slots no longer prevent cancellation admission. An actual-worker regression and complete plugin
registration probe pass. The browser journey is being connected to the production-image gate with
an explicitly synthetic model fixture; database, Temporal and ffmpeg operations remain real.

### What went wrong / limits

The container-crash probe disproved the assumption that function retry configuration alone bounds
provider execution. It still cannot measure invisible startup attempts or actual invoice charges.
The detector's original warning overstated disagreement as a confirmed omission; the current wording
now asks for review without claiming an accuracy label. A time-skipping server initially advanced
past a queued test workflow; disabling automatic time skipping fixed that scheduling test.

Review found a sandbox import leak from eager package exports, lost response recovery across workflow
owners, cancellation being overwritten by late settlement, media-slot starvation of controls, missing
review checks, and browser request state lost on reload. Fixes and regression checks are underway.
Real rendering fixtures exposed timestamp/edit-list edge cases. Twelve renderer checks now pass
against actual ffmpeg/PyAV output, including corrupt input and process cancellation. The recorded
browser run reached a three-chapter proposal with an explicit drop and real checked media. Its
next failures were useful integration findings: a query omitted reusable checks without run metadata,
an absent accepted pointer matched an unrelated artifact, and several Video.js instances shared one
player state. The first two are corrected; independent player controls are being verified.

Structural transcript browser tests found a caption speaker fallback mismatch after undo and an
unchanged word selection being retained as an unsaved draft after deletion. Both were fixed. The
split/merge/delete/insert/undo, two-tab correction, and empty-transcript journeys have each passed
against the development server, real database and recorded Temporal worker. The final production
image gate remains pending.

The installed NLTK release has an unpatched security advisory in code Temnia does not need. Removed
the runtime dependency and retained only the three pure segmentation metrics with upstream license
and attribution. A frozen oracle from the installed release matched all 18,228 comparisons exactly.
The evaluation CLI's first real ledger bundle then exposed another integration mismatch: its usage
schema accepted scalars while persisted provider usage includes nested provenance. That correction
is being tested without discarding the underlying accounting facts.

The approval journey exposed a product deadlock: all four sections were accepted but the model's
repeated request for human review still vetoed export. It also made four further verifier calls.
Human acceptance now resolves editorial concerns when technical checks pass; metadata-only review
reuses the checked media and keeps the earlier verdict as evidence. A later undo/reject journey
found verification lookup confused identical content across revisions. It now follows exact
revision and artifact dependencies; the complete chapter rerun remains pending.

Independent review found two cancellation races, weak final export hash binding, and retained
masters that could be leaked on idle workers or removed during active use. The fixes add locked
ownership/state checks, exact export dependencies, and a local-file activity lease with bounded
cleanup. The new speech path also needed to mirror liveness into the transcript row and finish
its separate harness-run state. These are operative path findings, not just helper-test changes.

Qualification checks initially required zero reserved money after a successful GPU result.
That was too strict: a completed call can still have an unknown invoice charge. Its conservative
reservation remains part of the budget. The driver now distinguishes unresolved execution from
terminal work with retained unknown-cost exposure, checks every reservation against the run total,
and saves a private diagnostic report before allowing cleanup. Lost-result recovery must show
identical physical-dispatch counts before and after the retry.

No live model gateway key or human boundary labels are available in this
session. Those measurements remain unavailable; synthetic responses do not substitute for them.

### Numbers

- R2 create-only probe: 26 bytes; original preserved; second create refused; object removed.
- Modal crash probe: one submitted CPU call, two observed containers, zero inference calls.
- Long-source ONNX probe: 23.618 seconds, 283,140 windows, 3,534 speech intervals.
- Detected speech: 7,372,712 ms; unmatched recognition spans: 653,759 ms (8.867%); largest gap: 2,119 ms.
- Detector speech after the last recognized word: 434 ms. No human omission label was assigned.
- Control scheduling regression: two real-worker cases passed with both media slots occupied.
- Worker constructor probe: six workflows, 27 pipeline activities and seven control activities at
  this checkpoint; no queue was polled by the constructor probe.
- Renderer suite: 12 passed; compiler suite: 19 passed. These counts cover their focused suites.
- NLTK parity: 18,228 oracle comparisons, maximum absolute difference 0.0; 40 tests passed and one
  pre-existing optional comparison remained skipped.
- Development browser structural journeys: 3 passed across the initial run and corrected rerun;
  the initial structural journey required a fix, and the complete chapter journey is still pending.
- Full integration, final exact-commit gate and PR delivery are still pending at this checkpoint.

### Tomorrow / next

Finish and verify rendering, finite orchestration and correction/review state. Run the full image
and browser gate, publish the same PR through the verified workflow, and qualify the additive speech
GPU path separately. Keep live model economics and human editorial accuracy explicitly unmeasured
until their evidence exists.

### X draft update

A useful failure in Temnia today: disabling function retries did not prevent a crashed cloud
container from restarting. A durable admission claim stopped the second execution before model
work. Chapter editing is taking shape; the next proof is the complete render-and-review journey.

### LinkedIn draft update

Today I tested a recovery assumption in Temnia's media pipeline: would disabling retries prevent
repeated execution after a container crash? The service restarted the same submitted call in a
second container. A durable admission claim stopped that second execution before model work.

The chapter harness now has separate control capacity so long media jobs cannot occupy every slot
needed to admit cancellation. Work is also underway on versioned evidence, exact-cover chapter
compilation, real file checks and reversible transcript corrections.

These are engineering mechanisms under test. The complete chapter browser journey, live model
cost and human editorial quality still need their own evidence.
