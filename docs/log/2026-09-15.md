# 2026-09-15

## Done

Closed the current indexed editorial recovery batch at Rajesh's explicit instruction to get the
pipeline tests green, commit and stop. Added paged structured reviewer context, exact fragments
for unusually long sentences, bounded inspection audit segments, child-workflow continuation,
admitted-decision reuse, editorial resume after incomplete review renders and reviewer-family
reservation. Wired the topic action to V7 and updated the recorded fixture.

The [implementation record](../design/indexed-harness-recovery-2026-09-15.md) explains the changes
and remaining limits. The [production completion ledger](../plans/indexed-harness-production-completion.md)
is paused, not complete. Removed unfinished connected-repair staging before closing this batch.

## How

Kept source and assignment identities in immutable artifacts. Structured context pages carry
exact JSON fragments; speech fragments preserve the original sentence ID. Admission requires
complete delivered ranges, including evidence retained in verified prior checkpoint segments.
The model receives bounded excerpts rather than a full transcript.

V7 decisions run in Temporal children. An SDK request quantum continues the same assignment from
its checkpoint without consuming an output correction attempt. Exact admitted work is reusable;
unknown paid outcomes retain their fence. Editorial progress is separate from a compiled review
video, so explicit retry can resume review and repair against the expected revision.

## What went wrong

The first full pipeline run finished with two failures: a stop-message assertion still expected
the earlier ending, and a qualification test expected the original "exactly one" prompt wording.
Updated the message assertion and restored that wording. Focused checks also caught fragment
coverage error-message expectations, a compact-map size assertion and type/formatting issues;
these were corrected before the final suite. Earlier database checks without the test database
environment were insufficient; the final full pipeline run uses a separately migrated disposable
database and enables model tests.

The new workflow tests replace Temporal I/O. They exercise the continuation decision and stable
assignment identity, but do not establish real server replay or recovery across worker crashes.
The V7 image/browser journey and live provider qualification remain unverified.

## Numbers

- Final full pipeline suite: 1,283 passed, one skipped in 152.21 seconds, with database and model
  tests enabled. The skip is the optional real AdaptiveDetector test because the qualified
  `scenedetect-headless 0.7.1` package is not installed locally.
- Focused recovery, qualification and stop-message checks: 66 passed.
- Contract tests: 34 passed; schema and generated Python drift checks passed.
- Web unit tests: 198 passed, four database-dependent tests skipped in this invocation.
- Pipeline lint/format and Pyright passed. Contract and web type checks passed.
- Structured context pages: at most eight record fragments and 12,000 characters.
- Exact speech pages: at most 16,000 characters; SDK continuation quantum: 32 requests.
- No new paid provider calls, production render evaluation, push or deployment in this batch.

## Tomorrow / remaining work

Stopped per Rajesh's instruction. Remaining work is recorded, not scheduled: connected repair
beyond current component limits, typed internal-topic decisions, cross-section reconciliation,
parent workflow-history continuation, real Temporal failure/scale tests, V7 image/provider
qualification and source-grounded editorial/playback evaluation. Pipeline success is not a
production-readiness or arbitrary-upload-length claim.

## X draft

A long-video workflow needs a useful place to resume. Temnia now keeps accepted editorial work
apart from rendered review videos, so retry can return to unfinished review. Long transcript
sentences are read in exact pages. Recovery tests are passing; live long-video validation remains.

## LinkedIn draft

A failed long-video job should not lose the editorial work it already completed.

Today's Temnia batch separates accepted editorial progress from a rendered review video. Retry
can return to unfinished review and reuse decisions tied to the exact same source and assignment.
The model reads transcript and review context in bounded pages, including unusually long sentences.

There is still a distinction between receiving every page and understanding a discussion correctly.
The recovery mechanics have regression tests; real long-source provider runs and human editorial
and playback evaluation remain necessary. I closed this batch at a documented stopping point.
