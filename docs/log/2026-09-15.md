# 2026-09-15

## Done

Closed the current indexed editorial recovery batch at Rajesh's explicit instruction to get the
pipeline tests green, commit and stop. Added paged structured reviewer context, exact fragments
for unusually long sentences, bounded inspection audit segments, child-workflow continuation,
admitted-decision reuse, editorial resume after incomplete review renders and reviewer-family
reservation. Wired the topic action to V7 and updated the recorded fixture.

The [implementation record](../design/indexed-harness-recovery-2026-09-15.md) explains the changes
and remaining limits. The [production completion ledger](../plans/indexed-harness-production-completion.md)
is paused, not complete. Removed unfinished connected-repair staging before closing this batch.

## How

Kept source and assignment identities in immutable artifacts. Structured context pages carry
exact JSON fragments; speech fragments preserve the original sentence ID. Admission requires
complete delivered ranges, including evidence retained in verified prior checkpoint segments.
The model receives bounded excerpts rather than a full transcript.

V7 decisions run in Temporal children. An SDK request quantum continues the same assignment from
its checkpoint without consuming an output correction attempt. Exact admitted work is reusable;
unknown paid outcomes retain their fence. Editorial progress is separate from a compiled review
video, so explicit retry can resume review and repair against the expected revision.

## What went wrong

The first full pipeline run finished with two failures: a stop-message assertion still expected
the earlier ending, and a qualification test expected the original "exactly one" prompt wording.
Updated the message assertion and restored that wording. Focused checks also caught fragment
coverage error-message expectations, a compact-map size assertion and type/formatting issues;
these were corrected before the final suite. Earlier database checks without the test database
environment were insufficient; the final full pipeline run uses a separately migrated disposable
database and enables model tests.

The new workflow tests replace Temporal I/O. They exercise the continuation decision and stable
assignment identity, but do not establish real server replay or recovery across worker crashes.
The V7 image/browser journey and live provider qualification remain unverified.

## Numbers

- Final full pipeline suite: 1,283 passed, one skipped in 152.21 seconds, with database and model
  tests enabled. The skip is the optional real AdaptiveDetector test because the qualified
  `scenedetect-headless 0.7.1` package is not installed locally.
- Focused recovery, qualification and stop-message checks: 66 passed.
- Contract tests: 34 passed; schema and generated Python drift checks passed.
- Web unit tests: 198 passed, four database-dependent tests skipped in this invocation.
- Pipeline lint/format and Pyright passed. Contract and web type checks passed.
- Structured context pages: at most eight record fragments and 12,000 characters.
- Exact speech pages: at most 16,000 characters; SDK continuation quantum: 32 requests.
- No new paid provider calls, production render evaluation, push or deployment in this batch.

## Tomorrow / remaining work

Stopped per Rajesh's instruction. Remaining work is recorded, not scheduled: connected repair
beyond current component limits, typed internal-topic decisions, cross-section reconciliation,
parent workflow-history continuation, real Temporal failure/scale tests, V7 image/provider
qualification and source-grounded editorial/playback evaluation. Pipeline success is not a
production-readiness or arbitrary-upload-length claim.

## X draft

A long-video workflow needs a useful place to resume. Temnia now keeps accepted editorial work
apart from rendered review videos, so retry can return to unfinished review. Long transcript
sentences are read in exact pages. Recovery tests are passing; live long-video validation remains.

## LinkedIn draft

A failed long-video job should not lose the editorial work it already completed.

Today's Temnia batch separates accepted editorial progress from a rendered review video. Retry
can return to unfinished review and reuse decisions tied to the exact same source and assignment.
The model reads transcript and review context in bounded pages, including unusually long sentences.

There is still a distinction between receiving every page and understanding a discussion correctly.
The recovery mechanics have regression tests; real long-source provider runs and human editorial
and playback evaluation remain necessary. I closed this batch at a documented stopping point.

## Review, stop and takeover (Fable)

- Done: reviewed Codex's branch at `fb67422` and again at `cd48459`; measured checkpoint growth on
  the four-hour fixture; Rajesh stopped Codex. Created `feat/topics-production` in
  `../temnia-topics` from `cd48459`, fixed the model-side toolset validator with a regression
  test, restored the web to V3, and wrote the production 360 plan.
- Numbers: last-round checkpoint 200,780 bytes (111,806 audit); serialized request 437,594 bytes;
  4.7 MB of prompt over 42 rounds for one V3 inventory call. New test: 11 cases; focused suites
  32 and 26 (web) passing; six gateway-ledger cases need `TEST_PIPELINE_DATABASE_URL` and were not
  run here.
- Next: Rajesh reviews the plan; implementation starts at step 1 (fit proof and single-call
  windows). No push, PR, deploy or paid call was made.

## `standalone-topics/8` implemented end to end (Fable)

- Done: the window program from the 360 plan, on `feat/topics-production`: pure windows,
  prompts, projection and gap-tolerant assembly (`topic_windows.py`); one activity per decision
  with transient retry, route fallback, one diagnostic correction and typed stops
  (`topic_decisions.py`); the flat parent workflow (`topic_windows_workflow.py`); route
  preferences and the projection on the run row (`runs.py`, `ChapterRunInput.routes`); the web
  allowance, author and reviewer selects, projection line and raise-allowance control; the
  recorded fixture's V8 outputs; staging file with a $100 cap and $20 default; runbook.
- Numbers: 1,278 pipeline tests passed against a migrated local database before the last
  additions; the new suites add 6 window, 4 decision and 3 workflow tests, all passing; web unit
  tests 33 passed; pyright and ruff clean; Next typecheck clean. Inventory prompts for the
  four-hour fixture are 41–53 KB against a 128 KB bound.
- What needed care: the reuse loader accepts only an index published with the worker's exact
  producer identity, so the decision test publishes it that way; artifact references are not
  hashable, so dedupe is by id; `useSortedKeys` on the contracts.
- Next: the exact-commit gate, push, PR; Rajesh runs Karma, a two-hour and a four-hour source
  on staging with the allowance and models of his choice. No paid call was made here.

## First staging run of `standalone-topics/8` and what it taught

- Run `12b9c920`, Karma, $5 allowance, Kimi author, Gemini reviewer: 83 calls, $0.87, `failed`
  with `DecisionRoutesExhausted` during bounded review. The ledger shows why: three review
  decisions in flight met HTTP 429 on Gemini and DeepSeek in bursts, and the fallback route's
  answers were cut off at my 4,096/8,192-token output allowance because DeepSeek spends its
  reasoning inside it. Two attempts twenty seconds apart per route then declared the pool
  exhausted. Inventory, packaging, eight cold reviews and ten review items had been admitted;
  five rejections were retained, all "did not finish successfully" on DeepSeek.
- Fixed on `feat/topics-production-followups`: a per-route cooldown in the worker shared by every
  caller (Retry-After or 20 s), a 20/40/80/160 s ladder per route with the provider's pause
  honoured, one five-minute pool pause before the typed stop, output allowances of 16k/32k/8k/16k/32k
  doubled at high reasoning effort, a cut-off answer retried with the allowance doubled, and the
  two typed-stop messages a failed patch had dropped from `run_failures.py`. Projection now prices
  typical answers (Karma: about $1.58 against $0.63 observed) and reads correctly.
- Verified: fallback ladder, exhaustion, rejection, truncation retry and cooldown tests through the
  ledger against Postgres; no paid call in this session.

## The retried run stopped on `outcome_unknown`; that stop is now settled by the worker

- Run `12b9c920` retried after #51 and stopped at 91 calls with "A provider call ended without a
  confirmed outcome." The ledger: one Gemini review stream dropped after announcing generation
  `gen-1789468147-PmzISSHGfv7FaBYixcdC`; the budgeted model's twenty-second receipt wait found
  nothing; the run was fenced; the one reconciliation at workflow failure skipped the attempt
  because the CLI reconciles only terminal attempts; nothing asked the gateway again. Nineteen
  minutes later the gateway still answered 404 for that generation. A design hole, not a
  provider fact: the ledger's owned recovery path existed and had one test, and no caller.
- On `fix/topics-outcome-unknown`: `receipts.py` drives that recovery from receipts (reported
  charge → settled at the charge; generation unknown to the gateway ten minutes after the stream
  ended → released at zero with the 404 retained; recorded but uncosted → fence kept). The
  decision activity waits on a 30 s to 5 min ladder (17.5 minutes) and continues on its route;
  a sibling decision that meets the run fence waits the same way. The reaper settles fenced V8
  runs whose execution has ended and parks them retryable; live executions are left alone
  (Temporal `describe` is the liveness fact, bound in the worker). The unknown-outcome sentence
  and the panel's Retry refusal now say what happens next.
- Verified against Postgres: reported receipt settles and lifts the fence and a fresh attempt is
  allowed; absent receipt keeps the fence inside the grace then releases at zero; a recorded
  receipt without cost keeps the fence; an attempt without a handle is unresolvable; parking
  needs a clear ledger and the ending execution's identity; the reaper settles a finished run and
  leaves a live one; a decision recovers its own dropped stream through the ledger and finishes
  on the same route; a receipt that never settles is the typed stop after the ladder.

## Direction: simplify to production, frontier first

- Rajesh asked why the routes are flaky, whether to leave OpenRouter, and whether LangGraph or
  Google ADK would help. Assessment: the route pool and OpenRouter's billing were the causes, the
  frameworks are not; no rewrite. Plan written and merged as
  `docs/plans/topics-simplification-360-view.md`; decisions recorded in AGENTS.md. The unit cost
  freak-out was answered bottom-up: about $4.50 to $9 for a four-hour source on frontier seats,
  against $6 per hour of source at clip-tool retail and $100 to $300 per episode for a human edit.
- Implementation starts in a new session: PR 1 vendors and usage (Karma on frontier models),
  PR 2 deletion, PR 3 scale (two-hour and four-hour runs), PR 4 evaluation on three held-out
  recordings.

## PR 1 of the simplification: vendors and usage (Fable)

- Done, on `feat/topics-simplification-1`: direct vendor routes (`vendor`, account terms, no
  gateway facts) with a two-family rule per pool; `harness/vendors.py` (keys, adapters with SDK
  retries off, settings injection with thinking, tier and Anthropic's automatic cache, usage
  settlement at the route's prices, rate-limit header readings and the gate's `pace`); the
  budgeted model on a vendor route reserves the typical answer, settles from usage, settles a
  dropped stream or cancellation at its estimate and retries, abandons a dead execution's
  attempt at its estimate (`ledger.abandon_attempt`) instead of fencing the run; an optional
  `inventory` seat with its own fallback position (Sonnet 5 on staging); `gateway: direct` in
  the config contract; Logfire tracing when `LOGFIRE_TOKEN` is set; `temnia-harness vendors
  probe`; the staging snapshot `0a40fd58` with the D2 seats and the vendors' published prices;
  the runbook and the web test. Design record:
  `docs/design/topics-vendors-and-usage-2026-09-15.md`.
- Verified: the whole pipeline suite, 1,340 passed and 12 skipped in 146 s against the migrated
  local database; usage settlement with cache reads (840 micros
  on the fixture prices), a dropped stream settling at its estimate with the run still
  `running`, a crashed execution abandoned then dispatched again, a keyless vendor skipped then
  named in the stop; the legacy decision and receipt suites unchanged. No paid call was made.
- Not done here: the three vendor keys on `pipeline` (Rajesh, once), the ZDR requests to the
  vendors (the snapshot records `zero_data_retention: false` until then and the boot log says
  so), the Karma run on the frontier seats, and steps 2 to 4 of the plan.

## First Karma run on the frontier seats: $18.81 on the row, $4.44 at the vendors, no video

- Run `ba09a55d` (98 calls, `needs_review`, 13 candidates all withheld). Read from the ledger over
  SSH: repair $15.78 of the $18.81; 15 attempts `transport-dropped` at 0 seconds, each settled at
  its estimate (about $0.95, Opus at half a 65k allowance), $14.37 in all; successful attempts
  $4.44, inside the plan's $4 to $7. Two defects, both mine:
  1. `VendorModel` closed its httpx client on `__aexit__`, and the budgeted model enters the
     context once per request while reusing the instance across a decision's rounds, so every
     second round failed instantly on a closed client and was retried through a fresh model.
     Nothing reached Anthropic; the estimate settlement charged the run anyway.
  2. The repair prompt named the candidate namespace but not the operation namespace the
     admission enforces (`<workItemId>:operation:`); all 14 repair patches were rejected (the
     model wrote `repair-component-0001:op:extend-end-karma`), so the reviewer's 18 findings
     (openings mid-thought, unfinished discussions) were never repaired and every candidate stayed
     withheld.
- Fixed on `fix/vendor-client-lifecycle`: the vendor client is rebuilt on every context entry and
  closed on exit; an HTTP request hook marks a dispatch as sent, and a failure before that mark
  settles at zero (`transport-unsent`) instead of the estimate; the repair instruction names both
  namespaces (`topic-repair-window/2`). Tests: client lifecycle across two contexts, the sent and
  unsent settlements through the ledger, the instruction text.
- The panel's "Failed to fetch" was the browser tab holding a pre-deploy server action; a reload
  clears it. The run row's $18.81 overstates the vendors' bill by the $14.37 of unsent
  settlements; the Anthropic console is the true figure for this run.

## Second Karma run after #55: no phantom settlements; repair still rejected on shape

- Run `ebec806a`: 81 calls at $4.81 with every attempt real (no `transport-dropped` or
  `transport-unsent`), so the client fix holds. The projection's 52 counts decisions; the row
  counts HTTP requests: 3 inventory, 6 author rounds over 3 items, 12 cold, 35 review over 34
  items, and 24 repair rounds over 5 components with 9 rejections and their corrections.
- The repair patches now carry the right operation ids and fail the next rules: `extend_start`,
  `extend_end`, `replace_extent`, `replace_candidate` and `retitle` must return the replacement
  under the existing candidate id (the model minted `<workItemId>:candidate:` ids, which my
  version-2 instruction told it to do for every replacement), and a replacement's extent must
  stay inside the inline windows, which are `repair_source_indices` (findings, affected
  candidates and their opportunities, eight sentences of context); reading further with
  `read_source` does not authorize extending into it.
- Rather than learn one rule per paid run, `topic-repair-window/4` states every model-facing
  rule of the repair admission as a numbered block: operation count and reasons, finding
  citation, the operation namespace, identity on edits, the extent rules per kind, the finding
  kind each change needs, the shapes of drop, merge, split and add_opportunity, new-id rules,
  and the opportunity constraints; the windows are the whole authorized extent, with "drop and
  say why" as the answer when coherence needs speech outside them.
