# 2026-09-10 — Compact chapter transport and staging continuation

Working date is Asia/Kolkata; operational timestamps below use UTC on September 9.
This session continues after Rajesh merged PR #29. No post has been published.

## Done

Verified both staging services on merge `0f6d43cffe37350d2d4f30effcc1072c5da6bc32`.
Qualified the compact summary/proposal/verifier request path on three model families.
DeepSeek/DeepInfra and Kimi/Alibaba passed; Qwen/Alibaba returned schema data instead
of a summary and was refused. One bounded replacement probe passed for GLM/Baseten.

Deployed a new immutable route snapshot to the worker and web, preserving the previous
snapshot, mounts, budget caps and unknown run. Actual service/container configuration,
worker boot, route validation, file write protection, web release marker and health all
passed. A single authenticated browser submission started a new run on the 151-minute
recording with the original brief and a separate $2 cap. It stopped at summary 15 because
the model ranges did not exactly cover that source window. No edit was produced.

Implemented a versioned whole-window source fallback and guarded same-run Retry for this
known planning refusal. The exact response recovers offline with all 80 sentences preserved.
The 14 accepted v1 reports retain their actual bytes, hashes and fingerprints, and recompute
identically from their saved responses. Staging remains stopped pending the recovery PR's merge.

## How

Used exact deployed images, synthetic qualifier inputs, strict structured output, ZDR
routing, pinned providers and disabled automatic SDK retries. Saved immutable responses
and matching gateway charge records before validating. Independent offline review checked
request/schema/prompt identities and the new snapshot's proofs.

Compared replacement candidates using fresh primary metadata and the actual retained
long request. GPT-OSS/Baseten did not fit the application's conservative context admission;
no call was purchased. GLM/Baseten provided sufficient listed context and a distinct provider.
These observations select an experiment route; they do not establish editorial quality.

Configuration reload used a single bounded, journaled operation with private backups and
readiness checks between worker and web. The browser showed the old unknown result and
disabled recovery controls before the new admission. The new run has a bounded read-only
observer that preserved both ledgers and performed no retry, cancellation or budget change.
The production evaluation exporter verified the final artifacts and lineage, and the completed
217-event history was captured. The transcript's creation-time hash was absent in the old
database row; the evidence artifact independently bound the expected raw transcript hash.

The production Temporal replayer accepted all four retained histories: 77, 191, 361 and 217
events. Focused grounding/export tests, database retry races and web query tests passed.
The real Postgres/Temporal continuation regression reused 15 persisted responses, preserved
14 existing report identities and all distinct recorded charges, produced one v2 recovery report,
and reached global proposal preparation with a provider-dispatch trap observing zero calls.
Production-image browser testing and the full exact-commit gate are required for PR delivery.

## What went wrong

Qwen/Alibaba returned a one-element array containing the requested JSON Schema, with a
normal finish. The validator correctly refused it. The retained response does not identify
whether the model, provider adapter or gateway adapter caused the error. The older route
proof cannot qualify a changed prompt and schema; replacing it required fresh paid evidence.

The long-source run then exposed a separate coverage failure. Its fifteenth response ended
normally, but its summary ranges were invalid. Validation stopped further work; all 15 calls
have known charges. The UI offered starting over and disabled Retry even though the paid
responses were retained. The follow-up plan adds a narrow whole-window source fallback with
an explicit diagnostic and guarded same-run continuation. It keeps the existing model wire,
prompt identities and valid grounding reports unchanged.

## Numbers

- First compact batch: seven calls, 509,157 micros admitted, 38,359 micros reported;
  two Qwen stages skipped after the first failure.
- Replacement: three calls, 17,448 micros admitted, 391 micros reported.
- Qualification total: ten calls, **526,605 micros admitted; $0.038750 reported**.
  No new unknown outcomes or unresolved charges in qualification.
- Old source run retained: 27 dispatches, **$0.020236 reported; $0.109644 reserved**.
- New source run: **$2 cap**. The full $0.80 qualification envelope plus both source
  allocations remains within the original $3 session ceiling: $2.929880 maximum planned.
- Configuration rollout completed 19:25:32 UTC; new browser run admitted 19:26:18 UTC.
- New run completed at 19:41:15 UTC after **14m 57s**, with 15 known charges totaling
  **$0.009397**, zero reserved and zero repairs. Median model-call latency was **52.073s**.
- Fourteen accepted reports: 119 passages, 112 reference-valid first pass, seven source
  fallbacks correcting nine misplaced anchors. The fifteenth report was refused.
- Recovery checks: 42 focused Python tests, 22 database run tests, three Temporal persistence
  tests, two web database tests and 50 focused web tests passed. All four retained histories replayed.
- No proposal, edit, render, independent verdict, human acceptance or export was produced.

## Tomorrow

After the recovery fix is verified and merged, continue the same source run without repeating
its 15 paid responses. Then inspect actual chapter media and the review journey. Human
listening and labelled boundary quality remain separate from transport and mechanical checks.

## X draft

Today Temnia's compact chapter planner passed live format checks across three model
families. One route returned the schema instead of an answer; validation caught it.
Ten qualification calls cost $0.038750. The 151-minute test then caught a summary coverage
error. Its recovery now preserves every source sentence and reuses the saved responses;
the merged fix still needs to complete the real recording test.

## LinkedIn draft

Temnia is testing chapter planning on a complete 151-minute recording. Before that run,
we requalified the exact compact request format against live providers.

Two routes passed immediately. A third returned the requested schema instead of summary
data, so we retained the failed response and qualified a replacement. Ten calls reported
$0.038750 in total, with no unresolved charges in those probes.

The recording run stopped at summary 15 because its ranges did not cover the input window
exactly. It reported $0.009397 with no unresolved charge. No chapter edit was produced.

We added a complete-source fallback and same-run recovery for this case, preserving the
saved responses. Passing format checks does not tell us whether chapter boundaries are good;
that still needs actual output inspection and listening.
