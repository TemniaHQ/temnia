# 2026-09-10 — Compact chapter transport and staging continuation

Working date is Asia/Kolkata; operational timestamps use UTC with the relevant date.
This session continues after Rajesh merged PRs #29 and #30. No post has been published.

## Done

Verified both staging services on merge `0f6d43cffe37350d2d4f30effcc1072c5da6bc32`.
Qualified the compact summary/proposal/verifier request path on three model families.
DeepSeek/DeepInfra and Kimi/Alibaba passed; Qwen/Alibaba returned schema data instead
of a summary and was refused. One bounded replacement probe passed for GLM/Baseten.

Deployed a new immutable route snapshot to the worker and web, preserving the previous
snapshot, mounts, budget caps and unknown run. Actual service/container configuration,
worker boot, route validation, file write protection, web release marker and health all
passed. A single authenticated browser submission started a new run on the 151-minute
recording with the original brief and a separate $2 cap. It stopped at summary 15 because
the model ranges did not exactly cover that source window. No edit was produced.

Implemented a versioned whole-window source fallback and guarded same-run Retry for this
known planning refusal. The exact response recovers offline with all 80 sentences preserved.
The 14 accepted v1 reports retain their actual bytes, hashes and fingerprints, and recompute
identically from their saved responses.

After PR #30 merged, verified both deployed images and worker source hashes. The refreshed
browser shows its recovery copy and enabled Retry. A scoped pre-resume capture reproduced the
saved run's operation, attempt, artifact and dependency hashes; the protected unknown run is
unchanged. The run remains stopped while a newly found renderer heartbeat gap is corrected.
No Retry, model call, budget change or acceptance occurred in this follow-up.
The render correction now covers whole-activity liveness, lease-owned deletion
and failure expiry after lease release. Independent production review is complete.

## How

Used exact deployed images, synthetic qualifier inputs, strict structured output, ZDR
routing, pinned providers and disabled automatic SDK retries. Saved immutable responses
and matching gateway charge records before validating. Independent offline review checked
request/schema/prompt identities and the new snapshot's proofs.

Compared replacement candidates using fresh primary metadata and the actual retained
long request. GPT-OSS/Baseten did not fit the application's conservative context admission;
no call was purchased. GLM/Baseten provided sufficient listed context and a distinct provider.
These observations select an experiment route; they do not establish editorial quality.

Configuration reload used a single bounded, journaled operation with private backups and
readiness checks between worker and web. The browser showed the old unknown result and
disabled recovery controls before the new admission. The new run has a bounded read-only
observer that preserved both ledgers and performed no retry, cancellation or budget change.
The production evaluation exporter verified the final artifacts and lineage, and the completed
217-event history was captured. The transcript's creation-time hash was absent in the old
database row; the evidence artifact independently bound the expected raw transcript hash.

The production Temporal replayer accepted all four retained histories: 77, 191, 361 and 217
events. Focused grounding/export tests, database retry races and web query tests passed.
The real Postgres/Temporal continuation regression reused 15 persisted responses, preserved
14 existing report identities and all distinct recorded charges, produced one v2 recovery report,
and reached global proposal preparation with a provider-dispatch trap observing zero calls.
Production-image browser testing and the full exact-commit gate are required for PR delivery.

For the renderer follow-up, six new tests exercise the production entry and real filesystem
lease, plus fresh and reused media paths with delayed I/O seams. The two main regressions
also fail against an isolated pre-fix tree: quiet rendering times out and a cancelled waiter
deletes the owner's file. All 20 focused and existing media tests pass, as do Ruff and Pyright.
Root replayed all four retained histories again with the unchanged production workflow code.
These tests do not substitute for decoding and listening to the full staging recording.

## What went wrong

Before resuming after PR #30, a renderer review found that strict decode, artifact I/O and
cached-source hashing could be silent longer than the activity's 30-second heartbeat limit.
A separate lease edge allowed a cancelled waiting attempt to remove the active attempt's
revision workspace. These were found by code review before the long source reached rendering.
The correction extends the existing owned heartbeat supervisor and keeps deletion inside
the acquired lease. It does not change models, budget, render encoding or artifact identities.

Qwen/Alibaba returned a one-element array containing the requested JSON Schema, with a
normal finish. The validator correctly refused it. The retained response does not identify
whether the model, provider adapter or gateway adapter caused the error. The older route
proof cannot qualify a changed prompt and schema; replacing it required fresh paid evidence.

The long-source run then exposed a separate coverage failure. Its fifteenth response ended
normally, but its summary ranges were invalid. Validation stopped further work; all 15 calls
have known charges. The UI offered starting over and disabled Retry even though the paid
responses were retained. The follow-up plan adds a narrow whole-window source fallback with
an explicit diagnostic and guarded same-run continuation. It keeps the existing model wire,
prompt identities and valid grounding reports unchanged.

## Numbers

- First compact batch: seven calls, 509,157 micros admitted, 38,359 micros reported;
  two Qwen stages skipped after the first failure.
- Replacement: three calls, 17,448 micros admitted, 391 micros reported.
- Qualification total: ten calls, **526,605 micros admitted; $0.038750 reported**.
  No new unknown outcomes or unresolved charges in qualification.
- Old source run retained: 27 dispatches, **$0.020236 reported; $0.109644 reserved**.
- New source run: **$2 cap**. The full $0.80 qualification envelope plus both source
  allocations remains within the original $3 session ceiling: $2.929880 maximum planned.
- Configuration rollout completed 19:25:32 UTC; new browser run admitted 19:26:18 UTC.
- New run completed at 19:41:15 UTC after **14m 57s**, with 15 known charges totaling
  **$0.009397**, zero reserved and zero repairs. Median model-call latency was **52.073s**.
- Fourteen accepted reports: 119 passages, 112 reference-valid first pass, seven source
  fallbacks correcting nine misplaced anchors. The fifteenth report was refused.
- Recovery checks: 42 focused Python tests, 22 database run tests, three Temporal persistence
  tests, two web database tests and 50 focused web tests passed. All four retained histories replayed.
- No proposal, edit, render, independent verdict, human acceptance or export was produced.
- September 10 follow-up: both PR #30 deployments verified at 01:33 UTC; unchanged paid
  results and old unknown exposure verified before Retry. Zero additional provider calls.
- Renderer follow-up: six new cases plus 14 existing media cases passed in 38.40 seconds;
  four retained production histories replayed successfully. Two isolated pre-fix cases failed
  as expected, confirming that the tests detect the corrected defects.

## Tomorrow

After the renderer follow-up is verified and merged, continue the same source run without
repeating its 15 paid responses. Then inspect actual chapter media and the review journey. Human
listening and labelled boundary quality remain separate from transport and mechanical checks.

## X draft

Today Temnia's compact chapter planner passed live format checks across three model
families. One route returned the schema instead of an answer; validation caught it.
Ten qualification calls cost $0.038750. The 151-minute test then caught a summary coverage
error. Its recovery now preserves every source sentence and reuses the saved responses;
the merged fix still needs to complete the real recording test.

## LinkedIn draft

Temnia is testing chapter planning on a complete 151-minute recording. Before that run,
we requalified the exact compact request format against live providers.

Two routes passed immediately. A third returned the requested schema instead of summary
data, so we retained the failed response and qualified a replacement. Ten calls reported
$0.038750 in total, with no unresolved charges in those probes.

The recording run stopped at summary 15 because its ranges did not cover the input window
exactly. It reported $0.009397 with no unresolved charge. No chapter edit was produced.

We added a complete-source fallback and same-run recovery for this case, preserving the
saved responses. Passing format checks does not tell us whether chapter boundaries are good;
that still needs actual output inspection and listening.

## Session follow-up — visible staging flow (provisional)

This checkpoint records the later September 10 session. After the first boundary adjustment
and playback checks, Rajesh's listening review found a cutoff at the third chapter's end.
A replacement run now deliberately drops the incomplete trailing question. Its corrected
third render plays to completion; acceptance and UI export await Rajesh's review. The final
outcome must be added before this entry is finalized. Earlier facts and drafts remain
historical records. No post was published.

### Done

Rajesh merged PR #31 as `abbe7de849f8a4c99ee94ac542cb8c44ed4fc22e`. The pipeline deployment
completed and was checked against its actual running container at 02:19 UTC. The web retained
its PR #30 image with the matching harness configuration. Neither saved 151-minute run was
retried or modified: the 15-response planning run and the older unknown outcome remain intact.

Rajesh uploaded a new 43:56 recording. Its source and WhisperX transcript reached ready,
and a live chapter run stopped after its second summary failed strict quote grounding.
Both paid responses remain saved. The refusal was diagnosed without changing the run.

Created a real four-minute excerpt from Rajesh's local uploaded recording using stream copy
to produce a usable example before another code and release cycle. The excerpt was uploaded
through the product UI. Its fresh transcript produced 51 sentences and 515 words. A new live chapter
run proposed three chapters on its first attempt, rendered all three, completed their technical
checks and independent model verification, and reached reviewable revision 1 at 03:09 UTC.
The authenticated browser verified advancing playback time without media errors in all three
rendered chapter players, and exercised the first shared-cut preview on the source player.
These are live provider responses and actual rendered files, with browser checks recorded
separately from human editorial acceptance.

Then previewed the second shared cut and used the browser's boundary control to move it
from 160.560 to 161.000 seconds. Revision 2 completed with the same 161.000-second join
on both neighboring chapters and visible manual-boundary flags. All 51 technical checks
passed. The unchanged first chapter reused its media and captions; both neighbors received
new media and captions, with fresh check artifacts for all three chapters under the new edit.
Both updated neighboring players advanced without media errors in the browser.

The original cut was already inside a transcript gap: this is a pacing adjustment, with no
claim that the earlier cut was proven wrong. The independent verifier used text evidence and
the technical report and returned `needs_review`, requiring human listening. The review
workflow completed without active ledger work or Temporal activities. All three sections
were left proposed with no accepted revision, and Rajesh was asked to listen before UI export.

Rajesh listened and specifically reported that the third chapter was cut off at its end.
Inspection traced that defect to the artificial four-minute sample boundary, described below.
A fresh live run on the same uploaded excerpt requested three kept chapters followed by an
explicit drop of the incomplete final sentence. It reused the accepted evidence and the first
two chapter media files from the initial revision, rendered a new third chapter, passed all
51 technical checks and completed model verification with no run error. No new upload or
production code change was needed. The earlier runs and their paid outputs remain intact.

The new third chapter is 77.520 seconds long. In the browser, its final portion advanced from
66.018195 to 77.520 seconds and reached `ended=true`, `paused=true`, `readyState=4`, without
a media error. A screenshot recorded the actual video at its end. The player was then left
paused at 66 seconds, with 11.520 seconds remaining for Rajesh's listening review. This verifies
playback to the corrected endpoint; no approval or acceptance has been recorded.

### How

Used finite observers over one SSH process per observation, with server-local polling and
organization-scoped, read-only database transactions. Exact Temporal execution identities,
artifact hashes, model attempts, reported charges and unresolved reservations were retained
in private receipts. Earlier chapter runs were checked for unchanged state.
No production code, route snapshot or model configuration was changed in this follow-up.

The four-minute input fits one evidence window, so it goes directly to a DeepSeek/DeepInfra
proposal and a GLM/Baseten verifier without hierarchical summary calls. That is the existing
short-source path. It does not exercise the longer recordings' summary path.

### What went wrong

The 43:56 run's second summary covered all 80 input sentences in eight correctly ordered
units, but expanded the quote references into 953 word IDs. Of those, 795 were real source
words inside their declared units that had not been supplied as prompt-visible anchors.
There were no invented IDs, foreign-window references, gaps or overlaps. The validator
refused the response before its existing fallback. The exact saved prompt and response were
checked against accepted evidence; the proposed repair remains deferred while the visible
flow is completed. Repeating the same paid model calls was not part of this diagnosis.

The demonstration excerpt itself ended at 240.040 seconds, truncating the question
"So what is karma?", which finishes at 240.445 seconds in the full recording. That artificial
crop created the cutoff Rajesh heard. Advancing video time and passing technical checks had
not established a complete spoken ending. The replacement run ends kept content at 238.080
seconds and deliberately drops 238.080–240.040, preserving the whole existing input as keeps
plus an explicit drop. The old pacing adjustment and its paid verification remain saved in
their original run.

### Numbers at this checkpoint

| Observation | Recorded result |
| --- | --- |
| New full recording | 2,636 seconds; 126,408,770 bytes; 579 sentences; 6,203 words |
| Full-recording run | `a5b878d4-82db-4ac8-99f0-c4f8f5b3844d`; revision 0; two model calls; $0.002323 reported; zero reserved |
| Real excerpt | 240.040 seconds; 11,946,655 bytes; transcript revision 1; one transcription attempt |
| Initial excerpt run | `8e412fe7-b54a-46d4-ad89-f6e769be6d77`; three chapters; revision 1; two model calls; $0.001509 reported; zero reserved; no repairs |
| Initial player checks | Playback reached 22.2073, 8.6327 and 15.8431 seconds in the three chapter players, without media errors |
| Source cut previews | First preview: 55.960–61.9827 seconds; second preview: 157.560–163.617 seconds |
| Boundary edit | Revision 2 complete; boundaries 0, 58.960, 161.000 and 240.040 seconds; second shared cut moved 440 ms |
| Updated chapter durations | 58.960, 102.040 and 79.040 seconds; 51 of 51 technical checks passed |
| Updated player checks | Both changed neighbors played to 14.672142 and 13.207283 seconds without media errors |
| Revision 2 model accounting | Three total calls; $0.003091 reported, including $0.001582 for the latest GLM verifier; zero reserved |
| Replacement excerpt run | `aa5fb076-4497-443a-bac1-838554bae077`; revision 1; three keeps and one drop; two model calls; $0.001544 reported; zero reserved; no repairs |
| Replacement partition | Keeps 0–58.960, 58.960–160.560 and 160.560–238.080 seconds; drop 238.080–240.040 seconds; 51 of 51 technical checks passed |
| Corrected third player | Duration 77.520 seconds; played 66.018195–77.520 through the end without media errors; left paused at 66 seconds for review |
| Preserved earlier planning run | 15 model calls; $0.009397 reported; zero reserved; revision 0 |
| Preserved unknown run | 27 model calls; $0.020236 reported; $0.109644 reserved; revision 0 |

These charge figures cover recorded model calls, not total ingest, GPU or storage costs.
Rajesh authorized further spending to reach a working result, superseding the earlier aggregate
session restriction. The current live runs use the deployed $3 per-run maximum.

### Next

Obtain Rajesh's review of the corrected ending, complete UI export after human editorial
approval, then verify the downloaded files.
Keep functional verification distinct from human acceptance. Finalize this entry with the actual
outcome and charges. The 43:56 and 151-minute recordings remain unqualified for the complete
chapter flow; the four-minute result does not close those larger-source tests.

## Follow-up — Automatic intelligence and default chapter creation

Rajesh explicitly rejected manually supplied sentence partitions as evidence that the harness
works intelligently. He requested automatic editorial corrections, a one-click default action
and the pending Chapter-Llama integration. This supersedes the preceding operator-led demo's
next step; that demo and its expenses remain preserved.

### Done and how

Implemented server-owned, versioned default chapter instructions with optional customization.
Pending browser intents preserve their version, request UUID and exact custom text across reloads.
New runs freeze their editorial policy independently of the existing public run config and
request hash; old runs retain legacy behavior. An independent critic assesses actual compiled
cuts, grounds findings in source IDs, and triggers a scoped, metered repair before rendering.
The repaired edit is assessed again. Timing repairs select existing candidates, preserve
unaffected physical cuts and refuse unchanged/repeated candidates. The finalizer now verifies
the actual retained model response and its family, inputs and technical-report lineage.

Removed mandatory summary calls when the complete transcript fits the qualified planner.
Measured the retained 43:56 source: all 579 sentences and 6,203 words fit one 87,718-byte compact
request, replacing eight summary windows. The admission measurement bought no model calls.

Added independent speech evidence over the verified master with explicit selected-track/source
clock mapping and exact sample length. Legacy extracted audio had no saved source mapping and
could not honestly supply this evidence. Unknown timing stays unknown. Source edges now receive
the same uncertainty checks, with explicit speech-contact risk rather than a claim of clipped words.

Integrated the official pinned Chapter-Llama ASR adapter and Llama 3.1 base through a local
inference/evaluation seam and isolated durable Modal candidate activity. The optional worker
configuration freezes deployment/model/resources; generation has a reservation, physical attempt,
create-only admission, retained handle/result and portable evaluation lineage. It is never enabled
by the recorded CI backend. The configured HF account initially received `403 GatedRepo`; Rajesh
granted access and the pinned configuration/weight checks returned HTTP 200. Pinned model prefetch and the first real inference audition both succeeded. The candidate remains
opt-in: it combined an earlier transition and separated a question from its answer in this sample.

### What went wrong

The original harness had construction checks and a text verdict but no automatic semantic repair
program, and chapter evidence did not consume independently measured speech. Its fixed sentence
windowing also bought summaries unnecessarily. The implementation review caught two additional
problems before release: timing corrections could have reverted earlier cuts outside the affected
scope, and a submitted v2 verdict could have differed from the retained response. Both now have
regression tests. A read-only metadata extraction initially used an artifact helper that takes a
row lock; it was corrected to scoped read-only SQL plus size/hash-verified object reads.

The first six live editorial probes all returned valid strict JSON, but only two passed source
reference grounding. Models confused compiled cut IDs with candidate IDs, over-attached a
source-edge finding, or supplied unnecessary timing overrides during a structural repair. The
validators correctly refused those responses. Generic v2 prompts now supply explicit compiled-cut
ownership and conditional source-edge references, and distinguish structural repairs from timing
overrides. The guide is not a list of defects. The original v1 requests/results remain retained;
current reconciliation accepts their original journal identities without rewriting them.

The first real default-brief workflow made five model calls and one automatic repair. It moved
the congratulations/reply cut from 129.480 to 131.640 seconds and proposed dropping only the
1.960-second unfinished source tail. Four kept chapters passed all 68 technical checks. Portable
export initially rejected the retained v2 verdict field; the reader now retains that typed body
alongside its verified v1 projection and preserves old body hashes when the field is absent.
The existing run was recovered without another model call, and all four files were copied to
Downloads with matching hashes. Human acceptance remains false.

Auditing the final critic against the saved evidence caught an unsupported timing claim: the
238.080-second tail cut lies in the detector gap from 236.734 to 239.586 seconds, not continuous
speech. The separate 131.640-second cut really does intersect a detector interval. The earlier
post-repair critic had returned an empty pass. This disagreement motivates deterministic local
cut facts and a compact v3 context, rather than accepting the final model's arithmetic. The
global detector comparison uses aligned word intervals, so uncovered time includes inter-word
gaps and is not a measure of proven missing transcription.

Independent v3 review caught raw-safe candidates that become unsafe on the actual frame grid,
plus a real-but-unshown candidate that the earlier repair validator would accept. Options now
use quantized cut facts, omit unchanged physical cuts and require supplied-option membership for
new repairs. The same review found that a prompt bump could reject an already paid v2 response.
Prepared plans now retain the prompt identity, and finalization applies the recorded generation's
rules. Replaying the completed 135-event default run preserved all 21 activity names/order and
all five model dependency/request hashes. Reusing both saved assessments, the repair compilation
and final verification returned the same accepted IDs and hashes, including the old false claim.
The source still has 27 artifacts, five dispatches, one repair, $0.028081 recorded cost, zero
reserved and no human acceptance; no provider was called during these compatibility checks.

The live v3 suite produced six strict-schema responses and five grounded results. DeepSeek's
repair identified the correct tail-only drop but added anchors to an unaffected section whose
original quote array was empty. The validator correctly refused it. The repair prompt advances
to v4 with endpoint-anchor guidance restricted to changed sections; the assessment stays v3.
No rejected response is coerced or substituted. Earlier qualification journals remain reconcilable.

The next real default-brief run caught a compiler defect. Its critic correctly named an internal
speech-risk cut and the incomplete tail. The repair changed only the final keep and added the
explicit tail drop, preserving every earlier section exactly. Compilation nevertheless rejected
the untouched 126.600-second cut as a new risky constraint, so no repair was applied. Its original
four chapters still passed 68 construction checks; the run is retained as an unsuccessful repair,
not a working result. The compiler follow-up separates prior accepted cuts from new timing choices,
retains their exact times/review flags and binds the preserved proposal/edit identities into artifact
lineage. A Temporal patch keeps old repair requests on their original compilation policy.

### Numbers and qualification limits

- Combined pipeline preflight: 1,006 tests passed and 11 skipped in 141.17 seconds. The full
  exact-commit gate also enables the pinned CPU-model tests and production-image browser journey.
- 50 focused web tests, TypeScript and Biome passed for default/custom creation and request identity.
- 69 focused source-speech/media tests passed, including real ffmpeg 8.1.2 offset and gap fixtures.
- Pinned Silero smoke: 8.824 seconds, 141,184 samples, 276 windows and three speech intervals;
  0.077 seconds observed for decode/inference after model load, not an end-to-end benchmark.
- 72 editorial/compiler/receipt tests passed, followed by seven Postgres/MemoryStore integration tests,
  including a Temporal critic/finalizer replay with one recorded provider invocation and identical output.
- 63 focused run/persistence/Temporal tests passed, including explicit owned known-failure
  reconciliation that preserves other unknown exposure. This did not touch old staging attempts.
- Four retained production histories replayed: 77, 191, 361 and 217 events.
- First live editorial suite: six calls, 6/6 strict JSON shapes, 2/6 grounded results;
  $0.12863983 exact gateway cost ($0.128642 after per-call microdollar rounding), no unresolved
  outcomes. The versioned request correction is followed by a separate v2 qualification suite.
- V2 live editorial suite: six calls, 6/6 strict JSON and reference-grounding passes,
  $0.07352081 exact gateway cost ($0.073523 rounded), no retries or unresolved costs. GLM identified
  the incomplete synthetic tail as repairable; Kimi identified it but requested instructions;
  DeepSeek missed it with an empty passed verdict. All three separate repair probes removed only
  the fragment. The 12 calls total $0.20216064 exact / $0.202165 rounded. Transport/reference
  qualification does not establish editorial accuracy or promote a model to a seat.
- First real default run: five calls, one repair, $0.02807729 exact gateway / $0.028081 rounded
  ledger cost, zero reserved; 181,343 input tokens and 12,947 output tokens including 10,117
  reasoning tokens. The repair alone consumed 72,637 input tokens, motivating context reduction.
  Kept durations: 58.960, 72.680, 28.920 and 77.520 seconds. The run is still unaccepted.
- V3 context measurement on the same retained inputs: initial assessment 92,291 to 25,671
  UTF-8 bytes (-72.18%); repair 207,567 to 29,663 (-85.71%); final assessment with the same
  technical report 114,643 to 43,866 (-61.74%). All 51 sentences, their IDs, times and endpoint
  anchors remain. These are prompt byte measurements, not measured V3 token savings.
- V3 live suite: six calls, 6/6 strict schemas and 5/6 grounded outputs; $0.10895528 exact
  gateway / $0.108958 rounded, no unknown outcomes or retries. GLM and Kimi each identified the
  repairable synthetic tail and their repairs passed. DeepSeek again missed the tail as critic;
  its repair was rejected for changing the unrelated empty quote array. The owned qualifier
  sidecar was removed after receipts were retained. Three suites total 18 calls and $0.31111592
  exact / $0.311123 rounded; none establishes a general editorial winner.
- Final qualification generation 4 (assessment v3, repair v4): six strict schemas, five grounded
  outputs, $0.06465401 exact / $0.064657 rounded and no unknowns or retries. The selected
  DeepSeek repair preserves the untouched empty quote array and proposes only the tail drop;
  the selected GLM critic identifies that repairable fragment. GLM's separate repair omitted
  the explicit drop section and failed exact cover. Kimi's critic requested instructions for the
  fragment; its independent repair passed. The report's overall `passed` remains false, and the
  exact selected-role successes authorize the real audition without promoting non-selected roles.
  All four qualification suites total 24 calls, $0.37576993 exact / $0.375780 rounded.
  The final same-input repair prompt is 29,858 bytes, 85.62% below the original 207,567 bytes.
- Second real default run: four settled calls, one rejected repair attempt, zero applied repairs;
  $0.00825494 exact / $0.008257 ledger, zero reserved, 34,149 input tokens and 8,607 output
  tokens including 5,814 reasoning tokens. It used a three-repair allowance, but the known compile
  refusal stopped that attempt. The paid response, refusal, original outputs and unaccepted
  state are preserved. Lower cost alone does not make this run a quality improvement.
- The rejected repair was reproduced offline, then compiled under the new preservation policy.
  The prior cuts at 58.960, 126.600 and 160.560 seconds retained their exact times, candidate IDs,
  reasons and review flags; the 126.600-second detector crossing remains visible. The only new
  section is the proposed tail drop. This proof made no model call and changed no saved run.
  Both completed real histories replayed with the old policy, retaining all nine paid request
  identities and the original successful/refused compile outcomes.
- The fresh third default run generated the desired tail drop before the release gate began:
  its first proposal failed quote ownership, the existing automatic proposal repair recovered,
  and the resulting plan kept sentences 0–49 and explicitly dropped sentence 50. Its cuts were
  0, 58.960, 108.160, 160.560, 238.080 and 240.040 seconds. This was a model-generated plan,
  with no operator partition; rendering and the independent final verdict were still pending at
  this observation. The final real-run outcome and exact-SHA gate receipt are recorded on the PR.
- Portable-v2 follow-up: 82 focused bundle/evaluation/editorial/candidate tests passed,
  Ruff/Pyright clean, and independent review found no remaining reader defect.
- 34 Chapter-Llama contract/evaluation/lifecycle tests passed, with one existing optional-model skip;
  the separate candidate/bundle review reports 63 focused tests. These suites overlap.
- Real Chapter-Llama attempt: 1,064 input / 32 output tokens; 41.147 seconds load, 4.236 seconds
  generation, 45.434 seconds compute. Listed allocation estimate $0.030234; invoice unknown.
  Proposed starts 0:00, 1:57 and 3:23; not promoted as the default planner.
- The combined exact-SHA release gate follows this commit. Its receipt and PR checks are the
  authoritative release result. No M1 completion or autonomous real-video acceptance is claimed.

### Next

Run the corrected program on the known excerpt using only the default brief, inspect the actual
ending and then exercise the retained long recording. Complete the release gate and send the
concrete PR for Rajesh to merge. After deployment, verify the same default action on staging.
Measure first-pass quality, repairs, human review and costs separately. Human acceptance and
export remain explicit review actions.

### Revised social draft material

X: A playable chapter can still end halfway through a thought. That happened in Temnia's demo.
The follow-up now checks the proposed edit, grounds the defect in source words and repairs it
before rendering. Tests prove the program's boundaries; real video review still decides quality.

LinkedIn: The useful feedback today was a challenge to the workflow: if I supply the correct cuts
by hand, I haven't shown that the editing harness is intelligent. Temnia now has a default chapter
action and a typed critic/repair program over the actual cut plan. We also found unnecessary work:
one retained 43-minute transcript fits the model context directly, avoiding eight summary calls.
The next evidence is the automatically produced video, followed by human review—not a test count.
