# September 12, 2026 — Full-source model evaluation exposes editorial gaps

Continuation after midnight in India of the September 11 session. The
[previous log](2026-09-11.md) records the OpenRouter transport implementation and
qualification. The [experiment plan](../plans/openrouter-model-evaluation.md) and
[evidence record](../design/openrouter-topic-model-evaluation-2026-09-11.md) retain
the declared conditions, original failures and accounting distinctions.

## Done

Ran three declared model configurations on the full 43:56 Karma recording through
the production topic workflow. Astra/Gemini and Kimi/Gemini were the original
two-arm comparison. A separate Gemini/Kimi configuration was declared before
inspecting Kimi's candidate content; both author and reviewer change explicitly.
No additional qualification calls were needed for it.

Kimi/Gemini produced eleven original candidates and eleven rendered review videos.
Its scoped repair was rejected, so the final state is `needs_review`, not editorial
acceptance. Gemini/Kimi proposed four candidates and reached independent review,
then its repair exhausted the declared output allowance; no edit or render followed.
Astra's author stream reported a network error with its early generation ID retained;
it produced no selection and its original unknown fence remains active.

Exported and validated all three terminal bundles, unfilled human-label templates
and unlabelled reports. Ran the original native model comparison and the separately
declared configuration comparison. Both retain missing human measurements and report
no winner. Prepared a local review packet with actual Kimi media links, content-only
candidate transcripts and separate diagnostic material. Media playback and human
acceptance have not been performed. All three owned trial workers stopped gracefully
after terminal/no-pending-work checks; a fresh inventory confirmed none running.

## How

Every arm used the same source/transcript bytes, evidence, generic brief, rubric,
editorial programme, AdaptiveDetector and declared request profile. Native `/4`
qualification binding retained both complete original reports and all 82 receipt
aliases. Actual transcript bytes were verified separately from the original null
database hash; no historical metadata was rewritten. Starts used stable identities,
isolated queues and sequential owned workers. The ordinary staging configuration
remained unchanged.

An assistant froze a whole-source opportunity inventory before the new outputs.
Two assistant inspections then froze selected-content diagnoses before reading each
configuration's model critics. These were source-familiar, text-only development
inspections, with prior Kimi exposure acknowledged for Gemini. They were never fed
back into the running programme and are not independent human gold.

The full clean-commit local gate passed for implementation checkpoint `e7cf00b`
at 19:17:43.687 UTC on September 11: sixteen package tasks, 1,590 Python tests with
one optional detector-agreement skip, both production images and twenty browser
tests. Final evidence additions are delivered through the same exact-commit gate
and verified PR workflow; Git's receipt and required check identify the delivered
commit. No merge or social post is performed by this session.

## What went wrong

Both authors selected the same wealth discussion ending on a fatalistic premise
before the answer. Kimi also separated an opening from its prayer antecedent and
a warning from a later positive qualification. Gemini did not create a prayer
opportunity until its source critic found one; an earlier action/stillness answer
remained without an opportunity. Another developed practice discussion was discovered
but rejected as not contiguously extractable. These different loss points need
different evaluation; counting selected videos does not measure recall.

Individual critics caught concrete defects while source critics sometimes passed
them. In Gemini's run, source review also demanded wealth context over seven minutes
earlier while individual review passed the opening. Its newly discovered prayer
opportunity marked another question as completion. These are source-grounded
calibration concerns, not adjudicated critic error rates.

Kimi's actual patch changed protected opportunity evidence. The validator correctly
rejected the atomic patch, but the prompt asks for updated opportunities and the
schema exposes full records without stating which fields are editable. A pure offline
replay reproduced the refusal; retaining original evidence definitions made the
candidate extensions structurally admissible. That diagnostic copy was never
persisted as a result. The attempted wealth extension would still omit its next
qualification. Separately, the current repair vocabulary cannot directly trim one
candidate or replace both edges in one operation.

Gemini's real repair returned `finish_reason=length`: 32,754 output tokens included
31,454 reasoning tokens against the 32,768 allowance. Its charge settled before the
normalization guard refused truncation. This was not a timeout, unknown expense or
patch-validator rejection. Small exact-schema qualification did not predict whether
this much larger repair request would finish.

## Numbers

| Scope | Requests | Settled model expense | Actual outcome |
| --- | ---: | ---: | --- |
| Two OpenRouter qualification cohorts | 21 | $0.588704 | Three routes pass all four stages in the second cohort; original failures retained |
| Astra author / Gemini reviewer | 1 | $0 | Unknown stream outcome, no selection |
| Kimi author / Gemini reviewer | 14 | $1.027687 | Eleven review videos; repair rejected |
| Gemini author / Kimi reviewer | 7 | $0.619309 | Four initial candidates, zero videos; repair truncated |
| OpenRouter session total | 43 | $2.235700 | No model winner or human acceptance measurement |

Two original unresolved calls retain **$6.001793 estimated exposure**: first-cohort
Opus $0.992173 and full-source Astra $5.009620. Separately retained provider receipts
report Opus $0.040225 and Astra $0; those observations do not overwrite the original
journals or settle the fences. Older Vercel expenses and infrastructure/rendering
charges are outside this table.

Kimi's eleven renders total 309,293,430 media bytes and pass 187 technical checks
(seventeen each). All twenty-two edges still require speech-coverage review. There
are no human labels, accepted revisions or observed playback judgments. Technical
delivery therefore does not supply a publication-quality score.

## Production-quality continuation

Implemented `standalone-topics/3` from the observed failures instead of continuing
the model comparison on the same defective program. The new workflow inventories
the whole source independently, removes author rationale from portfolio review,
uses a compact source-review contract, supports both-edge extent replacement, gives
repair only finding-authorized source windows, preserves opportunity evidence while
accepting mapping changes, and defaults to three bounded repairs. A strict gate
withholds candidates without an explicit source selection or with a required
finding.

The completed Karma run `0c3a9707-30b9-558f-b0e3-1098bd1c04e2` used Kimi K3 as
author and Gemini 3.8 Flash as independent inventory/reviewer through OpenRouter.
It spent $0.652119 across 18 attempts and rendered ten MP4/VTT pairs. Initial review
found incomplete karma-fundamentals and wealth endings, a missing three-strands
opening and unsafe physical edges. One live repair was accepted, the changed
candidates received fresh cold review, and a fresh source review completed.

The final reviewer left one `preference`: the wealth candidate continued through
s349–s356 after its topic ended at s348. Direct transcript inspection showed that
the tail starts a different sports and discipline discussion. The policy now
promotes a source-relative `unfocused_extent` on a selected candidate to a required
publication correction. Replaying the immutable r11 selection and review under that
rule admits nine videos and withholds wealth. The admitted media total 36:37 and
286,069,192 bytes.

Downloaded all ten MP4s and captions from their recorded storage descriptors and
verified every size and SHA-256. All ten fully decode as 1280×720, 25 fps H.264 with
stereo AAC; caption times stay within their media. Inspected twelve-frame contact
sheets and every selected transcript boundary. The dated Downloads review packet
contains the nine-video set, captions, transcripts, technical audit, contact sheets,
the exact r11 bundle and the publication-gate report.

The clean r13 run matched the same source, fingerprint and evidence identities and
tested the final focus normalization. An upstream Gemini rate limit arrived inside
an OpenRouter HTTP 200 stream during cold review. Temnia fenced the run as
`outcome_unknown`; it was not retried. The failed bundle records five completed
attempts, $0.586809 settled expense and $0.137916 unresolved exposure. An r12
prepare attempt made no model call: the qualification identity rejected a temporary
prompt change before workflow start, and the final implementation removed that
unnecessary request-shape change.

### Additional measured runs

| Run | Settled expense | Result |
| --- | ---: | --- |
| First v3 production run | $0.918466 | Ten renders withheld; source review truncated twice |
| r8 | $0.657911 | Five renders; required defects withheld, rejected repair retained |
| r9 | $0.538582 | `outcome_unknown`; $0.211014 exposure retained |
| r10 | $0.873347 | Eight renders; repair validator exposed immutable-evidence drift |
| r11 | $0.652119 | Ten renders; nine admitted after corrected publication-gate replay |
| r13 | $0.586809 | `outcome_unknown`; $0.137916 exposure retained |

These six v3 runs settled $4.227234. They are implementation experiments, not a
model-win comparison. Infrastructure and rendering costs remain outside this table.

## Next

Review the nine admitted Karma videos as the first concrete publication set and
record accept/reject reasons per video. Then run the frozen v3 policy on fresh full
sources and measure opportunity recall, independent-video acceptance and repair
yield. Keep the OpenRouter upstream-rate-limit failures as transport evidence when
deciding whether to add provider credentials or qualify another reviewer route.

### Human playback result and final release refinements

Rajesh reviewed the nine admitted videos. He found the cuts technically almost perfect:
every video played as a complete segment without a broken sentence at either edge. Two
remaining defects were concrete. A small tail of Sakala Maa's end pause appeared at the
next video's opening, and the prayer video retained the start of the mantra discussion
that should instead open the mantra-and-karma video.

The first issue traced to the generic compiler choosing the midpoint of a safe silence.
V3 now chooses the latest safe 25 fps boundary before the next utterance, so the preceding
video owns the available pause and any unavoidable opening residue is under one frame.
The second issue is recorded as semantic extent ownership rather than blanket duplication.
The V3 author, independent source reviewer and patch contracts now require the earlier
candidate to stop before a new topic and the later candidate to begin at its earliest
necessary premise. One grounded finding may authorize both coordinated extent changes.

Focused validation added a real-grid pause-ownership case and an atomic two-candidate
handoff repair case; all 116 topic compiler, feasibility, selection, qualification,
workflow and review tests passed. The web unit suite also passed 250 tests with five
skips, and the affected Python and TypeScript type checks passed.

The first exact repository gate found one lifecycle mismatch that the focused compile
test did not cover: after a V3 video was retitled by a human, the correction workflow
recompiled it with `topic-compiler/2`, so unchanged media was rendered again. The patch
workflow now preserves the originating compiler generation; its test matrix covers V1,
V2 and V3 and confirms a title-only correction reuses the physical media.

The full follow-up Karma run `dd868c83-b207-5d5f-a961-d56cdb341224` then supplied the
counterexample the focused prompt test could not. It matched the frozen source, fingerprint and
evidence, spent $1.188331 over 22 settled attempts with no reservation, and completed in
`needs_review`. The final selection started the mantra video at s237 but still ended the prayer
video at s247: eleven transition sentences remained in both. Gemini explicitly selected both and
reported both opportunities as represented, despite the prompt's semantic-ownership instruction.
It did catch the analogous s349-s356 wealth/surrender overlap. Because that required finding named
both candidates, the gate withheld both and rendered eight videos.

Replaced the prompt-only expectation with a typed portfolio obligation. Code now enumerates every
exact candidate overlap from immutable extents. V3 source review must echo and classify every pair;
misallocated extent and duplicate core require a grounded required finding naming both candidates,
and an omitted, altered or duplicate classification invalidates the entire source review. The V2
schema is unchanged. Source and repair prompts advance to `/6` and `/7`; their exact OpenRouter
requests and another Karma result remain required before this correction is accepted.

The next run, `b062492f-55dd-50d1-809b-f3d87558fcf2`, matched the same frozen source and evidence,
spent $0.703027 over 12 settled attempts with no reservation, and completed in `needs_review`. Its
initial author selection correctly ended prayer at s239 and began mantra at s240, with no overlap.
The reviewer then found required repairs on four other candidates. One karma-foundations candidate
needed both an ending extension and a title correction. Kimi emitted separate `replace_extent` and
`retitle` operations, and the validator refused the patch because a candidate may be affected only
once per atomic transaction. The last valid selection was retained and only unaffected videos were
rendered.

Added a bounded `replace_candidate` operation to express that coupled repair once. It keeps the
candidate ID, remains inside finding-authorized source, and requires an extent-related finding for
content changes plus an `unsupported_title`, value or focus finding for each changed title or
purpose. Repair advances to `topic-selection-patch/7`. The exact patch request and a fresh full run
remain required.

Strengthened the synthetic qualification case so it requires one candidate's incomplete ending and
unsupported title to be corrected together. A complete 15-call cohort passed every exact stage on
Astra, Gemini and Kimi for $0.570853. Each patch response used `replace_candidate`, cited both
findings, retained the ID and extended through the supplied completion sentence. This supersedes an
earlier $0.113730 patch-only pass whose old fixture exercised only `retitle`.

The r16 Karma run `c80b211d-453e-57b8-832e-22c1c8a81936` matched the frozen source and evidence,
spent $1.487824 over 25 settled attempts with no reservation, and completed in `needs_review` with
zero rendered videos. Initial prayer and mantra candidates did not overlap, but mantra opened with
the dependent “And depends…”. Three repairs chased antecedents backward until mantra began at s232
and overlapped prayer through s247. An intermediate source review called s245–s247 necessary shared
context even though those sentences were prayer completion and mantra setup. The final source output
failed its required schema, so the publication gate admitted nothing.

Closed both control gaps. `necessary_shared_context` now requires the entire overlap to appear in
both candidates' `requiredContextSpans`; core or completion has one owner. At a topic handoff, author
and repair must trim connective or anaphoric runway to the first self-contained new-topic statement
when extending backward would annex a completed neighbouring discussion. Prompt versions advance to
author `/6`, source `/7` and repair `/8`; their exact changed stages and another full Karma run remain
required.

The next full run, r18 (`2291832e-84dd-53ad-813a-0fc61e8483c6`), matched the same frozen
43:56 source, fingerprint and evidence. It spent $0.922924 over 22 settled attempts with no retained
reservation and rendered 11 videos using `topic-compiler/3`. The fifth candidate initially began at
s248, “And depends on what sadhana we do.” Cold review rejected that dependent opening. Repair
trimmed it to s251; source review then required the online-sadhana setup at s248–s250; the next repair
restored the bad opening. A third repair finally began the video at the self-contained s249, and its
fresh cold and source reviews passed.

That apparent success preserved the defect Rajesh described. The prayer video still ended at s247,
including the preceding sadhana transition, while the mantra-and-karma video began later at s249.
The final source reviewer returned no findings and the repair summary explicitly assigned s240–s247
to prayer. Candidate-local completeness and exact-overlap accounting therefore cannot adjudicate a
non-overlapping semantic handoff. R18 is retained as a false-acceptance counterexample; it does not
undo Rajesh's separate playback result that the delivered cuts were sentence-complete.

Added a typed adjacent-handoff contract. Code now orders candidates by source position and supplies
the left tail and right head for every consecutive non-overlapping pair. The source reviewer must
classify every row and, for a misallocation, return exact final sentence IDs for both neighbouring
edges with one required two-candidate finding. Repair must implement those exact edges atomically or
the patch is refused. The historical overlap-only response remains readable; current V3 source and
patch prompts advance to `/8` and `/9`, and the native portfolio schema advances to
`topic-selection-portfolio/4`. The qualification fixture now includes a real adjacent pair, so an
empty handoff list cannot qualify the new source request.

Qualified the exact five-stage program on all three production route families. The r20 cohort
passed 14 of 15 calls for $0.629171; Gemini author received a conclusive HTTP 429 with no generation
charge. A separately retained replacement call passed for $0.011006, producing a fully bound
15-observation qualification manifest. Accepted qualification spend was $0.640177; the failed call
remains visible rather than being rewritten as a pass.

Ran the complete r20 Karma workflow with Kimi author/repair and Gemini inventory/review. It matched
the same source, fingerprint and evidence, settled 19 attempts for $0.907369, retained no reservation
and finished `needs_review` with seven renders. V4 correctly moved the perfection premise from the
Shani candidate into the next candidate, but the model pair had already combined prayer and mantra
into one s202–s312 video. The final reviewer called that combined span a clean distinct topic. This
fails Rajesh's requirement that the prayer video hand off the full mantra discussion to a separate
mantra-and-karmic-collision video. It also proves the current typed handoff cannot address a split
that is absent from both inventory and proposal.

Prepared and started r22 as a controlled author substitution: Astra author, Gemini reviewer, with
the same source, program, prompts, schemas, qualification calls, detector, compiler and execution
settings. The fixed Gemini inventory settled for $0.057453, but Astra's HTTP 200 stream exceeded the
frozen 540-second aggregate deadline without a complete response or a generation handle. Run
`294dcaff-4b8c-5ebe-8974-5cabf3b6fd1e` is `outcome_unknown` with its $5.271530 reservation retained.
No inference was replayed. This is the second full-Karma Astra transport failure despite successful
short qualification calls, so Astra is unavailable for this workload.

Ran r23 as a declared configuration comparison with Gemini author and Kimi inventory/review. The
fixed Kimi inventory settled for $0.235307. Gemini then returned HTTP 200 and an upstream rate-limit
error before a complete author response. Run `6f156f15-b07a-5f00-a3ce-cfc0412d4d3b` is
`outcome_unknown` with its $0.213370 reservation retained. No inference was replayed and no
selection exists to score.

Added DeepSeek V4 Pro 0813 through Fireworks as a declared challenger after verifying it in the live
OpenRouter catalogue. The exact inventory, author, cold-review, source-review and patch requests all
qualified for $0.144163. Started r24 with DeepSeek author/repair and Kimi inventory/review while
preserving the same Karma source, program, prompts, schemas, detector, compiler and execution
settings.

Run r24 (`02ad8512-6cca-5248-b845-1aa59a5380ae`) settled Kimi inventory, DeepSeek authoring and 20
Kimi cold reviews for $1.235900. DeepSeek proposed 20 candidates. It separated prayer at s223–s240,
but overlapped the following sadhana/mantra-risk candidate at s238–s267 and divided the connected
mantra discussion across that candidate, spiritual window-shopping at s268–s292, and karmic
collision at s293–s312. Kimi's full-source review then crossed the frozen 540-second deadline
without a complete response or generation handle. The run is `outcome_unknown`; its $1.015747
reservation remains active and no patch, final review or render followed. No inference was replayed.

Prepared r25 as the final controlled pairing: DeepSeek author/repair with Gemini inventory/review.
The route snapshot changes only reviewer ordering, binds the already retained exact qualification
reports, and keeps the Karma source, program, prompt, schema, detector, compiler and execution
settings fixed. Its run is `919ecbcc-e41b-5e10-8479-a18304bc22e0`.

R25 completed 13 calls for $0.430738 with no reservation and rendered nine videos. DeepSeek merged
prayer, continuous sadhana, unguided mantras and karmic effects into one s223–s308 candidate. Gemini
explicitly accepted the compound span as complete and distinct and classified all eight external
handoffs as clean. Its only required finding concerned another candidate's dependent opening;
DeepSeek's repair was invalid, so the selection did not change. The run ended `needs_review` with
selection `dbd9229b395b32c82f929595a12d1e62a5b7cc0308a6fdb66582768faa96361f` and renders
`0819b418956e1aad771cca95fe302785681783efe0d7959cb6618d46fcf176e4`.

Stopped the model-arm loop. No tested author/reviewer configuration met Rajesh's prayer-to-mantra
ownership requirement. The remaining measured gap is internal topic structure inside one compound
candidate; the new adjacent-handoff contract cannot observe a boundary that no candidate represents.

## Revised X draft

Temnia now produced nine reviewable standalone videos from a 43:56 podcast. The
editor found topics, an independent critic caught incomplete edges, the editor
repaired them, and fresh reviews ran before rendering. One extra video was withheld
because its ending drifted into another discussion.

## Revised LinkedIn draft

Temnia's new full-source editorial program produced nine reviewable standalone
videos from a 43:56 podcast.

It first mapped the source without seeing an editor's choices. Kimi then proposed
the videos. Gemini independently found incomplete endings, a missing opening and
unsafe cut edges. Kimi repaired the selection, and the changed videos plus the full
portfolio were reviewed again before rendering.

The final review still called one off-topic tail a preference. The publication gate
now treats that source-grounded focus defect as required and withholds the video.
All delivered MP4s were hash-verified, fully decoded and inspected with their exact
transcript boundaries. Viewer review is next, followed by the same frozen test on
fresh sources.

Drafts only; no content was posted.
