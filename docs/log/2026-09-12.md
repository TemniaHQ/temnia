# September 12, 2026 — OpenRouter full-source model audition

Continuation after midnight in India of the September 11 model-evaluation session.
The [previous log](2026-09-11.md) records the transport implementation and two
qualification cohorts. The [experiment plan](../plans/openrouter-model-evaluation.md)
and [evidence record](../design/openrouter-topic-model-evaluation-2026-09-11.md)
retain the declared conditions, original failures and accounting distinctions.

## Done and how

The exact trial image passed fresh source/runtime verification. Both Astra/Kimi
arm configurations passed the production `/4` qualification binder against both
complete original reports and all 82 retained receipt aliases. Prepared new stable
run IDs through the existing production operator, preserving the original null
transcript-hash metadata and verifying actual transcript bytes separately.

Started Astra's full 43:56 Karma workflow once, after checking the exact worker,
isolated queues, current source/transcript bytes and frozen configuration. Its run
ID is `b8a22df8-c82e-5c4b-a01a-019318a15a30`; Gemini is its reserved independent
reviewer. The initial observation was source-evidence processing, with zero settled
model cost and no active model reservation at that point. The following sections
record later outcomes; a successful start is not an editorial result. No content
was posted.

## First full-source outcome

Astra reached authoring with evidence byte-identical to the previous Karma run.
Its request was dispatched at 18:33:21.281 UTC on September 11 (00:03 on September
12 in India), and the stream opened with an early generation ID. At 18:36:34.375
the SDK reported an SSE error, `Network connection lost.` The request lasted
193.094 seconds, below both configured client deadlines. The exact network hop is
not established by this error.

The workflow ended `outcome_unknown` with no completed selection or videos. The
generation handle survived; the original $5.009620 estimated reservation remains
active and no charge is yet settled. Zero settled cost does not mean zero expense.
The original exception chain and terminal artifacts are retained. No inference
was repeated. Kimi remains the separate next author arm under the frozen plan.

One later read-only accounting request returned a matching OpenRouter receipt that
reports **zero charge**, with 20,829 input tokens and 204 reasoning/output tokens.
It reports no completed finish reason. The observed zero and original ledger fence
are retained separately; no inference or ledger reconciliation followed. The
terminal evaluation export validated, its human-label template remains unfilled,
and the owned Astra worker stopped gracefully with no pending work. Kimi's worker
then passed the same source/image/queue readiness checks.

## Kimi's first full-source review cycle

Kimi completed authoring in 222.512 seconds for $0.247932 and proposed eleven
initial videos. All eleven Gemini individual reviews and one source/portfolio
review completed, giving thirteen settled calls and $0.647422 at the end of the
first cycle. There were no unknown outcomes in that cycle. A scoped Kimi repair
then began under the unchanged programme; its result is recorded separately below.

Two assistant inspections froze text-only diagnoses before reading the model
critic opinions. They independently identified the same opening/context,
warning/qualification and unfinished-ending problems. The pre-output source
inventory also distinguishes a worthwhile prayer discussion being buried in broad
groupings from its words being entirely absent. This is useful diagnostic evidence,
not measured opportunity recall, human preference or playback acceptance. Neither
inventory nor diagnosis was supplied to the running harness.

One additional Gemini-author/Kimi-reviewer configuration was declared before
inspecting Kimi's candidate text. Both roles change explicitly, and the existing
32-dispatch programme's price envelope yields a $71.755776 admission allowance.
The comparison retains normal human metrics as unmeasured. It is separate from the
original Astra/Kimi comparison and reuses the same twelve native qualification
proofs; no extra qualification inference was needed.

## What went wrong and current numbers

Astra's source request ended with a reported stream error. Kimi's first proposal
contains context and completion defects that request qualification cannot detect.
Individual Gemini reviews caught two concrete defects while source review passed
every candidate. The frozen repair vocabulary also cannot directly trim either
edge of a single video. These are separate transport, editorial-judgment and
implementation findings, not one generic model failure.

Kimi's patch request subsequently settled at $0.380265 after 290.424 seconds,
bringing that run to fourteen settled requests and $1.027687 when rendering began.
Patch admission, final selection and rendering outcomes were still pending
inspection at this checkpoint. The separate Astra unknown reservation remains
$5.009620 despite its later zero-charge observation. September 11's qualification
expenses are recorded separately in that day's log. A clean-commit full local gate
is next; earlier focused checks are recorded in the evidence document.

## Next

Finish the declared configurations, preserve initial-to-final selection and
repair evidence, and review actual rendered outputs where available. Use human
opportunities and publication judgments before naming a winning model. The new
diagnoses need measured follow-up on critic sensitivity and useful edit operations;
they do not justify tuning prompts against one exposed recording and calling it
general improvement.

## X draft

Testing Temnia on a full conversation exposed a gap that JSON tests miss: a video
can start with “it” while its explanation stays in the previous video. The local
reader caught it; the full-source reviewer passed it. Now we can test the judgment,
not just whether the API returned.

## LinkedIn draft

Today's Temnia model evaluation is producing useful failures. One transport call
ended mid-stream, with its generation identity retained. Another model selected
eleven topic videos, but some boundaries separated setup from explanation or a
warning from its qualification.

Individual reviews caught concrete problems that the full-source review passed.
We also found that the current repair contract can extend a video but cannot
directly trim it. A stronger model cannot choose an edit the system cannot express.

These are development findings, not a model leaderboard. Human publication
judgments and fresh recordings still have to establish whether the outputs improve.
No post was published.
