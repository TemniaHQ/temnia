# First live chapter run: failure fixes — 2026-09-09

The 151-minute staging run stopped after seven summary responses. The seventh supplied five
empty summary-unit labels, which failed local validation. Seven immediate cost lookups did not
settle charges; later identity-checked lookups reported 4,196 microdollars. Evidence preparation
also recovered from an initial heartbeat timeout. No edit, render or export was published.

## Summary labels and immutable recovery

Summary-unit labels are bookkeeping generated by Temnia. They are not source sentence IDs or
word anchors. Derive them deterministically at the model activity boundary from ordered unit
positions and their original endpoint strings, after preserving the raw provider response and
its accounting evidence. Do not alter sentence endpoints, quotes, text, model identity, usage,
or the retained response. Keep strict semantic validation and exact-cover checks.

Apply the same transformation to fresh and reused responses, only for the summary schema.
Preserve valid unique labels verbatim; derive replacements for invalid bookkeeping labels
without changing any other value. This also preserves downstream request identity for older
runs whose summaries were already valid.
Keep the provider request schema, prompt version and request fingerprint unchanged so the
seven committed responses remain reusable. Existing Temporal histories retain the actual
activity result they recorded; the transformation runs inside the activity, not in workflow
replay. Invalid JSON, wrong unit shapes and invalid evidence anchors must still fail. This
avoids spending a repair call to generate internal labels; it is not permission to repair
editorial content or accept fabricated source references.

Verify empty, missing and duplicate labels, deterministic repeated normalization, preservation
of raw response/usage/anchors, malformed semantic fields, unrelated schema isolation, and
recovery of a persisted response without another provider dispatch. Replay a retained history
where feasible and exercise the actual Temporal model boundary in automated tests.

## Delayed gateway accounting

After durably saving a known response, allow bounded read-only polling when its exact generation
lookup says usage is pending. Share the gateway observation helper with qualification where
practical. Preserve strict model/provider/generation matching, transport timeouts, activity
heartbeats, cancellation, and the reservation when the lookup remains unresolved. Never retry
inference, infer a zero charge, release unknown exposure, or repeatedly query an identity error.
The runtime allows twenty seconds total for lookup and pending-receipt polling, with each request
also bounded by its configured timeout. Zero wait in qualification still means one lookup without
polling. Retain a sanitized error class when observation fails, since the first
live run's pre-reconciliation receipt did not preserve that diagnostic. The existing operator
reconciler remains the explicit recovery path after the bounded wait.

Verify pending-to-reported, bounded pending, BYOK/identity errors, cancellation, and persisted
response recovery. An accounting delay must not discard a known editorial result. Test actual
request counts separately from lookup counts.

## Evidence liveness and user states

The retained cancellation stack locates the timeout in the wait for an object-store stream
chunk. Chunk progress alone does not prove worker liveness during quiet I/O. Supervise the
complete leased evidence operation with the existing cancellation-aware heartbeat helper,
retaining chunk progress and the 30-second worker failure detector. Remove the narrower
segmentation pulse. Bound source-download header/chunk waits separately at three minutes;
the six-hour operation ceiling remains. Timeout/cancellation must remove partial files and
drain owned tasks before releasing the cache lease. Preserve existing verified-file reuse.
Test quiet I/O lasting longer than the heartbeat interval, stalled chunk timeout, and
cancellation cleanup through the real Temporal activity wrapper. No CPU isolation change is
needed for the observed failure.

Clear a start acknowledgement only when its matching durable run appears. Preserve unrelated
review and refresh errors. Show render preparation only after an edit exists, and technical
verification only after a render descriptor exists. Preserve invalid-artifact and acceptance
fences. The accompanying UI regression covers the real chapter journey.

## Delivery and staging

These changes belong to the existing PR #27. Preserve scoped database access and all immutable
source, route, run, operation and attempt identities. No migration, vendor switch, budget raise,
source correction, re-ingest or paid retry is part of implementing the fixes. Retain the failed
run and its reconciled charges. Update the qualification record and daily log with measured
facts, run focused regressions and the exact-commit release gate, and leave the PR for Rajesh
to merge. Staging recovery after deployment must reuse that run and its committed responses.
